---
phase: 08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - tests/test_backup_script.py
  - src/conf.d/webinterface.conf.in
  - DEPLOY-PI.md
autonomous: false
requirements: [D-04, D-05]
must_haves:
  truths:
    - "A backup taken after choosing an image restores that choice, so the setting survives container recreation and machine moves."
    - "The config file tells the reader that the Settings page overrides system_image."
    - "The deploy documentation states that the system-image choice lives in the persistent data folder and is included in backups."
    - "A human has confirmed on a live stack that choosing an image, saving, and reloading shows the new diagram without a daemon restart."
  artifacts:
    - path: "tests/test_backup_script.py"
      provides: "Round-trip assertion that a web.system_image row survives backup and restore"
      contains: "web.system_image"
    - path: "src/conf.d/webinterface.conf.in"
      provides: "system_image comment pointing at the Settings page"
      contains: "Settings"
    - path: "DEPLOY-PI.md"
      provides: "Data-folder and backup docs mentioning the stored GUI settings"
      contains: "system image"
  key_links:
    - from: "tools/pellmon_backup.py"
      to: "pellmon_settings.db"
      via: "existing SQLite backup API path, now proven to carry the new row"
      pattern: "pellmon_settings\\.db"
---

<objective>
Close out the phase: prove the new setting actually survives the backup/restore path, tell the
operator where it lives, and get a human confirmation on a live stack.

Purpose: D-05 is only half-satisfied by writing into `pellmon_settings.db` - the claim that the
choice "survives container recreation and is included in the backup tool" has to be tested, not
assumed. RESEARCH predicted `tools/pellmon_backup.py` needs no code change; this plan verifies
that prediction rather than trusting it. The phase also touches operator-facing config and docs
that would otherwise still describe a restart-required workflow.

Output: an extended backup test, updated config comment and deploy docs, a green full suite,
and a signed-off live verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-CONTEXT.md
@.planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-RESEARCH.md
@.planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-UI-SPEC.md
@.planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-01-SUMMARY.md
@.planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-02-SUMMARY.md
@.planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-03-SUMMARY.md

<interfaces>
From tests/test_backup_script.py:102-138 (the fixture and round-trip test to extend):

    def _make_rrd_setup(tmp_path):
        from Pellmonsrv.database import Keyval_storage
        data = tmp_path / 'data'; data.mkdir()
        rrd = data / 'rrd.db'
        subprocess.run(['rrdtool', 'create', str(rrd), '--step', '10',
                        'DS:t:GAUGE:40:U:U', 'RRA:AVERAGE:0.5:1:100'], check=True)
        settings = data / 'pellmon_settings.db'
        Keyval_storage(str(settings)).writeval('k', 'v1')
        conf = _write(tmp_path / 'config' / 'pellmon.conf',
                      '[conf]\ndatabase = %s\nconfig_dir = %s\n' % (rrd, tmp_path/'config'/'conf.d'))
        (tmp_path / 'config' / 'conf.d').mkdir()
        return conf, rrd, settings, Keyval_storage

    @needs_rrdtool
    def test_local_round_trip(tmp_path):
        ... pellmon_backup.main(['backup', '--local', ...]) ...
        ... pellmon_backup.main(['restore', '--local', '--yes', ...]) ...
        assert Keyval(str(settings)).readval('k') == 'v1'

    The module also defines a `needs_rrdtool` skip marker; reuse it, do not invent another.

From src/conf.d/webinterface.conf.in:36-43 (the comment block to update):

    [conf]
    # Select system image. Set an absolute path to use your own.
    # Choices: system.svg|system_nbe.svg|system_nbe_2w.svg|system_nbe_3w.svg|system_nbe_v7.svg
    #          |system_matene.svg|/path/to/your_own_image.svg
    ############################
    # Choose system image here #
    ############################
    system_image = system.svg

From DEPLOY-PI.md - relevant headings:
    line 139: ### Where PellMon keeps its data
    line 224: ### Backup and restore

From plan 08-01: setting key is `web.system_image`, read with
`Keyval_storage.getval(key, default='')`, written with `writeval(key, value)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the stored choice survives backup and restore</name>
  <files>tests/test_backup_script.py</files>
  <read_first>
    - tests/test_backup_script.py lines 1-40 (imports, `_write`, the `needs_rrdtool` marker) and
      lines 100-140 (`_make_rrd_setup`, `test_local_round_trip`)
    - tools/pellmon_backup.py (the `backup_local` / restore paths that handle
      `pellmon_settings.db` via the SQLite backup API)
  </read_first>
  <behavior>
    - A settings DB containing a `web.system_image` row with value `system_nbe.svg`, backed up
      with `pellmon_backup.main(['backup', '--local', ...])` and restored after deleting the
      originals, reads back `system_nbe.svg` via `Keyval_storage.getval('web.system_image')`.
    - The restored DB still returns the default for a key that was never stored, i.e. the
      restore does not resurrect or invent rows.
  </behavior>
  <action>
    Add a test to `tests/test_backup_script.py` named for the system-image round trip, decorated
    with the module's existing `needs_rrdtool` marker. Reuse `_make_rrd_setup(tmp_path)` for the
    RRD + settings + config scaffolding, then write the `web.system_image` row into the settings
    DB with `Keyval_storage.writeval` before taking the backup. Follow
    `test_local_round_trip`'s exact sequence: backup to a tar.gz, unlink both `rrd.db` and
    `pellmon_settings.db`, restore with `--local --yes`, then assert with
    `Keyval_storage(str(settings)).getval('web.system_image')`.

    Do NOT modify `tools/pellmon_backup.py` unless this test fails. RESEARCH predicts no code
    change is needed because the settings DB is already archived wholesale; if the test proves
    otherwise, fix the tool minimally and record the discrepancy against RESEARCH in the SUMMARY.

    Use `getval` (plan 08-01) rather than `readval` so the "never stored" assertion does not
    depend on the legacy `'error'` sentinel.
  </action>
  <verify>
    <automated>python -m pytest tests/test_backup_script.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "web.system_image" tests/test_backup_script.py` is at least 2.
    - `python -m pytest tests/test_backup_script.py -q` reports zero failures (the new test may
      skip only where `rrdtool` is genuinely unavailable, via the existing `needs_rrdtool`
      marker - it must NOT skip in the WSL/Linux run).
    - `git diff --stat tools/pellmon_backup.py` is empty, or the SUMMARY explains exactly why a
      change was required.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update the config comment and the deploy documentation</name>
  <files>src/conf.d/webinterface.conf.in, DEPLOY-PI.md</files>
  <read_first>
    - src/conf.d/webinterface.conf.in (whole file, 76 lines)
    - DEPLOY-PI.md sections "### Where PellMon keeps its data" (from line 139) and
      "### Backup and restore" (from line 224)
  </read_first>
  <action>
    In `src/conf.d/webinterface.conf.in`, amend the `system_image` comment block so it states
    that this value is the fallback and that the Settings page in the web GUI overrides it, and
    that the absolute-path option is config-file-only (the GUI offers only the shipped files,
    per D-02). Keep the existing Choices list and the banner comment box intact; do not change
    the `system_image = system.svg` default value, and do not touch any other key. This is a
    `.in` template consumed by Autotools substitution - introduce no new `@...@` tokens.

    In `DEPLOY-PI.md`:
    - Under "Where PellMon keeps its data", note that GUI-chosen settings (currently the system
      image) are stored in `pellmon_settings.db` in the persistent data folder, written by the
      daemon because the web container mounts that folder read-only, and therefore survive
      `docker compose down -v` and container recreation.
    - Under "Backup and restore", note that the backup archive's `pellmon_settings.db` carries
      those GUI settings, so a restore brings back the chosen system image along with the
      plugin settings - no separate step.
    Match the surrounding heading depth, prose voice and code-fence style of the existing
    document. Do not restructure sections or renumber headings.
  </action>
  <verify>
    <automated>python -m pytest tests/test_config_interpolation.py tests/test_ci_docker_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vn '^\s*$' src/conf.d/webinterface.conf.in | grep -ci "settings page"` is at least 1.
    - `grep -c "system_image = system.svg" src/conf.d/webinterface.conf.in` is 1 (default value
      unchanged).
    - `grep -ci "pellmon_settings.db" DEPLOY-PI.md` is at least 2, with at least one occurrence
      in each of the two named sections.
    - `python -m pytest tests/test_config_interpolation.py tests/test_ci_docker_config.py -q`
      passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full-suite phase gate</name>
  <files>tests/</files>
  <read_first>
    - .planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-01-SUMMARY.md,
      08-02-SUMMARY.md, 08-03-SUMMARY.md (the pass/skip counts each plan recorded)
    - tests/README.md (the documented expected-red baseline and how to run the suite in
      venv-wsl vs system python)
  </read_first>
  <action>
    Run the full suite in the WSL venv and, separately, the dbus/gi-dependent modules under the
    system python3 (per CLAUDE.md's testing note). Compare the result against the baseline
    recorded in `tests/README.md` and in the three plan SUMMARYs from this phase.

    Zero failures and zero errors are required. Skips are acceptable only for the documented
    platform guards (`rrdtool` absent, dbus/gi absent) - a test that skips because a new module
    failed to import is a failure, not a skip, and must be fixed.

    Also confirm the two repo-wide compliance gates still hold across every file this phase
    touched: `tests/test_no_ad_hoc_print.py` and `tests/test_no_sys_path_shims.py`.

    Record the final passed/skipped counts and the skip reasons in the SUMMARY.
  </action>
  <verify>
    <automated>python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests -q` reports zero failures and zero errors.
    - Every skipped test's reason is one of the documented platform guards; the SUMMARY lists
      them by name.
    - `python -m pytest tests/test_no_ad_hoc_print.py tests/test_no_sys_path_shims.py -q` passes.
    - The SUMMARY states the passed/skipped counts for both the venv-wsl run and the system
      python3 run.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live verification on a running stack</name>
  <files>(no files changed - operator verification only)</files>
  <action>
    Pause and hand control to the developer. Do not edit code, do not mark the phase complete,
    and do not proceed to the SUMMARY until the resume signal is received. If the developer
    reports a deviation, capture it verbatim in the SUMMARY and treat it as a gap for
    `/gsd:plan-phase --gaps` rather than patching it silently here.
  </action>
  <what-built>
    The full feature is wired and unit-verified: a `Settings` entry in the web navbar leading to
    `/settings/`, a gallery of the six shipped burner diagrams with the current one marked, a
    Save button that persists the choice into `pellmon_settings.db` in the host data folder via
    two new D-Bus methods on the daemon, and a `/systemimage` handler that resolves the chosen
    image per request with no-cache headers. Everything except the live-stack behavior is
    covered by automated tests; D-03, D-04 and the container-recreation half of D-05 need real
    Docker, real D-Bus and a real browser, which the test suite cannot provide.
  </what-built>
  <how-to-verify>
    On the Linux/Pi host with the stack built from this branch:

    1. `docker compose up -d` and wait for both services to report healthy
       (`docker compose ps`).
    2. Open `http://<host>:8081/` in a browser and log in. Confirm a "Settings" entry appears
       in the navbar, to the right of "View log".
    3. Click Settings. Expected: a page titled "Settings" with one "System image" panel showing
       six tiles, each rendering the actual SVG diagram (not a broken-image icon), and the tile
       matching the current `system_image` from `webinterface.conf` marked "Current".
    4. Select a different diagram (for example "NBE, 2-way valve") and click "Save image".
       Expected: a green alert reading "System image saved. Reload the main page to see it."
    5. Go to the main page and reload. Expected: the new diagram is shown, WITHOUT restarting
       the daemon or the containers. If the old one still appears, hard-reload once and report
       it - that indicates the no-cache header is not reaching the browser.
    6. Confirm persistence across recreation: `docker compose down -v && docker compose up -d`,
       then reload the main page. Expected: the chosen diagram is still shown, and the Settings
       page still marks it "Current".
    7. Confirm the daemon-down path: `docker compose stop pellmonsrv`, reload the main page.
       Expected: the page still renders with an image (the `webinterface.conf` one), no error
       page. Open Settings and try to save. Expected: the message "Cannot save right now because
       the PellMon server is not running. The main page keeps its current image." Restart with
       `docker compose start pellmonsrv`.
    8. Confirm the auth gate: log out, then browse directly to `http://<host>:8081/settings/`.
       Expected: redirected to the login page, not the settings page.
    9. Optional but useful: with JavaScript disabled in the browser, repeat steps 4-5. The form
       must still work.
  </how-to-verify>
  <resume-signal>Type "approved", or describe which step behaved differently and what you saw</resume-signal>
  <verify>
    <automated>MISSING - steps 1-9 require Docker, a live D-Bus session bus and a browser; no automated equivalent exists in this repo. Automated coverage of the same logic lives in tests/Pellmonweb/test_settings_image.py, tests/Pellmonweb/test_settings_page.py and tests/test_backup_script.py (Task 1), all gated green by Task 3.</automated>
    <human-check>Developer executes steps 1-9 of how-to-verify on the Linux/Pi Docker stack and returns the resume signal.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer has returned an explicit "approved" for steps 1-8 (step 9 optional).
    - Any deviation is recorded verbatim in the plan SUMMARY, with the step number it came from.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup archive -> restored data folder | Archive members become files in the persistent folder; already gated by the Phase 7 traversal/symlink checks. |
| operator documentation -> deployment behavior | Wrong docs lead to a misconfigured or unbacked-up install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Tampering | restore path for `pellmon_settings.db` | transfer | Already mitigated by the Phase 7 controls this plan must not weaken: `test_restore_rejects_traversal` and `test_restore_rejects_symlink` in `tests/test_backup_script.py`. This plan adds a test and no tool code, so those gates stand; the full-suite gate in Task 3 re-runs them. |
| T-08-19 | Information disclosure | settings DB file mode after restore | mitigate | Covered by existing Phase 7 behavior (`Keyval_storage` pre-creates 0600); the new round-trip test exercises the same restore path, so a regression in file mode would surface in the existing mode assertions. |
| T-08-20 | Repudiation | unverified live behavior | mitigate | The blocking human-verify checkpoint records an explicit operator confirmation of D-03/D-04 and the container-recreation half of D-05, which no automated test in this repo can reach. |
| T-08-SC | Tampering | package installs | accept | No package-manager installs in this phase; `requirements.txt` unchanged. |
</threat_model>

<verification>
- `python -m pytest tests -q` green in venv-wsl, and the dbus/gi modules green under system
  python3.
- `grep -c "web.system_image" tests/test_backup_script.py` >= 2.
- `git diff --stat tools/pellmon_backup.py` empty (or a documented exception).
- Human checkpoint approved.

<human-check>
Steps 1-9 of Task 4 executed on a live Docker stack with a browser. This is the only verification
of D-03 (real SVG thumbnails render), D-04 (new image on next load, no restart) and the
container-recreation half of D-05.
</human-check>
</verification>

<success_criteria>
The chosen system image provably survives a backup/restore cycle and container recreation, the
config file and deploy documentation describe the new GUI-first workflow, the full test suite is
green with only documented platform skips, and a human has confirmed the end-to-end experience
on a live stack.
</success_criteria>

<output>
Create `.planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-04-SUMMARY.md` when done
</output>
