---
phase: 05-security-ci-deployment-hardening
plan: 02
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - docker-compose.yml
  - Dockerfile
  - tests/test_ci_docker_config.py
autonomous: true
requirements: [OPS-01, OPS-03]
must_haves:
  truths:
    - "GitHub Actions workflow runs pytest and test-imports.py on push and pull_request (OPS-01)"
    - "docker-compose.yml defines real readiness healthchecks and service_healthy dependency (OPS-03)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow definition"
      min_lines: 40
    - path: "tests/test_ci_docker_config.py"
      provides: "Automated configuration validation tests for CI and Docker Compose"
      min_lines: 35
---

<objective>
Configure automated CI testing and production Docker deployment healthchecks:
1. Create `.github/workflows/ci.yml` running pytest and import tests on Ubuntu runner (OPS-01).
2. Harden Docker Compose and Dockerfile healthchecks to reflect actual daemon/web readiness (OPS-03).
3. Validate configuration with automated tests.

Output: Working CI workflow and robust healthchecked Docker Compose setup.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-ci-deployment-hardening/05-CONTEXT.md
@.planning/phases/05-security-ci-deployment-hardening/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - test-imports.py
    - pytest.ini
    - requirements.txt
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`:
    - Workflow name: `CI`
    - Triggers:
      `push:` branches `[ master, python3-migration ]`
      `pull_request:` branches `[ master, python3-migration ]`
    - Job: `test` on `ubuntu-latest`
    - Steps:
      1. `actions/checkout@v4`
      2. `actions/setup-python@v5` with `python-version: "3.11"`
      3. Install Linux system packages:
         ```bash
         sudo apt-get update
         sudo apt-get install -y --no-install-recommends \
           python3-dbus python3-gi python3-gi-cairo gir1.2-glib-2.0 \
           librrd-dev rrdtool python3-rrdtool
         ```
      4. Install Python dependencies:
         ```bash
         python -m pip install --upgrade pip
         pip install -r requirements.txt
         pip install pytest pytest-mock pytest-socket
         ```
      5. Run test-imports.py:
         `python test-imports.py`
      6. Run full test suite:
         `pytest tests/ -v`
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "import os; assert os.path.isfile('.github/workflows/ci.yml'); content = open('.github/workflows/ci.yml').read(); assert 'test-imports.py' in content; assert 'pytest tests/' in content; assert 'python3-dbus' in content"</automated>
  </verify>
  <done>GitHub Actions workflow is established and configured with all required dependencies and gates.</done>
</task>

<task type="auto">
  <name>Task 2: Update Docker Compose and Dockerfile healthchecks</name>
  <files>docker-compose.yml, Dockerfile</files>
  <read_first>
    - docker-compose.yml (lines 55-80, 110-120)
    - Dockerfile (lines 65-75)
  </read_first>
  <action>
    1. In `docker-compose.yml`:
       - For `pellmonsrv`:
         Update `healthcheck` to test D-Bus service responsiveness on session socket:
         ```yaml
         healthcheck:
           test: [ "CMD-SHELL", "dbus-send --session --address=unix:path=/var/run/pellmon/bus_socket --dest=org.pellmon.int --print-reply /org/pellmon/int org.freedesktop.DBus.Peer.Ping > /dev/null 2>&1 || exit 1" ]
           interval: 15s
           timeout: 5s
           retries: 5
           start_period: 10s
         ```
       - For `pellmonweb`:
         Update `depends_on`:
         ```yaml
         depends_on:
           pellmonsrv:
             condition: service_healthy
         ```
         Update `healthcheck`:
         ```yaml
         healthcheck:
           test: [ "CMD", "curl", "-f", "http://localhost:8081/" ]
           interval: 15s
           timeout: 5s
           retries: 3
           start_period: 10s
         ```
    2. In `Dockerfile`:
       Ensure `curl` and `procps` and `dbus` are installed (already present).
       Ensure `HEALTHCHECK` command is set to test port 8081.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "compose = open('docker-compose.yml').read(); assert 'service_healthy' in compose; assert 'dbus-send' in compose or 'org.pellmon.int' in compose; assert 'curl' in compose"</automated>
  </verify>
  <done>Docker healthchecks probe actual service readiness and enforce proper startup ordering.</done>
</task>

<task type="auto">
  <name>Task 3: Add configuration verification tests</name>
  <files>tests/test_ci_docker_config.py</files>
  <read_first>
    - .github/workflows/ci.yml
    - docker-compose.yml
  </read_first>
  <action>
    Create `tests/test_ci_docker_config.py`:
    - `test_github_actions_workflow`:
      Parses `.github/workflows/ci.yml` (checks syntax/structure).
      Asserts triggers include push and pull_request for target branches.
      Asserts system packages include D-Bus and GLib.
      Asserts test commands include `pytest tests/` and `test-imports.py`.
    - `test_docker_healthchecks`:
      Parses `docker-compose.yml` (checks syntax/structure).
      Asserts `pellmonsrv` defines a readiness healthcheck checking `org.pellmon.int`.
      Asserts `pellmonweb` has `depends_on` with `condition: service_healthy`.
      Asserts `pellmonweb` defines a healthcheck querying HTTP 8081.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/test_ci_docker_config.py -v</automated>
  </verify>
  <done>Automated tests verify CI and Docker Compose configuration integrity.</done>
</task>

</tasks>
