---
phase: 05-security-ci-deployment-hardening
plan: 04
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - requirements.txt
  - README.md
autonomous: true
requirements: [OPS-04, OPS-05, OPS-06]
must_haves:
  truths:
    - "requirements.txt uses pinned versions (==) for all active dependencies (OPS-04)"
    - "All 23 .py2bak backup files are deleted from the repository (OPS-05)"
    - "README.md explicitly documents that production deployment is Linux-only (OPS-06)"
    - "Full test suite passes with zero failures and zero unexpected regressions across the entire project"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned dependency specifications"
    - path: "README.md"
      provides: "Updated documentation specifying Linux-only production deployment and Docker Compose setup"
---

<objective>
Complete project hardening, repository hygiene, and documentation:
1. Pin all dependency versions in `requirements.txt` with exact `==` pins (OPS-04).
2. Purge all 23 obsolete `.py2bak` files from `src/` (OPS-05).
3. Modernize `README.md` with Linux-only production clarification, Docker Compose guidance, and password hashing instructions (OPS-06).
4. Run full verification suite across the entire project as the final quality gate.

Output: Clean repository, reproducible build manifest, accurate documentation, and 100% green test gate.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-ci-deployment-hardening/05-CONTEXT.md
@.planning/phases/05-security-ci-deployment-hardening/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin dependency versions in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt
  </read_first>
  <action>
    Update `requirements.txt` to replace `>=` version floors with exact `==` pins matching our verified runtime environment:
    ```text
    # Core dependencies - Python 3 versions
    # Note: Some dependencies are platform-specific and may need manual installation

    # Cross-platform dependencies
    pyserial==3.5
    CherryPy==18.10.0
    Mako==1.3.10
    python-dateutil==2.9.0.post0
    argcomplete==3.6.3
    simplejson==4.1.2

    # Platform-specific dependencies (Linux/Unix)
    # rrdtool==1.7.0  # Requires system package: apt-get install python3-rrdtool
    # dbus-python==1.2.18  # Requires system package: apt-get install python3-dbus
    # PyGObject==3.42.0  # Requires system package: apt-get install python3-gi

    # Optional dependencies
    ws4py==0.6.0

    # Plugin dependencies
    # OWFS plugin
    pyownet==0.10.0.post1

    # NBEcom plugin (replaces python-crypto with pycryptodome)
    pycryptodome==3.23.0
    xtea==0.7.1

    # Openweathermap plugin
    pyowm==3.5.0
    ```
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "lines = [l.strip() for l in open('requirements.txt') if l.strip() and not l.startswith('#')]; assert all('==' in l for l in lines), 'Unpinned dependency found in requirements.txt'"</automated>
  </verify>
  <done>All requirements are pinned with exact versions for reproducible builds.</done>
</task>

<task type="auto">
  <name>Task 2: Delete all .py2bak files from src/</name>
  <files>src/</files>
  <action>
    Remove all 23 `.py2bak` files from git tracking and the filesystem using `git rm`:
    `git rm -f src/Pellmonsrv/*.py2bak src/Pellmonsrv/**/*.py2bak src/Pellmonweb/*.py2bak src/Scotteprotocol/*.py2bak`
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "import glob; matches = glob.glob('src/**/*.py2bak', recursive=True); assert len(matches) == 0, f'Found remaining .py2bak files: {matches}'"</automated>
  </verify>
  <done>All 23 legacy .py2bak backup files have been removed from the repository.</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md with Linux-only notice and modern deployment guide</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 1-40, 115-172)
  </read_first>
  <action>
    In `README.md`:
    1. Add an alert/section under the title noting:
       > **Note:** Production deployment of PellMon is **Linux-only**. Core hardware and communication subsystems require Linux-specific facilities (D-Bus system/session bus, PyGObject/GLib main loops, RRDtool C-bindings, Linux serial devices `/dev/ttyUSB*`, and GPIO). Windows is supported for development, mock-based testing, and syntax verification only.
    2. Document Docker Compose as the primary and recommended deployment method:
       - Explain `docker compose up -d` with `docker-compose.yml`.
       - Mention shared D-Bus socket and healthcheck readiness gating.
    3. Document Web Authentication & Password Hashing:
       - Show how to generate PBKDF2 password hashes for `config/pellmon.conf` under `[authentication]`:
         `python3 -c "from Pellmonweb.auth import hash_password; print(hash_password('yourpassword'))"`
       - Note backward compatibility with existing plaintext passwords.
    4. Modernize dependencies section from Python 2 names (`python-serial`, `python-cherrypy3`, etc.) to Python 3 names (`python3-serial`, `python3-cherrypy3` / `CherryPy`, `python3-dbus`, `python3-gi`, `python3-rrdtool`, `pycryptodome`, `xtea`).
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "readme = open('README.md').read(); assert 'Linux-only' in readme; assert 'docker compose' in readme.lower(); assert 'hash_password' in readme"</automated>
  </verify>
  <done>README clearly explains Linux production requirement, Docker Compose deployment, and credential hashing.</done>
</task>

<task type="auto">
  <name>Task 4: Run full verification gate</name>
  <files>tests/</files>
  <action>
    Execute full automated test suite and import verification:
    1. Run `test-imports.py`.
    2. Run full pytest suite `pytest tests/ -v`.
    Assert 0 failures across the entire suite.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <done>Full project test suite passes with zero failures.</done>
</task>

</tasks>
