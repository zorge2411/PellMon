---
phase: 02-exception-visibility-retrofit
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/Pellmonsrv/plugins/test_calculate_logging.py
  - src/Pellmonsrv/plugins/calculate/__init__.py
autonomous: true
requirements: [OBS-02]
must_haves:
  truths:
    - "Writing to a calculated item still returns 'error' to the caller, but now logs a full traceback showing the real underlying exception"
    - "Reading a calculated item whose program fails logs a full traceback instead of a repr() one-liner"
    - "Reading or writing a plain non-calculated item produces zero log records"
    - "The calcthread background loop logs a traceback when a calc program raises"
  artifacts:
    - path: "tests/Pellmonsrv/plugins/test_calculate_logging.py"
      provides: "caplog assertions for calculate getItem/setItem/activate failure visibility, via a test-only maketrans shim"
      min_lines: 60
    - path: "src/Pellmonsrv/plugins/calculate/__init__.py"
      provides: "Loud calc-program failures (OBS-02)"
      contains: "logger.exception"
  key_links:
    - from: "tests/Pellmonsrv/plugins/test_calculate_logging.py"
      to: "Pellmonsrv.plugins.calculate"
      via: "test-only string.maketrans shim injected before import"
      pattern: "string\\.maketrans = str\\.maketrans"
---

<objective>
Make the Calculate plugin's swallowed failures visible (OBS-02). This file contains the phase's most illustrative case: `setItem()` calls the Python-2-only builtin `unicode()` at line 351, which raises `NameError` on every single write, and that `NameError` is currently degraded to a bare `'error'` string with a one-line `logger.info`.

Purpose: ROADMAP Phase 2 success criterion 3 — a deliberately triggered failure in `plugins/calculate/__init__.py` logs a full traceback instead of being swallowed.
Output: a caplog regression test (using a test-only `string.maketrans` shim) plus Category-A-only conversions in the plugin.

SCOPE BOUNDARY — do NOT fix the underlying bugs. `from string import maketrans` (line 28) is PROTO-05/Phase 4. `unicode(value)` (line 351) is PROTO-02/Phase 4. Both must still be present and still broken after this plan; only the log call around them changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exception-visibility-retrofit/02-CONTEXT.md
@.planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md
@.planning/phases/02-exception-visibility-retrofit/02-PATTERNS.md

<interfaces>
<!-- Verified 2026-09-17 by direct read of src/Pellmonsrv/plugins/calculate/__init__.py -->

Module already has its logger (line 30):  logger = getLogger('pellMon')
Module-level import blockers for tests:
  line 24: import os, grp, pwd            -> grp/pwd absent on Windows venv-py3
  line 28: from string import maketrans   -> ImportError on ANY Python 3 (PROTO-05, Phase 4)

Category A conversion sites (line numbers as of 2026-09-17, re-grep before editing):
  295-297  activate() inner:  except Exception as e: logger.info(str(e)); raise e
  316-318  activate() outer:  except Exception as e: logger.info(str(e)); raise
  330-332  getItem():         except Exception as e: logger.info(calc_item+' error: '+repr(e)); return 'error'
  355-358  setItem():         except Exception as e: calc = Calc(prog, self.db); logger.info(calc_item+' error: '+str(e)); return 'error'
  381-382  calcthread.run():  except Exception as e: logger.info('error in ' + self.progitem + str(e))

Category B — control flow, leave alone (no logging added):
  54, 151, 169, 181, 191   Calc interpreter end-of-program / undefined-variable probes
  218, 220                 Calc.run stack/return re-raise-with-context path
  292-293                  taskcycle `raise e` passthrough (already re-raises; outer 295 logs it)
  333, 340, 359, 365       getItem/setItem missing-`calc_item` fallbacks — the dominant
                           path for every plain non-calculated item
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write caplog regression tests using the test-only maketrans shim</name>
  <files>tests/Pellmonsrv/plugins/test_calculate_logging.py</files>
  <read_first>
    - src/Pellmonsrv/plugins/calculate/__init__.py (lines 20-35 for imports, 285-385 for activate/getItem/setItem/calcthread)
    - .planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md (Pitfall 3 — the exact test-only maketrans shim; Pitfall 5 — LogRecord-attribute assertions only)
    - tests/Pellmonsrv/conftest.py (from plan 02-01 — reuse its sys.modules stub approach for grp/pwd; do NOT reuse `daemon_module` itself)
    - tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py (existing conventions in this test package)
  </read_first>
  <action>
    Create `tests/Pellmonsrv/plugins/test_calculate_logging.py`.

    IMPORT PRECONDITIONS — this module is unimportable today for two independent reasons; both must be worked around in the TEST only, never in src/:
    1. `import os, grp, pwd` (line 24) fails on Windows venv-py3. Inject `grp` and `pwd` stubs into `sys.modules` before import, using the same technique as `tests/Pellmonsrv/conftest.py`.
    2. `from string import maketrans` (line 28) raises `ImportError` on every Python 3. Apply the shim from 02-RESEARCH.md Pitfall 3 before import: set `string.maketrans = str.maketrans` when `string` lacks the attribute, then `importlib.import_module("Pellmonsrv.plugins.calculate")`.
    Put both in a module-scoped fixture, and undo both on teardown so `tests/test_plugin_imports.py` keeps producing its genuine outcomes.

    The module docstring MUST state that the `maketrans` shim exists solely to make the module importable for exception-visibility testing, is NOT a production fix, and must be deleted once Phase 4 / PROTO-05 fixes the import for real.

    Test 1 — `setItem()` NameError visibility (the OBS-02 smoking gun): construct a `Calculate` plugin instance far enough to drive `setItem` on a calculated item (populate the module-level `itemList` and the instance's `name2index` directly rather than running the full `activate()`; those are module globals by design). Call `setItem(name, '1')`. Assert BOTH:
      - the return value is still exactly `'error'` (proves logic unchanged), and
      - a `caplog.records` entry exists with `levelno == logging.ERROR`, `record.exc_info is not None`, and `record.exc_info[0] is NameError` (proves the real `unicode` NameError now reaches the log).

    Test 2 — `getItem()` failure visibility: drive `getItem` on a calculated item whose calc program raises. Assert the return value is still `'error'` AND a record exists with `levelno == logging.ERROR`, `record.exc_info is not None`, and the `calc_item` name in `record.getMessage()`.

    Test 3 — Category B silence (the regression guard that matters most): drive `getItem` and `setItem` against a plain non-calculated item (one with no `calc_item` key, hitting the line 333/359 fallbacks). Assert `caplog.records` is empty — zero records at any level. This is what prevents a future contributor from converting those fallbacks into logged catches and flooding the log on every item access.

    Test 4 — `activate()` re-raise preserved: trigger the outer `activate()` except path and assert with `pytest.raises` that the exception still propagates, AND that an ERROR record with `exc_info is not None` was emitted first.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_calculate_logging.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists with four test functions matching the descriptions above
    - `grep -n "string.maketrans = str.maketrans" tests/Pellmonsrv/plugins/test_calculate_logging.py` returns a match
    - The module docstring contains the phrase "not a production fix" and names PROTO-05
    - Before Task 2 lands, the file fails with assertion errors (not import/collection errors); record the RED result in the SUMMARY
    - `grep -c "caplog.text" tests/Pellmonsrv/plugins/test_calculate_logging.py` returns 0
    - Test 3 asserts `len(caplog.records) == 0` (or equivalent explicit emptiness assertion)
    - After running this file, `venv-py3/Scripts/python.exe -m pytest tests/test_plugin_imports.py -v` still reports the same outcomes recorded in before.xml (the shims do not leak)
  </acceptance_criteria>
  <done>Four caplog tests exist, import the module via the documented test-only shim, and fail with assertion errors against the current unmodified plugin.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the five Category A log calls in calculate/__init__.py</name>
  <files>src/Pellmonsrv/plugins/calculate/__init__.py</files>
  <read_first>
    - src/Pellmonsrv/plugins/calculate/__init__.py (lines 285-385 — re-grep each site; line numbers drift)
    - .planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md (section 3, Category A and Category B tables for this file)
    - .planning/phases/02-exception-visibility-retrofit/02-PATTERNS.md (verified before/after pairs for this file)
    - tests/Pellmonsrv/plugins/test_calculate_logging.py (from Task 1)
  </read_first>
  <action>
    Make exactly five edits. The module already has `logger = getLogger('pellMon')` at line 30 — do not add or change it.

    EDIT 1 — `activate()` inner handler (approx. 295-297): change `except Exception as e:` to `except Exception:` and `logger.info(str(e))` to `logger.exception('calculate plugin activation error')`. Keep `raise e` as `raise` — WAIT: `raise e` requires the bound name. To preserve the exception identity exactly, keep the clause as `except Exception as e:` at this site and change only the log call to `logger.exception('calculate plugin activation error')`, leaving `raise e` untouched. Do not convert `raise e` to a bare `raise`.

    EDIT 2 — `activate()` outer handler (approx. 316-318): this one ends in a bare `raise`, so the bound name is not needed. Change `except Exception as e:` to `except Exception:` and `logger.info( str(e))` to `logger.exception('calculate plugin activation error')`. Leave the bare `raise` untouched.

    EDIT 3 — `getItem()` calc-run handler (approx. 330-332): change `except Exception as e:` to `except Exception:` and `logger.info(calc_item+' error: '+repr(e))` to `logger.exception('%s error'%calc_item)`. Leave `return 'error'` untouched.

    EDIT 4 — `setItem()` calc-run handler (approx. 355-358): change `except Exception as e:` to `except Exception:` and `logger.info(calc_item+' error: '+str(e))` to `logger.exception('%s error'%calc_item)`. CRITICAL: keep the `calc = Calc(prog, self.db)` line that precedes the log call exactly as-is. That assignment is unused downstream but `Calc()` can itself raise, and that raise currently propagates to the outer bare `except:` at line 359 — removing the line would change control flow. Leave `return 'error'` untouched. Do NOT touch `stack = [unicode(value)]` on line 351 — that is PROTO-02, Phase 4.

    EDIT 5 — `calcthread.run()` handler (approx. 381-382): change `except Exception as e:` to `except Exception:` and `logger.info('error in ' + self.progitem + str(e))` to `logger.exception('error in %s'%self.progitem)`. Leave the `sleep(self.cycle)` loop untouched.

    DO NOT TOUCH, under any circumstance:
    - Line 28 `from string import maketrans` (PROTO-05, Phase 4) and line 351 `unicode(value)` (PROTO-02, Phase 4). Both must still be present and broken after this plan.
    - The bare `except:` clauses at lines 54, 151, 169, 181, 191 (Calc interpreter control flow), 333, 340, 359 (missing-`calc_item` fallbacks), and the `except Exception as e:` at 365. These fire on every normal item read/write; adding logging would flood the log and would itself be an observable behavior change. Leave them bare — do not narrow them, do not log them. (Narrowing is optional per 02-RESEARCH.md but carries nonzero behavior risk in an interpreter loop; this plan chooses "leave bare" and Task 1's Test 3 pins the silence contract regardless.)
    - Lines 218/220's re-raise-with-context path and 292-293's `raise e` passthrough.

    Total expected diff: 10 changed lines (5 except clauses + 5 log calls). A larger diff means over-conversion — revert and re-scope.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_calculate_logging.py tests/ -q -m "not known_broken"</automated>
  </verify>
  <acceptance_criteria>
    - `venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_calculate_logging.py -v` exits 0 with all four tests passing
    - `grep -c "logger.exception" src/Pellmonsrv/plugins/calculate/__init__.py` returns exactly 5
    - `grep -c "logger.info" src/Pellmonsrv/plugins/calculate/__init__.py` returns 0
    - `grep -n "from string import maketrans" src/Pellmonsrv/plugins/calculate/__init__.py` still returns a match (Phase 4 bug intentionally preserved)
    - `grep -n "unicode(value)" src/Pellmonsrv/plugins/calculate/__init__.py` still returns a match (Phase 4 bug intentionally preserved)
    - `grep -n "calc = Calc(prog, self.db)$" src/Pellmonsrv/plugins/calculate/__init__.py` still returns the `setItem` except-block occurrence
    - `grep -vn "^\s*#" src/Pellmonsrv/plugins/calculate/__init__.py | grep -c "except:" ` returns the same count as before the edit (no bare except was converted)
    - `git diff --numstat src/Pellmonsrv/plugins/calculate/__init__.py` reports 10 added and 10 removed lines (±2)
    - `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` exits 0
  </acceptance_criteria>
  <done>Five `logger.exception` call sites replace the five `logger.info` calls in Category A paths; the bare excepts, the `maketrans` import, and the `unicode()` call are all unchanged.</done>
</task>

</tasks>

<threat_model>
N/A — observability-only change, no logic altered, verified via before/after outcome parity (plan 06). ASVS V7 audit: the five converted messages interpolate only calc-item names and program names; no credentials or keys pass through this plugin.
</threat_model>

<verification>
1. `venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_calculate_logging.py -v` — all four pass.
2. `grep -c "logger.exception" src/Pellmonsrv/plugins/calculate/__init__.py` — exactly 5.
3. `grep -n "unicode(value)\|from string import maketrans" src/Pellmonsrv/plugins/calculate/__init__.py` — both still present.
4. `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` — exits 0.
</verification>

<success_criteria>
- `setItem()` still returns `'error'` but now logs an ERROR record whose `exc_info[0]` is `NameError`.
- `getItem()`/`setItem()` on a plain non-calculated item emit zero log records.
- The Phase 4 bugs (`maketrans`, `unicode`) are untouched.
</success_criteria>

<output>
Create `.planning/phases/02-exception-visibility-retrofit/02-04-SUMMARY.md` when done, recording the exact `git diff --numstat` counts.
</output>
