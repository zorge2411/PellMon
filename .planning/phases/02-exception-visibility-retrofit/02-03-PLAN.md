---
phase: 02-exception-visibility-retrofit
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/Pellmonsrv/test_pellmonsrv_logging.py
  - src/Pellmonsrv/pellmonsrv.py
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "A plugin whose activate() raises produces a full traceback in the daemon log, and the daemon still starts with the remaining plugins"
    - "A failed RRD item read during polling logs a traceback naming the item, and the poller still writes 'U' for that item"
    - "A malformed plugin_dirs config setting logs a traceback instead of printing to stdout"
    - "No print() call remains in pellmonsrv.py"
  artifacts:
    - path: "tests/Pellmonsrv/test_pellmonsrv_logging.py"
      provides: "caplog assertions for the plugin-activation loop and Poller failure paths"
      min_lines: 60
    - path: "src/Pellmonsrv/pellmonsrv.py"
      provides: "Loud daemon-side plugin-activation and polling failures (OBS-01, OBS-02)"
      contains: "logger.exception"
  key_links:
    - from: "src/Pellmonsrv/pellmonsrv.py"
      to: "logger"
      via: "module-level logger = logging.getLogger('pellMon') available before MyDaemon.run() assigns the global"
      pattern: "^logger = logging\\.getLogger\\('pellMon'\\)"
---

<objective>
Make the daemon's own plugin-activation and RRD-polling failure paths loud (OBS-01 daemon half, OBS-02), and remove the two stray `print()` calls in `pellmonsrv.py` (OBS-03 partial).

Purpose: ROADMAP Phase 2 success criteria 2 and 3 — plugin activation failures and protocol/database-write failures in `pellmonsrv.py` must log a full traceback instead of a one-line `.info` message or a stdout print.
Output: a caplog regression test plus the Category-A-only conversions in `pellmonsrv.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exception-visibility-retrofit/02-CONTEXT.md
@.planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md
@.planning/phases/02-exception-visibility-retrofit/02-PATTERNS.md

<interfaces>
<!-- Verified 2026-09-17 by direct read of src/Pellmonsrv/pellmonsrv.py -->

CRITICAL: pellmonsrv.py has NO module-level `logger`. `logger` is a module GLOBAL
assigned inside two functions:
  line 438-439:  MyDaemon.run():        global logger; logger = logging.getLogger('pellMon')
  line 518-519:  config.__init__():     global logger; logger = logging.getLogger('pellMon')
Every `logger.*` call elsewhere in the module (including Poller.run and the
plugin-activation loop) therefore only resolves after one of those two functions
has run. Unit tests that exercise Poller/activation directly hit NameError.

Fixture available from plan 02-01 (tests/Pellmonsrv/conftest.py):
  daemon_module  -> imported Pellmonsrv.pellmonsrv module object, with
                    dbus/dbus.service/dbus.mainloop.glib/gi.repository/pwd/grp
                    stubbed in sys.modules for the duration of the test

Conversion sites (Category A, per 02-RESEARCH.md; re-grep before editing):
  ~93-103   plugin activation loop   except Exception as e -> logger.info('%s plugin error: %s')
  ~258-268  Poller data read         except IOError as e / except Exception as e -> logger.info / logger.debug
  ~288-289  Poller whole-iteration   except Exception as e -> logger.info('error in polling %s')
  ~346      db_copy_thread           bare `except: pass`
  744-752   config plugin_dirs parse except configparser.NoSectionError as e: print('noconf', e)
                                     except Exception as e: print(e); logger.info('invalid setting for plugin_dirs')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write caplog regression tests for the activation loop and Poller failure paths</name>
  <files>tests/Pellmonsrv/test_pellmonsrv_logging.py</files>
  <read_first>
    - src/Pellmonsrv/pellmonsrv.py (lines 85-134 for the Database plugin-activation loop; lines 245-300 for Poller.run)
    - tests/Pellmonsrv/conftest.py (the `daemon_module` fixture from plan 02-01 — its stub set and teardown contract)
    - tests/conftest.py (repo-root fixture style)
    - .planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md (Pitfall 5 — assert on LogRecord attributes only)
  </read_first>
  <action>
    Create `tests/Pellmonsrv/test_pellmonsrv_logging.py`. Every test in this file requests the `daemon_module` fixture from `tests/Pellmonsrv/conftest.py` — `Pellmonsrv.pellmonsrv` cannot be imported directly on `venv-py3` (no `dbus`).

    Test 1 — plugin activation failure (OBS-01, daemon half): drive the `Database.__init__` plugin-activation loop with a stand-in plugin object whose `plugin_object.activate(...)` raises a distinctive exception (e.g. `RuntimeError("deliberate activate failure")`). Prefer exercising the real loop by monkeypatching the `PluginManager` the loop uses so it yields the stand-in plugin; if the loop's surrounding `Database.__init__` is too entangled with `conf`/D-Bus to construct, extract the assertion by invoking the loop body through the module's own attributes rather than reimplementing it — do not copy the loop into the test. Set `conf.command` to something other than `'debug'` (monkeypatch `daemon_module.conf`) so the `else` branch, not the `raise` branch, is taken. Assert: a record exists with `levelno == logging.ERROR`, `record.exc_info is not None`, `record.exc_info[0] is RuntimeError`, and the plugin's name in `record.getMessage()`; and assert the plugin name landed in the failed-plugins list so control flow is provably unchanged.

    Test 2 — debug-mode re-raise preserved: with `daemon_module.conf.command` set to `'debug'`, the same activation failure must re-raise (use `pytest.raises(RuntimeError)`). This test guards the explicitly-untouched `if conf.command == 'debug': raise` branch against being collapsed during the conversion.

    Test 3 — Poller item-read failure (OBS-02): construct a `Poller` (or call `Poller.run`'s failure path) with a database item whose value access raises, and assert a record with `levelno == logging.ERROR`, `record.exc_info is not None`, and the item's name present in `record.getMessage()`. Also assert the poller's RRD value string for that item is still `'U'` — proving the fallback behavior is unchanged and only the log changed. Use `mocker.patch` on `subprocess` calls so no `rrdtool` binary is required, and rely on the suite-wide `--disable-socket` guardrail remaining active.

    Test 4 — module-level logger availability: assert `hasattr(daemon_module, "logger")` immediately after import, before any `MyDaemon.run()` or `config.__init__` call. This pins the Task 2 requirement that makes the other three tests possible.

    Module docstring must name OBS-01 and OBS-02, cite ROADMAP Phase 2 success criteria 2 and 3, and state that these tests are expected to FAIL until Task 2 lands.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_pellmonsrv_logging.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists with four test functions matching the descriptions above
    - Before Task 2 lands, the file fails with assertion errors or `NameError: name 'logger' is not defined` — not with a collection/import error; record the RED result in the SUMMARY
    - Every assertion reads `record.levelno`, `record.exc_info`, or `record.getMessage()`; `grep -c "caplog.text" tests/Pellmonsrv/test_pellmonsrv_logging.py` returns 0
    - The debug-mode re-raise test uses `pytest.raises` and is present
    - `grep -n "def test_" tests/Pellmonsrv/test_pellmonsrv_logging.py` returns exactly 4 matches
  </acceptance_criteria>
  <done>Four caplog/behavior tests exist and fail against the current unmodified pellmonsrv.py.</done>
</task>

<task type="auto">
  <name>Task 2: Add a module-level logger and convert the Category A failure paths in pellmonsrv.py</name>
  <files>src/Pellmonsrv/pellmonsrv.py</files>
  <read_first>
    - src/Pellmonsrv/pellmonsrv.py (lines 17-40 imports; 85-134 activation loop; 245-300 Poller.run; 320-350 copy_db/db_copy_thread; 740-756 plugin_dirs config parse — re-grep all sites, line numbers drift)
    - .planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md (the Category A table and the Category B exclusion list for this file)
    - .planning/phases/02-exception-visibility-retrofit/02-PATTERNS.md (section "src/Pellmonsrv/pellmonsrv.py" — verified before/after pairs)
    - tests/Pellmonsrv/test_pellmonsrv_logging.py (from Task 1)
  </read_first>
  <action>
    EDIT 0 — module-level logger. Immediately after the `import logging` / `import logging.handlers` lines (approx. 24-25), add `logger = logging.getLogger('pellMon')` at module scope. Leave both existing `global logger; logger = logging.getLogger('pellMon')` assignments in `MyDaemon.run()` and `config.__init__()` exactly as they are — `logging.getLogger('pellMon')` returns the same singleton object, so this addition is behavior-identical at runtime while making `Poller` and the activation loop importable/testable in isolation. Note this rationale in the SUMMARY so a reviewer does not read it as a logic change.

    EDIT 1 — plugin activation loop (approx. 93-103). Change `except Exception as e:` to `except Exception:`. Leave `failed_plugins.append(plugin.name)` and the `if conf.command == 'debug': raise` branch byte-identical. In the `else` branch only, replace `logger.info('%s plugin error: %s'%(plugin_name, str(e)))` with `logger.exception('%s plugin error'%plugin_name)`.

    EDIT 2 — Poller data-read paths (approx. 258-268). Change `except IOError as e:` to `except IOError:` and, in its nested handler, `except Exception as e: logger.info('error in retry %s'%str(e))` to `except Exception: logger.exception('error in retry for %s'%data['name'])`. Change the outer `except Exception as e: logger.debug('error polling %s: %s'%(data['name'], str(e)))` to `except Exception: logger.exception('error polling %s'%data['name'])`. Leave the surrounding `'U'` fallback assignment untouched.

    EDIT 3 — whole-poll-iteration catch-all (approx. 288-289). Change `except Exception as e: logger.info('error in polling %s'%str(e))` to `except Exception: logger.exception('error in polling')`.

    EDIT 4 — `db_copy_thread` safety net (approx. line 346, currently a bare `except: pass`). Change to `except Exception:` with `logger.exception('unexpected error in db_copy_thread')` replacing the `pass`. `copy_db()` already logs its own known failure modes internally; this outer catch is the only thing that would hide an unanticipated error.

    EDIT 5 — `plugin_dirs` config parse (approx. 744-752). Delete `print('noconf', e)` and `print(e)` (D-04). Convert the `except configparser.NoSectionError as e:` branch to `logger.debug('no plugin_dirs section: %s'%str(e))` followed by the existing `pass` — this is an expected absent-section case, so it stays quiet and gets no traceback. Convert `except Exception as e:` to `except Exception:` with `logger.exception('invalid setting for plugin_dirs')` replacing the `logger.info('invalid setting for plugin_dirs')` line.

    CATEGORY B — DO NOT TOUCH. Per 02-RESEARCH.md's explicit disposition, leave every `try: <read one optional config value> except: <assign hardcoded default>` block in `config.__init__` and `drop_privileges` exactly as-is (approx. lines 448, 526, 537, 544, 554, 562, 565, 585, 636, 643, 649, 654, 659, 690, 694, 699, 703, 711, 718, 722, 727, 731, 736, 739, 775, plus the RRD counter-wrap guard at ~256). These fire on every normal daemon start with a minimal `pellmon.conf`; converting them to `logger.exception` would print 10-20 tracebacks per startup, which is itself an observable behavior change beyond OBS-01/02/03. Do not add logging to them and do not convert them to `except Exception:`.

    ALSO DO NOT TOUCH: `sendmail_thread`'s `except Exception as e: logger.info('error trying to send email'); logger.info(str(e))` (approx. 426-428). Per 02-RESEARCH.md's Security Domain note, `str(e)` from an `smtplib` auth failure can echo partial credential material; that block is outside this phase's Category A set and stays as-is.

    In total this file should show roughly 10-14 changed lines plus the one added logger line. A diff substantially larger than that means Category B was over-converted — revert and re-scope.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_pellmonsrv_logging.py tests/ -q -m "not known_broken"</automated>
  </verify>
  <acceptance_criteria>
    - `venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_pellmonsrv_logging.py -v` exits 0 with all four tests passing
    - `grep -n "print(" src/Pellmonsrv/pellmonsrv.py` returns no matches
    - `grep -c "logger.exception" src/Pellmonsrv/pellmonsrv.py` returns exactly 6
    - `grep -n "^logger = logging.getLogger('pellMon')" src/Pellmonsrv/pellmonsrv.py` returns exactly 1 match at module scope
    - `grep -c "global logger" src/Pellmonsrv/pellmonsrv.py` still returns 2 (the existing in-function assignments were not removed)
    - `grep -n "if conf.command == 'debug'" src/Pellmonsrv/pellmonsrv.py` still returns a match inside the activation-loop except block
    - `git diff --numstat src/Pellmonsrv/pellmonsrv.py` reports fewer than 25 changed lines total
    - `grep -vn "^\s*#" src/Pellmonsrv/pellmonsrv.py | grep -c "except:\s*$"` returns a count exactly 1 lower than the pre-edit count (only the `db_copy_thread` bare except was converted)
    - `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` exits 0
  </acceptance_criteria>
  <done>pellmonsrv.py has a module-level `pellMon` logger, six `logger.exception` call sites covering the Category A failure paths, zero `print()` calls, and an unchanged Category B config-fallback section.</done>
</task>

</tasks>

<threat_model>
N/A — observability-only change, no logic altered, verified via before/after outcome parity (plan 06). ASVS V7 audit applied: the six converted messages interpolate only plugin names, item names, and a fixed literal — no credentials. `sendmail_thread`'s credential-adjacent `str(e)` block is explicitly excluded from this plan's edit scope for that reason.
</threat_model>

<verification>
1. `venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_pellmonsrv_logging.py -v` — all four pass.
2. `grep -n "print(" src/Pellmonsrv/pellmonsrv.py` — empty.
3. `git diff src/Pellmonsrv/pellmonsrv.py` review — no line inside `config.__init__`'s optional-value fallbacks is touched.
4. `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` — exits 0.
</verification>

<success_criteria>
- A failing `plugin_object.activate()` logs an ERROR with exc_info naming the real exception, and the plugin still lands in `failed_plugins`.
- Debug-mode still re-raises.
- A failing item read during polling logs a traceback naming the item and still records `'U'`.
- `pellmonsrv.py` contains zero `print()` calls and an unchanged Category B section.
</success_criteria>

<output>
Create `.planning/phases/02-exception-visibility-retrofit/02-03-SUMMARY.md` when done, recording the module-level-logger rationale and the `git diff --numstat` line count.
</output>
