---
phase: 02-exception-visibility-retrofit
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/Pellmonsrv/test_plugin_manager_logging.py
  - src/Pellmonsrv/yapsy/PluginManager.py
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "A plugin module whose code raises on exec produces a full traceback in the log instead of a swallowed one-line message"
    - "A plugin descriptor that cannot be parsed still logs at debug level, but now carries the traceback"
    - "The per-symbol subclass probe in the plugin-category loop emits no log output at all during a normal plugin scan"
    - "No print() call remains in PluginManager.py"
  artifacts:
    - path: "tests/Pellmonsrv/test_plugin_manager_logging.py"
      provides: "caplog assertions on PluginManager's plugin-load failure path"
      min_lines: 40
    - path: "src/Pellmonsrv/yapsy/PluginManager.py"
      provides: "Loud plugin-load failures (OBS-01)"
      contains: "logging.exception"
  key_links:
    - from: "src/Pellmonsrv/yapsy/PluginManager.py"
      to: "logging.exception"
      via: "except Exception around the exec(compile(...)) plugin-load block"
      pattern: "logging\\.exception\\(\"Unable to execute the code in plugin"
---

<objective>
Make yapsy plugin-load failures loud (OBS-01). This is the single most impactful change in the phase: it is the direct reason ScotteCom's and NBEcom's broken imports produce zero operator-visible signal today.

Purpose: ROADMAP Phase 2 success criterion 2 — "a deliberately triggered plugin import/activation failure produces a full traceback via `logger.exception(...)` in the logs instead of being silently skipped by a bare `except:` in the yapsy plugin manager."
Output: a caplog regression test plus three surgically-scoped edits in `PluginManager.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exception-visibility-retrofit/02-CONTEXT.md
@.planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md
@.planning/phases/02-exception-visibility-retrofit/02-PATTERNS.md

<interfaces>
<!-- PluginManager.py's own logging convention, verified 2026-09-17:
     this file does NOT use `getLogger('pellMon')`. It calls the `logging`
     module-level functions directly (logging.debug(...) at lines 192/195/204).
     Preserve that convention — do not introduce a module logger here. -->

Relevant PluginManager.py API surface used by the test:
  PluginManager(categories_filter={...}, directories_list=[...], plugin_info_ext='pellmon-plugin')
  .locatePlugins()
  .loadPlugins()
  .getPluginsOfCategory(name)

Plugin descriptor format (src/Pellmonsrv/plugins/*.pellmon-plugin):
  [Core]
  Name = <display name>
  Module = <package dir name>
  [Documentation]
  ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the caplog regression test for plugin-load failure visibility</name>
  <files>tests/Pellmonsrv/test_plugin_manager_logging.py</files>
  <read_first>
    - src/Pellmonsrv/yapsy/PluginManager.py (lines 190-300 — locatePlugins descriptor parse, loadPlugins exec block, category subclass probe)
    - tests/test_plugin_imports.py (Phase 1 conventions: module docstring naming the requirement, descriptor-driven discovery, pathlib-based repo-root resolution)
    - src/Pellmonsrv/plugins/testplugin.pellmon-plugin (the descriptor format to imitate in the fixture)
    - .planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md (Pitfall 5 — assert on structured LogRecord attributes, never on captured console text)
  </read_first>
  <action>
    Create `tests/Pellmonsrv/test_plugin_manager_logging.py` asserting OBS-01's observable behavior against `Pellmonsrv.yapsy.PluginManager`. This module imports cleanly on `venv-py3` today (verified) — no stub fixture needed, do NOT use the `daemon_module` fixture here.

    Build a `tmp_path`-based fixture that writes a minimal fake plugin directory: a `broken.pellmon-plugin` descriptor with `[Core] Name = Broken` and `Module = broken`, plus a sibling `broken.py` whose module body raises at exec time (for example a bare `raise RuntimeError("deliberate plugin load failure")` at module scope). Point a `PluginManager` at that directory with `plugin_info_ext='pellmon-plugin'`, then call `locatePlugins()` and `loadPlugins()`.

    Required assertions, using pytest's built-in `caplog` fixture with `caplog.set_level(logging.DEBUG)`:
    - At least one `caplog.records` entry has `levelno == logging.ERROR` AND `record.exc_info is not None` AND `"Unable to execute the code in plugin"` in `record.getMessage()`. Assert on `record.exc_info is not None` — that is the traceback-attached proof — not on formatted text.
    - `record.exc_info[0] is RuntimeError` for that record, proving the real underlying exception type reaches the log rather than being flattened to a string.
    - `loadPlugins()` does not raise — control flow is unchanged; the broken plugin is still skipped, exactly as before.

    Add a second test covering the descriptor-parse path: write a `malformed.pellmon-plugin` file containing text that `configparser` rejects (e.g. a line with no section header), run `locatePlugins()`, and assert a record exists with `levelno == logging.DEBUG`, `record.exc_info is not None`, and `"Could not parse the plugin file"` in `record.getMessage()`.

    Add a third test asserting the hot-loop probe stays silent: run `loadPlugins()` against a directory containing a syntactically valid plugin module that defines several non-class module-level symbols, and assert that no `caplog.records` entry originates from the `issubclass` probe — concretely, assert that `caplog.records` contains zero records whose `getMessage()` mentions `issubclass` and zero ERROR-level records for that successful load. This test is what prevents a future contributor from "fixing" the D-03 narrow `except TypeError:` into a logged broad catch.

    Module docstring must name OBS-01, cite ROADMAP Phase 2 success criterion 2, and state that these tests are expected to FAIL until Task 2 lands.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_plugin_manager_logging.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists with three test functions covering: exec-failure ERROR+exc_info, descriptor-parse DEBUG+exc_info, and probe-silence
    - Before Task 2 lands, running the file fails with assertion errors (not collection/import errors) — record this RED result in the SUMMARY
    - No assertion in the file compares against `caplog.text` or captured stdout; every assertion reads `record.levelno`, `record.exc_info`, or `record.getMessage()`
    - `grep -n "exc_info is not None" tests/Pellmonsrv/test_plugin_manager_logging.py` returns at least 2 matches
  </acceptance_criteria>
  <done>Three caplog-based tests exist and fail with assertion errors against the current unmodified PluginManager.py.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the three D-03 dispositions in PluginManager.py</name>
  <files>src/Pellmonsrv/yapsy/PluginManager.py</files>
  <read_first>
    - src/Pellmonsrv/yapsy/PluginManager.py (lines 190-300 — re-grep the three sites before editing; 02-PATTERNS.md flags line-drift risk)
    - .planning/phases/02-exception-visibility-retrofit/02-PATTERNS.md (section "src/Pellmonsrv/yapsy/PluginManager.py" — exact verified before/after pairs)
    - .planning/phases/02-exception-visibility-retrofit/02-CONTEXT.md (D-03, D-04, D-05)
    - tests/Pellmonsrv/test_plugin_manager_logging.py (from Task 1 — the assertions this edit must satisfy)
  </read_first>
  <action>
    Make exactly three edits. Touch no other `except` clause and no control-flow statement in this file. Locate each site by grep (line numbers below are as of 2026-09-17 and may drift).

    EDIT 1 — descriptor parse (approx. lines 207-212, the `try: config_parser.read(candidate_infofile)` block):
    Change the bare `except:` to `except Exception:`. Keep the existing `logging.debug("Could not parse the plugin file %s" ...)` call at DEBUG level per D-03, but convert it to lazy `%`-style logging args and attach the traceback: pass `candidate_infofile` as a logging argument rather than pre-interpolating, and add `exc_info=True`. Use `logging.debug(..., exc_info=True)` here, NOT `logging.exception(...)` — `logging.exception()` is hardcoded to ERROR level in the stdlib and has no DEBUG-level equivalent, and D-03 explicitly keeps this site at debug severity. Keep the `continue` statement unchanged.

    EDIT 2 — plugin load/exec (approx. lines 273-281, the block that opens `candidate_filepath+".py"` and runs `exec(compile(...), candidate_globals)`). This is THE OBS-01 target:
    - Delete the `print(candidate_filepath)` call inside the `try` (D-04).
    - Delete the `print(e)` call inside the `except` (D-04).
    - Change `except Exception as e:` to `except Exception:` (the bound name is no longer needed once the traceback is auto-captured, per D-05).
    - Replace the two existing `logging.debug(...)` calls — the `"Unable to execute the code in plugin: %s"` one and the `"\t The following problem occured: %s %s "` one — with a single call: `logging.exception("Unable to execute the code in plugin: %s", candidate_filepath)`. The message string must match what Task 1's test asserts on.
    - Do not add a `raise`, do not add a `continue`, do not change what happens after the except block. The broken plugin must still be silently skipped from the caller's perspective — only the log stream changes.

    EDIT 3 — category subclass probe (approx. lines 287-290, `is_correct_subclass = issubclass(element, self.categories_interfaces[category_name])`):
    Change the bare `except:` to `except TypeError:` and nothing else. Add NO logging call. Per D-03 this probe tests every module-level symbol against every registered category; non-class symbols raise `TypeError` from `issubclass` on every normal scan, so logging here would flood the log with expected non-failures. `TypeError` is the only exception `issubclass()` raises for a non-class argument.

    Leave every other `except` clause, `logging.debug` call, and statement in this file untouched. This file uses module-level `logging.*` function calls, not `getLogger('pellMon')` — do not introduce a module logger (its own convention wins; D-01 governs files that need a logger added, which this one does not).
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_plugin_manager_logging.py tests/ -q -m "not known_broken"</automated>
  </verify>
  <acceptance_criteria>
    - `venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_plugin_manager_logging.py -v` exits 0 with all three tests passing
    - `grep -n "print(" src/Pellmonsrv/yapsy/PluginManager.py` returns no matches
    - `grep -c "logging.exception" src/Pellmonsrv/yapsy/PluginManager.py` returns exactly 1
    - `grep -n "except TypeError:" src/Pellmonsrv/yapsy/PluginManager.py` returns exactly 1 match, located in the `issubclass` probe block
    - `grep -vn "^\s*#" src/Pellmonsrv/yapsy/PluginManager.py | grep -c "except:\s*$"` shows a count lower than the pre-edit count by exactly 2 (the descriptor-parse and probe sites)
    - `grep -n "getLogger" src/Pellmonsrv/yapsy/PluginManager.py` returns no matches (file convention preserved)
    - `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` exits 0
  </acceptance_criteria>
  <done>PluginManager.py logs a full traceback for plugin exec failures, carries exc_info on descriptor-parse debug logs, narrows the probe to `except TypeError:` with no logging, and contains zero `print()` calls.</done>
</task>

</tasks>

<threat_model>
N/A — observability-only change, no logic altered, verified via before/after outcome parity (plan 06). One ASVS V7 note applied: the converted `logging.exception` message interpolates only `candidate_filepath` (a filesystem path), never a credential or key, so no information-disclosure risk is introduced by the added verbosity.
</threat_model>

<verification>
1. `venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_plugin_manager_logging.py -v` — all pass.
2. `grep -n "print(" src/Pellmonsrv/yapsy/PluginManager.py` — empty.
3. `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` — exits 0.
4. Diff review: the change touches exactly three `except` blocks and removes exactly two `print()` calls; no other line differs.
</verification>

<success_criteria>
- A deliberately broken plugin module produces an ERROR-level log record with `exc_info` naming the real underlying exception type.
- The descriptor-parse path still logs at DEBUG but now carries the traceback.
- A normal plugin scan produces zero log records from the subclass probe.
- Phase 1's green-baseline suite still exits 0.
</success_criteria>

<output>
Create `.planning/phases/02-exception-visibility-retrofit/02-02-SUMMARY.md` when done.
</output>
