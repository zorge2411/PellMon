---
phase: 02-exception-visibility-retrofit
plan: 06
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04", "02-05"]
files_modified:
  - tests/test_no_ad_hoc_print.py
  - .planning/phases/02-exception-visibility-retrofit/after.xml
  - .planning/ROADMAP.md
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "A repo-wide check enforces that the only print() calls left in src/Pellmonsrv and src/Pellmonweb are the three pellmonconf CLI banner lines, and it fails if a new one is introduced"
    - "Every test that existed before this phase has exactly the same pass/fail/skip outcome after it"
    - "ROADMAP Phase 2 success criterion 3 no longer names a file this phase was locked out of editing"
  artifacts:
    - path: "tests/test_no_ad_hoc_print.py"
      provides: "Executable OBS-03 enforcement gate"
      min_lines: 30
    - path: ".planning/phases/02-exception-visibility-retrofit/after.xml"
      provides: "Post-change JUnit outcome record"
      contains: "<testsuite"
  key_links:
    - from: "junit-outcome-diff.py"
      to: ".planning/phases/02-exception-visibility-retrofit/after.xml"
      via: "outcome-only comparison against before.xml"
      pattern: "IDENTICAL pass/fail/skip status"
---

<objective>
Close the phase: turn OBS-03 into an enforced automated gate, prove ROADMAP Phase 2 success criterion 1 (identical pass/fail before and after) with the executable outcome diff, and resolve the documented ROADMAP/CONTEXT conflict about `Scotteprotocol/protocol.py`.

Purpose: Without the enforcement test, OBS-03 regresses the first time someone adds a debug print. Without the after.xml diff, "behavior-preserving" is an assertion rather than a measurement.
Output: `tests/test_no_ad_hoc_print.py`, `after.xml`, an empty outcome diff, and an amended ROADMAP success criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exception-visibility-retrofit/02-CONTEXT.md
@.planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md
@.planning/phases/02-exception-visibility-retrofit/02-VALIDATION.md
@.planning/phases/02-exception-visibility-retrofit/02-01-SUMMARY.md
@.planning/phases/02-exception-visibility-retrofit/02-05-SUMMARY.md

<interfaces>
<!-- From plan 02-01: junit-outcome-diff.py
     CLI:  python junit-outcome-diff.py <before.xml> <after.xml>
     Exit: 0 only when no BEFORE test changed outcome and no BEFORE test disappeared.
           AFTER-only test IDs (the new Phase 2 test files) are reported but never fail it.
     Success line: "IDENTICAL pass/fail/skip status before and after"           -->

<!-- Allowed surviving print() sites after this phase (D-04 intentional CLI output):
       src/Pellmonweb/pellmonconf.py  'Open http://<ip>:%u ...'
       src/Pellmonweb/pellmonconf.py  'Run as root to be able to save changes'
       src/Pellmonweb/pellmonconf.py  'Quit with CTRL-C'                         -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the OBS-03 print-enforcement test</name>
  <files>tests/test_no_ad_hoc_print.py</files>
  <read_first>
    - tests/test_plugin_imports.py (Phase 1 conventions: module docstring naming the requirement, `pathlib`-based repo-root resolution via `parents[1]`, descriptor/AST-driven discovery rather than hardcoded lists)
    - src/Pellmonweb/pellmonconf.py (lines 150-160 — the exact three banner calls that must be allowlisted)
    - .planning/phases/02-exception-visibility-retrofit/02-CONTEXT.md (D-04 — the sweep boundary this test encodes)
  </read_first>
  <action>
    Create `tests/test_no_ad_hoc_print.py` implementing ROADMAP Phase 2 success criterion 4 as an executable gate.

    Walk every `*.py` file under `src/Pellmonsrv/` and `src/Pellmonweb/`, excluding anything whose name ends in `.py2bak`. Parse each file with the stdlib `ast` module and collect every `ast.Call` whose func is an `ast.Name` with `id == "print"`, recording `(relative_path, node.lineno)`. Use `ast`, not a regex over source text — a regex over `print(` would also match the string `"print("` inside a Mako template fragment or a comment, producing false positives, and would miss a print split across lines.

    Define an explicit allowlist constant naming the three sanctioned sites as `(relative_path, <banner text fragment>)` pairs for `src/Pellmonweb/pellmonconf.py`, matched by path plus the literal first string argument rather than by line number, so the test does not break when unrelated edits shift lines. Document in a comment beside the allowlist why each entry is exempt (manually-invoked standalone CLI tool startup banner, D-04) and that widening this allowlist requires a documented decision, not a convenience edit.

    The test asserts the collected set minus the allowlist is empty, and on failure reports each offending `path:lineno` in the assertion message so the failure is immediately actionable.

    Add a second test asserting the allowlist is not vacuous: all three allowlisted sites must actually be found in `pellmonconf.py`. Without this, deleting the banner would silently leave a passing-but-meaningless gate.

    Module docstring must name OBS-03 and cite ROADMAP Phase 2 success criterion 4. This test must run on any interpreter — import nothing beyond `ast`, `pathlib`, and `pytest`, and never import the modules it scans.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/test_no_ad_hoc_print.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `venv-py3/Scripts/python.exe -m pytest tests/test_no_ad_hoc_print.py -v` exits 0 with both tests passing
    - `grep -n "import ast" tests/test_no_ad_hoc_print.py` returns a match; `grep -c "subprocess\|re.compile\|grep" tests/test_no_ad_hoc_print.py` returns 0
    - Temporarily adding a `print('scratch')` line to `src/Pellmonsrv/database.py` makes the first test fail with an assertion message naming `src/Pellmonsrv/database.py` and its line number; remove the temporary line afterwards and confirm the test passes again — record both observations in the SUMMARY
    - The allowlist is keyed on path plus string content, not on line numbers (`grep -c "154\|155\|156" tests/test_no_ad_hoc_print.py` returns 0)
    - The non-vacuousness test asserts all three allowlisted sites are actually present
  </acceptance_criteria>
  <done>An AST-based test enforces that the only remaining prints in src/ are the three annotated pellmonconf banner lines, and it demonstrably fails when a new print is added.</done>
</task>

<task type="auto">
  <name>Task 2: Capture after.xml and prove outcome parity</name>
  <files>.planning/phases/02-exception-visibility-retrofit/after.xml</files>
  <read_first>
    - .planning/phases/02-exception-visibility-retrofit/02-VALIDATION.md (the Before/After Regression Check section)
    - .planning/phases/02-exception-visibility-retrofit/02-01-SUMMARY.md (the interpreter and the exact before-run tally to compare against)
    - junit-outcome-diff.py (the comparison semantics established in plan 02-01)
    - .planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md (Pitfall 1 — interpreter/platform must match the before run; Pitfall 5 — never diff console text)
  </read_first>
  <action>
    This task runs only after plans 02-02, 02-03, 02-04 and 02-05 have all landed. Confirm first that `git status` shows all Phase 2 source edits present in the working tree.

    Capture the post-change outcome record using the SAME interpreter recorded in `02-01-SUMMARY.md` — `venv-py3/Scripts/python.exe`. Per Pitfall 1, running this on WSL/Linux instead would introduce an additional pre-existing `test_plugin_module_imports[calculate]` failure that has nothing to do with Phase 2, making the comparison meaningless.

    `venv-py3/Scripts/python.exe -m pytest tests/ -v --junitxml=.planning/phases/02-exception-visibility-retrofit/after.xml`

    Then run the comparison:
    `venv-py3/Scripts/python.exe junit-outcome-diff.py .planning/phases/02-exception-visibility-retrofit/before.xml .planning/phases/02-exception-visibility-retrofit/after.xml`

    Required outcome: exit 0 with the line `IDENTICAL pass/fail/skip status before and after`. The tool's ADDED section should list the new Phase 2 test IDs from `test_plugin_manager_logging.py`, `test_pellmonsrv_logging.py`, `test_calculate_logging.py`, and `test_no_ad_hoc_print.py`, all passing — record that list in the SUMMARY as evidence the new coverage actually ran.

    If the tool exits nonzero, do NOT edit `before.xml`, do NOT add a test to an allowlist, and do NOT rerun hoping for a different result. Report the specific `test_id: before -> after` pairs in the SUMMARY as a phase blocker — a changed outcome means a Phase 2 edit altered behavior, which contradicts the phase's entire premise and must be root-caused in the owning plan.

    Record in the SUMMARY: the after-run pytest tally line, the before-run tally from `02-01-SUMMARY.md`, and the diff tool's exit code. Explicitly confirm the two documented expected-red failures (`test_plugin_module_imports[scottecom]`, `test_nbecom_deferred_protocol_import`) are still failing with the same failure type.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe junit-outcome-diff.py .planning/phases/02-exception-visibility-retrofit/before.xml .planning/phases/02-exception-visibility-retrofit/after.xml</automated>
  </verify>
  <acceptance_criteria>
    - `after.xml` exists, parses, and contains strictly more `<testcase>` elements than `before.xml` (the four new Phase 2 test files)
    - The diff command exits 0 and prints `IDENTICAL pass/fail/skip status before and after`
    - The SUMMARY records both tally lines and lists the ADDED test IDs with their outcomes
    - `after.xml` still shows `<failure>` children for `test_plugin_module_imports[scottecom]` and `test_nbecom_deferred_protocol_import`
    - `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` exits 0
  </acceptance_criteria>
  <done>after.xml is captured on the same interpreter as before.xml, and the outcome diff exits 0 with no BEFORE test having changed or disappeared.</done>
</task>

<task type="auto">
  <name>Task 3: Give the Scotteprotocol deferral a Phase 3 owner and align criterion 4 with D-01</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 "Success Criteria" block, criterion 3; and Phase 3's goal/requirements block where the deferred work lands)
    - .planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md (Open Question 1 and Pitfall 4 — the evidence and the recommended resolution)
    - .planning/phases/02-exception-visibility-retrofit/02-CONTEXT.md (D-02 — the locked three-file scope that already excludes Scotteprotocol)
  </read_first>
  <action>
    Two ROADMAP alignments remain after Phase 2's edits. Neither changes any phase's scope.

    ALIGNMENT 1 — name a Phase 3 owner for the deferred Scotteprotocol sweep. Phase 2 success criterion 3 was already amended in a prior commit to exclude `Scotteprotocol/protocol.py`, but the deferred work has no owner recorded on the receiving side. Add a line to Phase 3's detail block (under its Goal/Depends on/Requirements header, before or alongside its Success Criteria) stating that Phase 3 inherits the exception-visibility sweep of `src/Scotteprotocol/protocol.py`'s 19 bare/broad excepts alongside IMPORT-01, because the module cannot be imported under Python 3 today (verified: `Scotteprotocol/__init__.py:2`'s `from protocol import Protocol` raises `ModuleNotFoundError` before `protocol.py` is loaded) and exception-visibility work in an unimportable module cannot be verified. If, on reading, Phase 2 criterion 3 no longer carries the exclusion note, restore it per 02-RESEARCH.md Open Question 1 option (b).

    ALIGNMENT 2 — fix the D-01 conflict in Phase 2 success criterion 4. The criterion currently ends with "runtime code uses `logging.getLogger(__name__)` at appropriate levels", which directly contradicts locked decision D-01 (keep the shared `getLogger('pellMon')` convention; the REQUIREMENTS.md OBS-03 wording is directional, not a literal mandate to fragment the logger namespace mid-migration). Change that clause to state that runtime code uses the shared `logging.getLogger('pellMon')` logger at appropriate levels, per CONTEXT D-01. Leave the first half of criterion 4 (the repo-wide print search) unchanged.

    Do not change any other phase's goal, requirements list, or success criteria. Do not renumber criteria. Do not alter the Phase 2 plan checklist (the orchestrator maintains it).
  </action>
  <verify>
    <automated>grep -c "getLogger(__name__)" .planning/ROADMAP.md; grep -c "Scotteprotocol" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase 3's detail block contains a line naming `src/Scotteprotocol/protocol.py`'s exception-visibility sweep as work inherited alongside IMPORT-01
    - Phase 2 success criterion 3 names only `pellmonsrv.py` and `plugins/calculate/__init__.py`, and carries a deferral note pointing at Phase 3
    - `grep -c "getLogger(__name__)" .planning/ROADMAP.md` returns 0
    - `grep -n "getLogger('pellMon')" .planning/ROADMAP.md` returns a match inside Phase 2 success criterion 4
    - `grep -c "Success Criteria" .planning/ROADMAP.md` still returns 5 (no phase block structurally damaged) and Phase 2's criteria still number 1-4
    - `git diff .planning/ROADMAP.md` touches only the Phase 2 criterion-4 line and the added Phase 3 line (plus criterion 3 only if the exclusion note needed restoring)
  </acceptance_criteria>
  <done>The deferred Scotteprotocol sweep has a named Phase 3 owner, and Phase 2 criterion 4 cites the shared `pellMon` logger instead of `__name__`.</done>
</task>

</tasks>

<threat_model>
N/A — observability-only phase. This plan adds a static-analysis test, captures a test-outcome record, and edits a planning document; no runtime source is modified. No packages installed, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
1. `venv-py3/Scripts/python.exe -m pytest tests/test_no_ad_hoc_print.py -v` — exits 0.
2. `venv-py3/Scripts/python.exe junit-outcome-diff.py before.xml after.xml` — exits 0, prints `IDENTICAL pass/fail/skip status before and after`.
3. `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` — exits 0.
4. `grep -rn "print(" src/Pellmonsrv src/Pellmonweb --include="*.py" | grep -v "\.py2bak"` — exactly 3 lines, all in `pellmonconf.py`.
</verification>

<success_criteria>
- The print-enforcement gate passes and demonstrably fails when a new print is introduced.
- Every test present before Phase 2 has an identical outcome after it; the four new Phase 2 test files all pass.
- ROADMAP Phase 2 criteria and CONTEXT D-01/D-02 no longer conflict, and the deferred Scotteprotocol work is owned by Phase 3.
</success_criteria>

<output>
Create `.planning/phases/02-exception-visibility-retrofit/02-06-SUMMARY.md` when done, containing both pytest tally lines, the diff tool exit code, and the list of ADDED test IDs.
</output>
