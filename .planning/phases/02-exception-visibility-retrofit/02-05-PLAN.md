---
phase: 02-exception-visibility-retrofit
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/Pellmonsrv/database.py
  - src/Pellmonsrv/daemon.py
  - src/Pellmonsrv/plugins/consumption/__init__.py
  - src/Pellmonsrv/plugins/onewire/__init__.py
  - src/Pellmonsrv/plugins/owfs/__init__.py
  - src/Pellmonsrv/plugins/testplugin/__init__.py
  - src/Pellmonsrv/plugins/nbecom/__init__.py
  - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py
  - src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py
  - src/Pellmonweb/pellmonweb.py
  - src/Pellmonweb/pellmonconf.py
autonomous: true
requirements: [OBS-03]
must_haves:
  truths:
    - "Runtime failures in database.py, daemon.py, and the plugin modules reach the pellMon log instead of stdout"
    - "The NBE protocol state machine's retry/reconnect trace goes to the log at debug/warning level instead of stdout"
    - "The pellmonconf standalone CLI startup banner still prints to stdout"
    - "No behavior changes: every converted site keeps its surrounding control flow and return values"
  artifacts:
    - path: "src/Pellmonsrv/database.py"
      provides: "Module logger plus logged SQLite/item failures"
      contains: "getLogger('pellMon')"
    - path: "src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py"
      provides: "Logged NBE retry/reconnect trace (17 converted sites)"
      contains: "logger."
    - path: "src/Pellmonweb/pellmonconf.py"
      provides: "Logged config-path debug line, preserved CLI banner"
      contains: "getLogger('pellMon')"
  key_links:
    - from: "src/Pellmonsrv/database.py"
      to: "logging"
      via: "module-level shared-name logger, copied from plugins/consumption/__init__.py"
      pattern: "logger = getLogger\\('pellMon'\\)"
---

<objective>
Replace the 39 ad hoc `print()` calls that remain across `src/Pellmonsrv/` and `src/Pellmonweb/` (outside the PluginManager and pellmonsrv sites owned by plans 02 and 03) with shared-logger calls at appropriate levels, and add a module logger to the five files that have none.

Purpose: OBS-03 / ROADMAP Phase 2 success criterion 4 — "a repo-wide search finds no remaining ad hoc `print()` calls in `src/` outside of intentional CLI output."
Output: eleven files converted; exactly three `print()` calls survive, all of them the `pellmonconf.py` CLI startup banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exception-visibility-retrofit/02-CONTEXT.md
@.planning/phases/02-exception-visibility-retrofit/02-RESEARCH.md
@.planning/phases/02-exception-visibility-retrofit/02-PATTERNS.md

<interfaces>
<!-- Shared logger convention (D-01), verified at plugins/consumption/__init__.py:28,39 -->
from logging import getLogger
logger = getLogger('pellMon')     # module scope, shared NAME across all modules.
                                  # Do NOT use getLogger(__name__) — D-01 is locked.

<!-- Files that ALREADY have `logger = getLogger('pellMon')` (verified 2026-09-17):
       plugins/consumption/__init__.py:39
       plugins/onewire/__init__.py:28
       plugins/owfs/__init__.py:30
       plugins/testplugin/__init__.py:24
       plugins/nbecom/__init__.py:30
       plugins/nbecom/nbeprotocol/protocol.py:33
     Files with NO logger at all — one must be ADDED:
       src/Pellmonsrv/database.py
       src/Pellmonsrv/daemon.py
       src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py
       src/Pellmonweb/pellmonweb.py
       src/Pellmonweb/pellmonconf.py                                            -->

<!-- Full print() inventory for this plan, verified by grep 2026-09-17 (39 sites).
     Line numbers may drift — re-grep each file before editing.
       database.py                          113, 170
       daemon.py                            131
       plugins/consumption/__init__.py      209
       plugins/onewire/__init__.py          85
       plugins/owfs/__init__.py             159, 193, 234
       plugins/testplugin/__init__.py       64
       plugins/nbecom/__init__.py           92, 99, 127, 250, 267
       plugins/nbecom/nbeprotocol/protocol.py  75, 81, 106, 115, 119, 122, 132,
                                               149, 151, 197, 200, 219, 233,
                                               309, 314, 317, 332
       plugins/nbecom/nbeprotocol/frames.py 88
       Pellmonweb/pellmonweb.py             132, 138, 556
       Pellmonweb/pellmonconf.py            125  (convert)
       Pellmonweb/pellmonconf.py            154, 155, 156  (KEEP — CLI banner)     -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert prints in the daemon core and the simple plugins</name>
  <files>src/Pellmonsrv/database.py, src/Pellmonsrv/daemon.py, src/Pellmonsrv/plugins/consumption/__init__.py, src/Pellmonsrv/plugins/onewire/__init__.py, src/Pellmonsrv/plugins/owfs/__init__.py, src/Pellmonsrv/plugins/testplugin/__init__.py</files>
  <read_first>
    - src/Pellmonsrv/database.py (lines 1-30 imports, 100-200 for the two `print(e)` sites in the Getsetitem value setter and Keyval_storage.readval)
    - src/Pellmonsrv/daemon.py (lines 110-145 for the stderr-redirect fallback `print(str(err))`)
    - src/Pellmonsrv/plugins/consumption/__init__.py (lines 20-45 — this is the canonical logger-import analog to copy)
    - src/Pellmonsrv/plugins/owfs/__init__.py (lines 150-240 — the three `print(exc_type, exc_value)` sites and their surrounding `sys.exc_info()` assignments)
    - src/Pellmonsrv/plugins/testplugin/__init__.py (lines 55-70)
    - tests/Pellmonsrv/test_database.py (Phase 1 coverage of database.py — its outcomes must not change)
  </read_first>
  <action>
    Re-grep `print(` in each of the six files before editing; line numbers below are as of 2026-09-17.

    `src/Pellmonsrv/database.py` — this file has NO logger. Add `from logging import getLogger` alongside the existing `import sqlite3, threading` / `import time` block near the top, and `logger = getLogger('pellMon')` at module scope after the imports, copying the exact style of `plugins/consumption/__init__.py:28,39`. Then:
      - approx. line 113, inside the `Getsetitem` value setter's `except Exception as e:` — replace `print(e)` with `logger.exception('error setting value for %s'%self.name)` and change the clause to `except Exception:`. Match the file's existing (irregular) indentation exactly; do not reformat the surrounding block.
      - approx. line 170, inside `Keyval_storage.readval`'s `except Exception as e:` — replace `print(e)` with `logger.exception('error reading value for %s'%item)` (use whatever the local key/item variable is actually named at that site) and change the clause to `except Exception:`. Leave `return 'error'` untouched.
      - Also grep this file for a third `print(` in the `writeval` path; if one exists, apply the same treatment with a message naming the write target.

    `src/Pellmonsrv/daemon.py` — this file has NO logger. Add the same two lines. Then at approx. line 131, inside the daemonizer's stderr-redirect fallback, replace `print(str(err))` with `logger.exception('error redirecting stderr')`. This is the last-resort path executed just before stdio is redirected away, which is precisely why printing is useless there. Leave the surrounding retry/fallback control flow untouched.

    `src/Pellmonsrv/plugins/consumption/__init__.py` — logger already present at line 39. At approx. line 209, replace `print(e)` with `logger.exception(...)` using a message naming the operation at that site, and narrow the clause to `except Exception:` if it currently binds `e` only for the print.

    `src/Pellmonsrv/plugins/onewire/__init__.py` — logger present at line 28. At approx. line 85, same treatment as consumption.

    `src/Pellmonsrv/plugins/owfs/__init__.py` — logger present at line 30. At approx. lines 159, 193, 234, each `print(exc_type, exc_value)` sits inside an `except:` block. Replace each with `logger.exception(...)` using a message naming the enclosing operation. IMPORTANT: leave any surrounding `exc_type, exc_value, exc_traceback = sys.exc_info()` assignment in place even though it becomes unused — removing it risks touching a variable referenced later in the block. Do NOT widen or narrow these bare `except:` clauses in this plan; only the print call changes.

    `src/Pellmonsrv/plugins/testplugin/__init__.py` — logger present at line 24. At approx. line 64, replace `print('testplugin set: ', name, value)` with `logger.debug('testplugin set: %s %s', name, value)`. This is a debug trace in a test-fixture plugin, not an error. Verified 2026-09-17: no test in `tests/` references `testplugin` stdout and no test uses `capsys`/`capfd`, so this conversion cannot break an existing assertion.

    Use `%`-formatting for all messages, matching the dominant convention in `src/Pellmonsrv/` per CLAUDE.md. Do not introduce f-strings in these files. Do not alter any control flow, return value, or `except` clause behavior beyond dropping an `as e` binding that becomes unused.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "print(" src/Pellmonsrv/database.py src/Pellmonsrv/daemon.py src/Pellmonsrv/plugins/consumption/__init__.py src/Pellmonsrv/plugins/onewire/__init__.py src/Pellmonsrv/plugins/owfs/__init__.py src/Pellmonsrv/plugins/testplugin/__init__.py` returns no matches
    - `grep -c "getLogger('pellMon')" src/Pellmonsrv/database.py` returns 1 and the same for `src/Pellmonsrv/daemon.py`
    - `grep -c "getLogger(__name__)" src/Pellmonsrv src/Pellmonweb -r` returns 0 (D-01 preserved)
    - `venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_database.py -v` exits 0 with the same test count and outcomes as before the edit
    - `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` exits 0
    - `git diff src/Pellmonsrv/database.py` shows no reformatting of surrounding lines — only the logger import/instantiation additions and the two (or three) converted sites
  </acceptance_criteria>
  <done>Six daemon-side files are print-free, database.py and daemon.py each have a shared-name module logger, and Phase 1's database tests still pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the 23 debug-trace prints in the NBE protocol package</name>
  <files>src/Pellmonsrv/plugins/nbecom/__init__.py, src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py, src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py</files>
  <read_first>
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py (lines 25-40 for the existing logger, plus each of the 17 print sites in context to judge severity)
    - src/Pellmonsrv/plugins/nbecom/__init__.py (lines 20-35 imports and logger, plus the 5 print sites)
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py (lines 1-40 imports, line ~88 print site)
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocolexceptions.py (the protocol_error/seqnum_error/protocol_timeout/protocol_offline hierarchy referenced by the retry state machine)
    - tests/test_plugin_imports.py (its `test_nbecom_deferred_protocol_import` is a documented expected-red case — it must keep failing for the SAME reason after this task)
  </read_first>
  <action>
    Re-grep `print(` in all three files before editing. This package is part of the NBEcom deferred-import chain owned by Phase 3 (IMPORT-02) — restrict every change to logging calls. Do NOT touch any `import` statement, any control-flow statement, any retry counter, or any exception class.

    `src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py` (logger already at line 33) — 17 sites at approx. 75, 81, 106, 115, 119, 122, 132, 149, 151, 197, 200, 219, 233, 309, 314, 317, 332. Assign a level per site by reading its surrounding context:
      - retry/reconnect/state-transition trace prints -> `logger.debug(...)`
      - "no more retry" / give-up / connection-lost prints -> `logger.warning(...)`
      - prints inside an `except` block that render an exception (`print(repr(e))` and similar) -> `logger.exception(...)` with a message naming the operation, and drop the `repr(e)`/`str(e)` interpolation since `exception()` attaches the real traceback
    Record the chosen level for each of the 17 sites in the plan SUMMARY as a line-number-to-level table, so a reviewer can audit the severity judgments without re-reading the file.

    `src/Pellmonsrv/plugins/nbecom/__init__.py` (logger already at line 30) — 5 sites at approx. 92, 99, 127, 250, 267. Site 92 (`print('wait controller')`) is a state trace -> `logger.debug('wait controller')`. Sites 99, 127, 250, 267 all render an exception repr inside an except block -> `logger.exception(...)` with a message naming the operation (e.g. the `'direrror'` site becomes a message about the directory request; the `'exc in getgroup'` site's message must retain the group `name`). Drop the manual `repr(e)`/`time.time()` interpolation — `logger.exception` records both the traceback and the timestamp.

    `src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py` — this file has NO logger. Add `from logging import getLogger` and module-level `logger = getLogger('pellMon')` following the `plugins/consumption/__init__.py` pattern. Then at approx. line 88, replace `print('ERRROR chiphertext too short', len(h))` with `logger.error('ciphertext too short: %d', len(h))`. Fixing the two typos (`ERRROR`, `chiphertext`) is explicitly sanctioned — it is a log message string, not logic. Leave the surrounding branch behavior untouched.

    Use `%`-formatting or lazy logging args throughout; no f-strings in `src/Pellmonsrv/`.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "print(" src/Pellmonsrv/plugins/nbecom/` returns no matches
    - `grep -c "getLogger('pellMon')" src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py` returns 1
    - `grep -n "ciphertext too short" src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py` returns a match and `grep -n "ERRROR\|chiphertext" src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py` returns none
    - `git diff src/Pellmonsrv/plugins/nbecom/` contains no changed line that is an `import` statement (other than the two added logger lines in frames.py), no changed `if`/`while`/`for`/`return`/`raise` statement, and no changed exception class name
    - `venv-py3/Scripts/python.exe -m pytest tests/test_plugin_imports.py -v` shows `test_nbecom_deferred_protocol_import` still failing with the same failure type recorded in before.xml
    - The SUMMARY contains a 17-row line-number-to-log-level table for protocol.py
    - `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` exits 0
  </acceptance_criteria>
  <done>All 23 NBE-package prints are severity-appropriate logger calls, frames.py has a shared-name logger, and the NBEcom expected-red import test fails for exactly the same pre-existing reason.</done>
</task>

<task type="auto">
  <name>Task 3: Convert prints in the web process, preserving the pellmonconf CLI banner</name>
  <files>src/Pellmonweb/pellmonweb.py, src/Pellmonweb/pellmonconf.py</files>
  <read_first>
    - src/Pellmonweb/pellmonweb.py (lines 1-60 imports; 120-145 for `Dbus_handler.start()`'s two connectivity prints; ~550-560 for the parameterlist print)
    - src/Pellmonweb/pellmonconf.py (lines 110-160 — the config-path print at 125 and the three-line CLI banner at 154-156)
    - src/Pellmonsrv/plugins/consumption/__init__.py (lines 28, 39 — the logger-add pattern)
    - .planning/phases/02-exception-visibility-retrofit/02-CONTEXT.md (D-04 — the intentional-CLI-output carve-out)
    - tests/Pellmonweb/test_auth.py (Phase 1 web-side coverage — its outcomes must not change)
  </read_first>
  <action>
    Re-grep `print(` in both files before editing.

    `src/Pellmonweb/pellmonweb.py` — this file has NO module logger. Add `from logging import getLogger` to its import block and `logger = getLogger('pellMon')` at module scope (D-01: same logger NAME as the daemon; the two processes produce two separate streams sharing one name — this is intentional, not a shared handler). Then:
      - approx. line 132, inside `Dbus_handler.start()`: `print('server not running')` -> `logger.info('D-Bus server not running')`
      - approx. line 138, same method: `print('server is running')` -> `logger.info('D-Bus server is running')`
      - approx. line 556: `print(parameterlist)` -> `logger.debug('parameterlist: %s', parameterlist)` — this is a debug leftover that currently dumps the full parameter list to stdout on every page view.
    Do not convert or touch any existing `cherrypy.log(...)` call in this file; only `print()` calls are in scope.

    `src/Pellmonweb/pellmonconf.py` — this file has NO module logger. Add the same two lines. Then:
      - approx. line 125: `print(config_file)` -> `logger.debug('config file: %s', config_file)`
      - approx. lines 154, 155, 156: `print('Open http://<ip>:%u ...')`, `print('Run as root to be able to save changes')`, `print('Quit with CTRL-C')` — LEAVE AS `print()`, UNCHANGED. These are the startup banner of a manually-invoked standalone tool and fall under D-04's intentional-CLI-output carve-out. Converting them to `logger.info` would silently suppress the banner whenever no handler is attached, degrading UX for zero observability gain. Add a short `#` comment immediately above line 154 recording that these three prints are intentional CLI output, deliberately excluded from the OBS-03 sweep — this prevents a future sweep from "finishing the job" incorrectly.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "print(" src/Pellmonweb/pellmonweb.py` returns 0
    - `grep -n "print(" src/Pellmonweb/pellmonconf.py` returns exactly 3 matches, all three being the `Open http://`, `Run as root`, and `Quit with CTRL-C` banner lines
    - A `#` comment above the banner explicitly marks it as intentional CLI output excluded from OBS-03
    - `grep -c "getLogger('pellMon')" src/Pellmonweb/pellmonweb.py` returns 1 and the same for `src/Pellmonweb/pellmonconf.py`
    - `venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_auth.py -v` exits 0 with unchanged outcomes
    - `git diff src/Pellmonweb/` shows no `cherrypy.log` call was modified
    - `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` exits 0
  </acceptance_criteria>
  <done>pellmonweb.py is print-free with a shared-name logger; pellmonconf.py retains exactly its three banner prints, annotated as intentional.</done>
</task>

</tasks>

<threat_model>
N/A — observability-only change, no logic altered, verified via before/after outcome parity (plan 06).

One ASVS V7 control applied during execution: before committing each converted site, confirm the new log message does not interpolate a credential or key. Specific watch-list for this plan — `nbecom`/`nbeprotocol` handle XTEA session keys and RSA material, and `pellmonweb.py` handles session state. Where a converted print rendered a raw object (`print(repr(e))`, `print(parameterlist)`), keep the interpolation to named, non-secret values, and prefer `logger.exception(<fixed message>)` (which attaches the traceback) over manually interpolating exception text that could echo server-supplied credential fragments.
</threat_model>

<verification>
1. `grep -rn "print(" src/Pellmonsrv src/Pellmonweb --include="*.py" | grep -v "\.py2bak"` — after plans 02, 03 and this plan all land, output must be exactly the 3 `pellmonconf.py` banner lines. (Plan 06 turns this into an enforced pytest gate.)
2. `venv-py3/Scripts/python.exe -m pytest tests/ -q -m "not known_broken"` — exits 0.
3. `git diff src/Pellmonsrv/plugins/nbecom/` — no import/control-flow line changed.
</verification>

<success_criteria>
- All 39 in-scope `print()` calls across the eleven files are converted to shared-`pellMon`-logger calls at appropriate levels.
- Five previously logger-less files (`database.py`, `daemon.py`, `frames.py`, `pellmonweb.py`, `pellmonconf.py`) each gain exactly one `getLogger('pellMon')` module logger.
- The three `pellmonconf.py` banner prints survive, annotated.
- Phase 1's suite outcomes are unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-exception-visibility-retrofit/02-05-SUMMARY.md` when done, including the 17-row protocol.py line-to-level table.
</output>
