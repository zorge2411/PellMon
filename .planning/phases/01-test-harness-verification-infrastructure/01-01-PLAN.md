---
phase: 01-test-harness-verification-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements-dev.txt
  - pytest.ini
  - tests/conftest.py
  - tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Developer can run `pytest` from the repo root and pytest resolves `Pellmonsrv`/`Pellmonweb` imports without any sys.path hack in test files"
    - "A test can obtain a live, readable/writable serial-compatible object with no physical device attached"
    - "A test can send UDP bytes through a mock and assert on the payload without any real packet leaving the process"
    - "Real (unmocked) socket creation is blocked by default for the entire suite"
  artifacts:
    - path: "requirements-dev.txt"
      provides: "pytest + pytest-mock + pytest-socket + pytest-cov pins, separate from production requirements.txt"
      contains: "pytest-socket"
    - path: "pytest.ini"
      provides: "pythonpath=src, testpaths=tests, addopts=--disable-socket, registered markers"
      contains: "--disable-socket"
    - path: "tests/conftest.py"
      provides: "loop_serial, mocked_udp_socket, cherrypy_request_ctx fixtures"
      min_lines: 40
    - path: "tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py"
      provides: "TEST-01 demonstration that both transport mocks work"
  key_links:
    - from: "pytest.ini"
      to: "src/"
      via: "pythonpath ini option"
      pattern: "pythonpath\\s*=\\s*src"
    - from: "tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py"
      to: "tests/conftest.py"
      via: "fixture injection of loop_serial / mocked_udp_socket"
      pattern: "loop_serial|mocked_udp_socket"
---

<objective>
Stand up the pytest harness this repo has never had: a dev-only requirements file, a root `pytest.ini`, the `tests/` tree mirroring `src/`, the shared fixtures, and a self-contained smoke test proving the mocked serial and mocked UDP transports work with zero hardware.

Purpose: Every later phase (2-5) of this migration uses this harness as its pass/fail signal. Nothing downstream is trustworthy until this exists.
Output: `requirements-dev.txt`, `pytest.ini`, `tests/` skeleton with `tests/conftest.py`, and `tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py`.

Scope guard: this plan is **additive only**. Do not modify anything under `src/`. Do not delete or rewrite the existing root `test-imports.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-harness-verification-infrastructure/01-CONTEXT.md
@.planning/phases/01-test-harness-verification-infrastructure/01-RESEARCH.md
@.planning/phases/01-test-harness-verification-infrastructure/01-PATTERNS.md

<environment_facts>
Verified by the planner on 2026-09-17 in this checkout — do not re-derive, but DO re-verify in Task 1:

- The correct interpreter for this work is `venv-py3/Scripts/python.exe` (Windows dev venv). It already has `cherrypy 18.10.0` and `pyserial` installed, but **does not have pytest**.
- The bare `C:\Python314\python.exe` on PATH has pytest 9.0.2 / pytest-mock / pytest-socket / pyserial but **does not have cherrypy**, so `import Pellmonweb.auth` fails there. Do not use it.
- `src/Pellmonsrv/directories.py` does NOT exist (Autotools-generated, absent in a fresh checkout). This was RESEARCH.md Open Question 1. Confirmed harmless for this phase: with `sys.path` containing `src`, `venv-py3` imports `Pellmonsrv.database`, `Pellmonsrv.plugins.testplugin`, and `Pellmonweb.auth` cleanly. No `directories.py` stub is needed.
- `src/Pellmonsrv/__init__.py` executes `from .daemon import Daemon` — importing any `Pellmonsrv.plugins.*` module transitively imports `Pellmonsrv/daemon.py` (stdlib-only, safe).
- `src/Pellmonweb/__init__.py` executes `from .auth import AuthController, require, member_of, name_is` — importing `Pellmonweb.auth` requires `cherrypy` to be installed.
- 15 `.pellmon-plugin` descriptors exist in `src/Pellmonsrv/plugins/`.
</environment_facts>

<interfaces>
Fixture contracts this plan creates, consumed by plans 01-02 and 01-03 — implement these names and signatures exactly:

- `loop_serial` -> yields a `serial.Serial` instance from `serial.serial_for_url("loop://", timeout=1)`, closed on teardown.
- `mocked_udp_socket` -> returns the `MagicMock` instance produced by `mocker.patch("socket.socket").return_value`, pre-configured with `recvfrom.return_value = (b"", ("0.0.0.0", 0))`.
- `cherrypy_request_ctx` -> patches `cherrypy.request` (a `MagicMock` with `headers = {"Remote-Addr": "127.0.0.1"}` and `script_name = ""`), `cherrypy.session` (a plain `dict`), and `cherrypy.log`; returns the fake request object.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create requirements-dev.txt and verify the venv-py3 environment</name>
  <files>requirements-dev.txt</files>
  <read_first>
    - requirements.txt (repo root) — header-comment + `package>=X.Y` style to copy
    - requirements-wsl.txt (repo root) — the existing precedent for a second, purpose-scoped requirements file
    - .planning/phases/01-test-harness-verification-infrastructure/01-RESEARCH.md ("Standard Stack" + "Package Legitimacy Audit" sections) — version floors and the slopcheck [OK] verdicts for all four packages
  </read_first>
  <action>
Create `requirements-dev.txt` at the repo root (NOT in `src/`, and do NOT touch `requirements.txt` — per CONTEXT.md D-02 the production Docker image installs only `requirements.txt` and must stay lean).

Content: a leading comment line stating these are test/dev-only dependencies not installed in the production Docker image, then exactly these four pinned entries, matching the `package>=X.Y` style already used in `requirements.txt`/`requirements-wsl.txt`:
`pytest>=9.0,<10`, `pytest-mock>=3.15`, `pytest-socket>=0.8`, `pytest-cov>=7.1`.

Do not add `pyserial` — it is already a production dependency in `requirements.txt` (`pyserial>=3.5`) and its `loop://` URL handler is what the serial fixture uses.

Then install into the Windows dev venv (the only interpreter in this checkout that has both `cherrypy` and `pyserial`):
`venv-py3/Scripts/python.exe -m pip install -r requirements-dev.txt`

Then run the environment spike that RESEARCH.md Open Question 1 asked for, using that same interpreter with `src` on `sys.path`: import `Pellmonsrv.database`, `Pellmonsrv.plugins.testplugin`, and `Pellmonweb.auth`. All three must import cleanly with no generated `directories.py` present. If any of them fails, STOP and report — the rest of this phase's design assumes they succeed.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest --version &amp;&amp; venv-py3/Scripts/python.exe -c "import pytest_socket, pytest_mock, pytest_cov, serial, cherrypy; print('deps ok')" &amp;&amp; venv-py3/Scripts/python.exe -c "import sys; sys.path.insert(0,'src'); import Pellmonsrv.database, Pellmonsrv.plugins.testplugin, Pellmonweb.auth; print('spike ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `requirements-dev.txt` exists at repo root and `grep -v '^#' requirements-dev.txt | grep -c -E 'pytest|pytest-mock|pytest-socket|pytest-cov'` returns 4
    - `requirements.txt` is unchanged: `git diff --name-only requirements.txt` prints nothing
    - `venv-py3/Scripts/python.exe -m pytest --version` prints a version line starting with `pytest 9.`
    - The spike command prints `spike ok` (proves no `directories.py` stub is required)
  </acceptance_criteria>
  <done>`requirements-dev.txt` committed with exactly the four dev packages, installed into `venv-py3`, and all three target modules confirmed importable from that interpreter.</done>
</task>

<task type="auto">
  <name>Task 2: Create pytest.ini and the tests/ tree with shared fixtures</name>
  <files>pytest.ini, tests/conftest.py</files>
  <read_first>
    - test-imports.py (repo root) — lines 1-5 show the `sys.path.insert(0, 'src')` hack that `pytest.ini`'s `pythonpath` option replaces; do NOT replicate this in any new test file
    - src/Pellmonweb/auth.py lines 119-177 — the exact `cherrypy` attributes the auth fixture must patch: `cherrypy.request.headers["Remote-Addr"]` (line 127/131/147/150), `cherrypy.request.script_name` (line 137/156/177), `cherrypy.session` (line 137/165/171), `cherrypy.log` (line 127/131/147/150)
    - .planning/phases/01-test-harness-verification-infrastructure/01-PATTERNS.md — Fixture 1/2/3 bodies and the `pytest.ini` block to use verbatim
  </read_first>
  <action>
Create `pytest.ini` at the repo root with a single `[pytest]` section containing exactly these four settings:
- `pythonpath = src` (pytest >=7 ini option; centralizes path setup so no test file needs `sys.path` manipulation)
- `testpaths = tests`
- `addopts = --disable-socket` (pytest-socket global guardrail; this is what satisfies ROADMAP Phase 1 success criterion 4)
- a `markers =` block registering one marker: `known_broken: documents a confirmed, still-unfixed Python 3 defect; expected to FAIL until the phase named in the test docstring fixes it`

Do NOT put `--cov` into `addopts` (RESEARCH.md Open Question 2: coverage reporting is deferred to the Phase 5 CI work; keep the default run fast and clean).

Create the `tests/` tree mirroring `src/` per CONTEXT.md D-01: `tests/`, `tests/Pellmonsrv/`, `tests/Pellmonsrv/plugins/`, `tests/Pellmonweb/`. Do NOT add `__init__.py` files — pytest's default rootdir-based collection does not need them and adding them would change import-mode semantics.

Create `tests/conftest.py` with the three shared fixtures defined in this plan's `<interfaces>` block: `loop_serial`, `mocked_udp_socket`, `cherrypy_request_ctx`. Use the bodies from 01-PATTERNS.md verbatim.

Each fixture gets a docstring. The `cherrypy_request_ctx` docstring MUST record this codebase-specific gotcha: `AuthController.check_credentials()` (auth.py:149-151) has a bare `except:` whose own recovery code calls `cherrypy.log(...cherrypy.request.headers["Remote-Addr"]...)`, so calling it without this fixture raises a second, uncaught exception instead of returning the error string. Every test that calls an `AuthController` method must request this fixture.

The `mocked_udp_socket` docstring MUST note that `mocker.patch("socket.socket")` is applied after pytest-socket's own `--disable-socket` patch and therefore takes precedence inside the test — this fixture is the only sanctioned way for a test to touch `socket.socket`.

Do not use type hints (this codebase has none anywhere — see CLAUDE.md Conventions). New files under `tests/` do NOT need the GPL header used in `src/` and must NOT get a `.py2bak` sibling.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest --collect-only -q &amp;&amp; venv-py3/Scripts/python.exe -m pytest --fixtures -q 2>&amp;1 | grep -E "loop_serial|mocked_udp_socket|cherrypy_request_ctx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' pytest.ini | grep -c -E 'pythonpath\s*=\s*src'` returns 1
    - `grep -v '^#' pytest.ini | grep -c -- '--disable-socket'` returns 1
    - `grep -v '^#' pytest.ini | grep -c 'known_broken'` returns 1
    - `grep -c 'sys.path' tests/conftest.py` returns 0 (path setup is pytest.ini's job)
    - `venv-py3/Scripts/python.exe -m pytest --fixtures -q` lists all three of `loop_serial`, `mocked_udp_socket`, `cherrypy_request_ctx`
    - Directories `tests/Pellmonsrv/plugins` and `tests/Pellmonweb` exist and contain no `__init__.py`
    - `pytest --collect-only -q` exits 0 (no collection errors)
  </acceptance_criteria>
  <done>Running `pytest` from the repo root collects cleanly with `src` on the path, socket blocking active, and all three fixtures registered and discoverable.</done>
</task>

<task type="auto">
  <name>Task 3: Write the mocked-transport smoke test (TEST-01)</name>
  <files>tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py</files>
  <read_first>
    - tests/conftest.py (created in Task 2) — the fixtures being consumed
    - .planning/phases/01-test-harness-verification-infrastructure/01-RESEARCH.md "Pitfall 2" — why this test must NOT import `Scotteprotocol` or `nbeprotocol`
  </read_first>
  <action>
Create `tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py` with two tests that demonstrate the harness satisfies TEST-01 without any physical hardware:

1. `test_loop_serial_roundtrip(loop_serial)` — write a short framed byte string (e.g. `b"\x02TEST\x03"`), call `flush()`, then `read(7)` and assert the exact same bytes come back. This proves pyserial's in-process `loop://` handler gives a real `Serial`-compatible object for protocol encode/decode work.

2. `test_udp_send_uses_mock_not_real_network(mocked_udp_socket)` — construct `socket.socket(socket.AF_INET, socket.SOCK_DGRAM)`, call `sendto(b"ping", ("127.0.0.1", 1920))`, and assert `mocked_udp_socket.sendto` was called once with exactly those arguments. Also assert `recvfrom()` returns the fixture's configured `(b"", ("0.0.0.0", 0))` default, proving the receive side is stubbable.

CRITICAL scope boundary: this module must NOT import `Scotteprotocol` or `Pellmonsrv.plugins.nbecom.nbeprotocol`. Both packages are import-broken today (IMPORT-01/IMPORT-02, Phase 3) and neither `Scotteprotocol.Protocol` nor `nbeprotocol.Proxy` has a constructor-injectable transport yet — adding that seam is PROTO-04, Phase 4. This test proves the *mechanism* is ready for Phase 4 to plug into.

Add a module-level docstring stating exactly that: these fixtures are the hardware-mock boundary that Phase 4 (PROTO-04) will wire `Protocol`/`Proxy` into, and that this file deliberately stays self-contained until then.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py -v` exits 0 with 2 passed
    - `grep -c -E "Scotteprotocol|nbeprotocol" tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py` returns 0 outside the module docstring (the docstring may name them as Phase 4 future work)
    - The test suite passes with `--disable-socket` active (i.e. via the default `addopts`, no `--enable-socket` override anywhere)
    - `grep -c 'enable_socket' tests/` across the whole tests tree returns 0
  </acceptance_criteria>
  <done>Both transport mocks are demonstrated working under the global socket block, with no dependency on the currently-broken protocol packages.</done>
</task>

</tasks>

<threat_model>
N/A — test infrastructure only. This plan adds no production code paths, no new runtime input surface, and modifies nothing under `src/`. The one operational risk in scope (a stray UDP broadcast from a test run reaching a real burner controller on the developer's LAN) is mitigated by `--disable-socket` in `pytest.ini`.

Package legitimacy: all four new dev dependencies (`pytest`, `pytest-mock`, `pytest-socket`, `pytest-cov`) were run through `slopcheck` during research with an `[OK]` verdict and live PyPI version verification — see 01-RESEARCH.md "Package Legitimacy Audit". No `[ASSUMED]`/`[SUS]` packages, so no legitimacy checkpoint is required.
</threat_model>

<verification>
1. `venv-py3/Scripts/python.exe -m pytest -v` from the repo root — collects and runs, 2 passed, 0 errors.
2. `git status --short src/` — prints nothing (this plan touched nothing under `src/`).
3. `git status --short` shows exactly: `requirements-dev.txt`, `pytest.ini`, `tests/conftest.py`, `tests/Pellmonsrv/plugins/test_mocked_transport_smoke.py`.
</verification>

<success_criteria>
- `pytest` runs from the repo root with no `sys.path` hack in any test file
- `loop://` serial and mocked UDP fixtures both demonstrated green
- Real socket creation blocked suite-wide by default
- `requirements.txt` and everything under `src/` unchanged
</success_criteria>

<output>
Create `.planning/phases/01-test-harness-verification-infrastructure/01-01-SUMMARY.md` when done.
</output>
