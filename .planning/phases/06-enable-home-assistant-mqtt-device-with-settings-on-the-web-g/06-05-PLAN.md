---
phase: 06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g
plan: 05
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/Pellmonsrv/plugins/homeassistant/bridge.py
  - src/Pellmonsrv/plugins/homeassistant/tester.py
  - tests/Pellmonsrv/plugins/test_homeassistant_bridge.py
  - tests/Pellmonsrv/plugins/test_homeassistant_tester.py
  - tests/Pellmonsrv/plugins/test_homeassistant_paho_factory.py
autonomous: true
requirements: [D-04, D-05, D-06, D-08, D-09, D-13, D-14, D-15, D-16, D-17, D-18, D-19]

must_haves:
  truths:
    - "On connect PellMon subscribes to the Home Assistant birth topic and to the explicit per-entity /set topics (always, so that commands arriving while they are off can be logged and dropped per D-06), publishes availability, publishes retained discovery, and publishes all states after a settle delay"
    - "Discovery and availability are retained QoS 1; state messages are QoS 0 and never retained"
    - "Availability is online only while burner_connection is 'connected'; no_connection, demo and a missing burner item all publish offline"
    - "The client registers a retained 'offline' last will, and a clean shutdown publishes retained 'offline' before disconnecting"
    - "Changed values are published once (identical payloads deduplicated) and all values are republished every refresh interval"
    - "A /set command is ignored and logged (no db write) when commands are off, when retained, or when invalid; an accepted command writes through db.set_value, is logged, and the actual value is read back and republished even when the write fails"
    - "Saving settings reconnects live, and the connection test uses its own random client id, never publishes, reports a fixed-set reason, and always finishes within 8 s in total including the 5 s connect timeout (below the 10 s web poll cap)"
  artifacts:
    - path: "src/Pellmonsrv/plugins/homeassistant/bridge.py"
      provides: "Bridge, default_paho_factory, paho_available, command rate-limit constants (re-exports map_reason, REASONS, ConnectionTester from tester.py)"
      contains: "class Bridge"
    - path: "src/Pellmonsrv/plugins/homeassistant/tester.py"
      provides: "REASONS, map_reason, TEST_TOTAL_SECONDS, ConnectionTester (own module per checker warning 6)"
      contains: "class ConnectionTester"
    - path: "tests/Pellmonsrv/plugins/test_homeassistant_tester.py"
      provides: "ConnectionTester isolation, result and total-timeout tests"
    - path: "tests/Pellmonsrv/plugins/test_homeassistant_bridge.py"
      provides: "FakeMqttClient-driven tests for D-04..D-19 behaviour"
    - path: "tests/Pellmonsrv/plugins/test_homeassistant_paho_factory.py"
      provides: "Real paho 2.1 client built via the default factory under --disable-socket (API drift guard)"
  key_links:
    - from: "bridge.Bridge"
      to: "entities.discovery_messages / settings.effective"
      via: "module imports from the same package"
      pattern: "discovery_messages"
    - from: "paho callbacks _on_connect/_on_disconnect/_on_message"
      to: "worker queue"
      via: "queue.put_nowait only; no I/O on the paho network thread"
      pattern: "put_nowait"
    - from: "command handler"
      to: "db.set_value then db.get_text readback"
      via: "worker thread"
      pattern: "get_text"
---

<objective>
Implement the MQTT bridge: one worker thread (plus paho's network thread) that owns connection, availability, discovery, state publishing, Home Assistant commands, live reconfigure, status and the connection test. Everything is driven by an injectable client factory and injectable clocks so every behaviour is tested against the in-memory fakes from 06-01.

Purpose: This is the heart of D-04..D-09 and D-13..D-19. Keeping it a pure, single-threaded state machine behind a queue makes the concurrency risk (RESEARCH "Main risks" 1) testable.
Output: `bridge.py`, `tester.py` (ConnectionTester, REASONS, map_reason in their own module) and three test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-CONTEXT.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-VALIDATION.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-UI-SPEC.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-01-SUMMARY.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-02-SUMMARY.md
@CLAUDE.md

<interfaces>
From 06-02 (src/Pellmonsrv/plugins/homeassistant/):
- entities: ENTITIES, CLEANUP_BUTTONS, present_entities(keys), effective_range(entity, item), state_topic(cfg, e), command_topic(cfg, e), availability_topic(cfg), birth_topic(cfg), entity_by_object_id(obj), discovery_messages(cfg, present, commands_enabled, items, sw_version) -> [(topic, payload_or_empty)], needs_commands(entity)
- settings: DEFAULTS, effective(cfg) (node_id/uid_prefix filled), validate(data, for_test=False) -> (clean, errors)

From 06-01 (tests/Pellmonsrv/plugins/fake_mqtt.py; fixtures fake_factory, fake_db in conftest.py):
- FakeClientFactory(auto_connect=None, connect_error=None): same signature as the real factory; .calls, .clients, .last
- FakeMqttClient: .published [(topic, payload, qos, retain)], .subscriptions, .unsubscriptions, .will, .connect_calls, .loop_started/.loop_stopped, .disconnect_calls; fire_connect(rc), fire_disconnect(rc), fire_message(topic, payload, retain=False); published_to(topic), last_payload(topic)
- FakeDb.make_scotte_db(exclude=()): items incl. burner_connection ('connected'); .writes; .fail[name] = exc; snapshot(); get_text; set_value

Real factory contract: default_paho_factory(cfg, password, client_id, will_topic, on_connect, on_disconnect, on_message).

Bridge public API (consumed by 06-07's plugin; keep these names exactly):
- Bridge(db, client_factory, snapshot=None, clock=time.monotonic, wall_clock=time.time, sw_version='_dev_', settle=2.0, tester=None)
- start() / stop() / shutdown() (idempotent; atexit-safe)
- reconfigure(cfg, password)   (cfg is a validated clean dict; bridge applies settings.effective)
- on_changes(changed_params)    (Database listener; only enqueues)
- handle_all()                  (drain queues synchronously; used by the worker loop and tests)
- tick()                        (1 s periodic work; used by the worker loop and tests)
- status_dict()                 (thread-safe copy, see 06-03 contract)
- start_test(cfg, password) -> bool ; test_result_dict() -> {'state', 'message'}

Module split (checker warning 6): REASONS, map_reason and ConnectionTester live in src/Pellmonsrv/plugins/homeassistant/tester.py (stdlib only; it never imports bridge or paho); bridge.py does "from .tester import REASONS, map_reason, ConnectionTester" so bridge.map_reason etc. remain importable. tester.py is owned by this plan only (wave 2; no other plan touches it; 06-07 lists it in the plugin Makefile.am).

Timeout chain (checker warning 2, shared with 06-06 and 06-08): paho connect_timeout 5.0 s (set in default_paho_factory) -> ConnectionTester TEST_TOTAL_SECONDS = 8.0 total from start to result, including connect() -> web poll cap 10 s (06-06) -> browser abort 12 s (06-08).

Fixed reason set (06-UI-SPEC; REASONS constant): 'connection refused', 'host not found', 'timed out', 'wrong username or password', 'not authorised', 'certificate could not be verified', 'TLS handshake failed', 'connection lost', else 'unknown error (see the server log)'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Connection lifecycle, availability, discovery and state publishing</name>
  <files>src/Pellmonsrv/plugins/homeassistant/bridge.py, src/Pellmonsrv/plugins/homeassistant/tester.py, tests/Pellmonsrv/plugins/test_homeassistant_bridge.py, tests/Pellmonsrv/plugins/test_homeassistant_tester.py, tests/Pellmonsrv/plugins/test_homeassistant_paho_factory.py</files>
  <read_first>.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md (Patterns 1, 2, 5; Pitfalls 3, 5, 6, 10, 12; Corrections C5, C6), tests/Pellmonsrv/plugins/fake_mqtt.py, tests/Pellmonsrv/plugins/conftest.py, src/Pellmonsrv/plugins/homeassistant/entities.py, src/Pellmonsrv/plugins/homeassistant/settings.py</read_first>
  <behavior>
    - paho factory test (pytest.importorskip('paho.mqtt.client')): default_paho_factory with tls on/verify on, username set, will_topic 'scotte/status' returns a paho Client built with CallbackAPIVersion.VERSION2 and a client_id; mocker.spy on Client.will_set sees ('scotte/status', 'offline', qos=1, retain=True); tls_set called with cert_reqs ssl.CERT_REQUIRED; with verify off tls_insecure_set(True) is called after tls_set; will_topic None -> will_set not called; loop_start is never called; the built client's connect_timeout equals 5.0
    - reconfigure(enabled cfg) + handle_all(): factory called once with client_id 'pellmon-<node_id>' and will_topic '<prefix>/status'; connect(host, port, 60) then loop_start() called; status state 'connecting'
    - enabled False: no client created, status state 'off', and a previously running client gets retained 'offline', disconnect() and loop_stop()
    - connect() raising ConnectionRefusedError / socket.gaierror / TimeoutError / ssl.SSLCertVerificationError / ssl.SSLError -> status 'disconnected' with reason 'connection refused' / 'host not found' / 'timed out' / 'certificate could not be verified' / 'TLS handshake failed'; retried by tick() after 1, 2, 4 ... capped at 60 s of the injected clock
    - map_reason tests live in test_homeassistant_tester.py (tester.py owns REASONS/map_reason); bridge.map_reason is the same object
    - fire_connect(0) then handle_all(): subscriptions include ('<disc>/status', 1) and one explicit '<prefix>/<obj>/set' per present number/button entity at qos 1 even when allow_commands False (D-06: subscribed so they can be logged and dropped); availability 'online' published retained qos1; every discovery_messages() topic published with qos 1 retain True (sensor configs non-empty; number/button configs '' when commands off, D-18; burner_on/burner_off configs ''); no state yet
    - after the injected clock passes settle (2 s) and tick(): every present sensor and number state published to '<prefix>/<obj>/state' with qos 0 retain False using snapshot() text (power_percent gets the 'power' value)
    - fire_connect(134) -> status 'disconnected' reason 'wrong username or password'; 135 -> 'not authorised'
    - burner_connection 'no_connection' -> tick publishes retained 'offline' once (not repeated every tick), and no state publishes while offline; back to 'connected' -> 'online' then states after settle; 'demo' -> 'offline' with status burner 'demo'; burner_connection item missing -> 'offline', no discovery, status burner 'unsupported'
    - on_changes([{'name': 'boiler_temp', 'value': '57'}]) + handle_all() publishes one state message; the same value again publishes nothing (dedupe); a change of an item not in ENTITIES publishes nothing
    - refresh: after cfg refresh seconds of injected clock, tick() republishes every present state even if unchanged
    - present set: fake_db without chimney_draught -> no chimney_draught config or state published at all; if the db item set changes between ticks, discovery is republished
    - HA birth: fire_message('<disc>/status', 'online') -> availability and all discovery republished, states republished after settle (D-19/C6); payload 'offline' does nothing
    - fire_disconnect(7) -> status 'disconnected' reason 'connection lost'; paho callbacks only enqueue (a test calls the callback and asserts nothing is published until handle_all())
    - shutdown(): publishes retained 'offline' on '<prefix>/status', calls wait_for_publish, disconnect() and loop_stop(); calling it twice is harmless; with no client it does nothing (C5)
    - status_dict(): last_publish is the injected wall_clock time of the last publish, last_connect set on successful connect, entities == number of published non-empty configs, availability and burner fields reflect state; the dict has no password key
    - no publish is attempted while not connected (QoS 1 would queue in paho)
  </behavior>
  <action>
    Tests first (red), then implement. First create src/Pellmonsrv/plugins/homeassistant/tester.py (GPL header, stdlib imports only: logging, secrets, socket, ssl, threading, time; it must not import bridge) holding REASONS and map_reason (moved out of bridge per checker warning 6; ConnectionTester is added to it in Task 3), with map_reason tests in tests/Pellmonsrv/plugins/test_homeassistant_tester.py. Then implement src/Pellmonsrv/plugins/homeassistant/bridge.py with the GPL header, imports: json, logging, queue, socket, ssl, threading, time, and relative imports "from . import entities, settings" and "from .tester import REASONS, map_reason, ConnectionTester" (ConnectionTester import added in Task 3). paho is imported lazily inside default_paho_factory and paho_available() (try import paho.mqtt.client; return hasattr(mqtt, 'CallbackAPIVersion')), so the module imports on machines without paho. default_paho_factory: raise RuntimeError('paho-mqtt >= 2.0 required') without CallbackAPIVersion; mqtt.Client(mqtt.CallbackAPIVersion.VERSION2, client_id=client_id, clean_session=True); username_pw_set(cfg['username'], password or None) when username; tls_set(cert_reqs=ssl.CERT_REQUIRED) or tls_set(cert_reqs=ssl.CERT_NONE) then tls_insecure_set(True) when tls; will_set(will_topic, 'offline', qos=1, retain=True) only when will_topic; reconnect_delay_set(min_delay=1, max_delay=60); max_queued_messages_set(200); set connect_timeout = CONNECT_TIMEOUT (5.0, defined in tester.py and imported) when the Client exposes that attribute (paho 2.x does; the factory test asserts it); assign the three callbacks; never call enable_logger(). map_reason(obj): ints/reason codes by .value (4 or 134 -> 'wrong username or password', 5 or 135 -> 'not authorised', 3 or 136 -> 'connection refused'), exceptions by type (ConnectionRefusedError, socket.gaierror, TimeoutError/socket.timeout, ssl.SSLCertVerificationError before ssl.SSLError), else 'unknown error (see the server log)'. Bridge: a main queue.Queue for control/change/connect events and a separate bounded queue.Queue(maxsize=CMD_QUEUE_MAX) for commands (Task 2); callbacks do put_nowait only (Pattern 2); worker loop in start(): while not stopped: get(timeout=1.0) -> handle, on Empty or after handling call tick(). Connection: in the worker, blocking client.connect(host, port, 60) inside try/except mapped by map_reason; on success loop_start() (paho then reconnects on its own with its back-off); on failure schedule retry via tick with exponential back-off 1..60 s of the injected clock. On connect success (event from on_connect with value 0): subscribe birth topic qos 1, subscribe the explicit command topics of every present number/button entity regardless of allow_commands (D-06: while commands are off, messages on them are logged and dropped by the Task 2 gate; never a wildcard), publish availability, publish discovery_messages (qos 1, retain True), set state_due = clock() + settle. Availability: computed in tick from db['burner_connection'].value (missing -> 'unsupported', D-14 plus RESEARCH Pitfall 12; 'demo' -> offline per Claude's-discretion default, no setting); publish only on change and on (re)connect/birth. States: from snapshot() (fallback: db.get_text for present items when snapshot is None) only while connected and availability online; dedupe by last published payload per topic; refresh republishes all every cfg['refresh'] seconds (D-16). Track published non-empty config topics for the status entity count and for reconfigure cleanup. status fields are guarded by a threading.Lock (Pitfall 10). shutdown() for atexit (C5): if connected publish retained 'offline', wait_for_publish(timeout=2), disconnect(), loop_stop(), stop the worker; swallow and log exceptions at warning. Logging: logger = logging.getLogger('pellMon'), %-formatting, connection state changes at info, failures at warning with the mapped reason only; never log cfg, password or exception text from paho that could include credentials. No type hints; no threading.Timer. Put the factory test in tests/Pellmonsrv/plugins/test_homeassistant_paho_factory.py; everything else in tests/Pellmonsrv/plugins/test_homeassistant_bridge.py using fake_factory, fake_db and a manual clock object (callable with an advance(seconds) helper defined in the test module).
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_homeassistant_bridge.py tests/Pellmonsrv/plugins/test_homeassistant_tester.py tests/Pellmonsrv/plugins/test_homeassistant_paho_factory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import paho\|^from paho" src/Pellmonsrv/plugins/homeassistant/bridge.py` prints 0 (lazy import only)
    - `grep -c "CallbackAPIVersion.VERSION2" src/Pellmonsrv/plugins/homeassistant/bridge.py` >= 1
    - `grep -c "threading.Timer\|enable_logger" src/Pellmonsrv/plugins/homeassistant/bridge.py` prints 0
    - A test asserts every publish to a '/state' topic has qos 0 and retain False, and every '/config' and '/status' publish has qos 1 and retain True
    - Both test files pass
  </acceptance_criteria>
  <done>Connection, availability, discovery and state publishing behave per D-04, D-05, D-14..D-18 and D-19 (C5/C6).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commands from Home Assistant with readback, logging and rate limits</name>
  <files>src/Pellmonsrv/plugins/homeassistant/bridge.py, tests/Pellmonsrv/plugins/test_homeassistant_bridge.py</files>
  <read_first>.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md (Pattern 6, Pitfall 4), .planning/quick/260919-olq-fix-scotte-crlf-retry-duplication-and-se/260919-olq-SUMMARY.md (setItem contract), src/Pellmonsrv/plugins/homeassistant/bridge.py (Task 1 result)</read_first>
  <behavior>
    - after connect, subscriptions contain exactly one '<prefix>/<obj>/set' per present number and button entity (qos 1) for both allow_commands True and False (D-06 revised at plan review: subscribe always, drop while off); no wildcard ('#' or '+' never subscribed); no burner_on/burner_off /set topic
    - commands off: fire_message('scotte/boiler_temp_set/set', '60') + handle_all() -> no db write, no set_value call, no state republish, and one warning log containing the topic and 'commands are disabled'
    - commands off log throttle: a second message on the same topic within 60 s of the injected clock writes no second warning (counted, logged at debug), after 60 s it warns again (log-flood guard; the bounded command queue still applies)
    - commands off then reconfigure to on (reconnect): the same /set message now writes; commands on then off: writes stop immediately after the reconfigure event is handled
    - retained message on a /set topic is ignored and logged, no write (Pitfall 4)
    - payload longer than 32 bytes, non-UTF-8, unknown object id, object id whose item is absent in the db, or burner_on/burner_off -> ignored and logged, no write
    - number: '60' -> db.set_value('boiler_temp_set', '60') and state republished with the readback text; '60.0' accepted as 60; '60.5', 'nan', 'inf', 'abc', '39', '86' rejected and logged, but the current value is still read back and republished (D-08 snap-back)
    - item range intersection: when the db item has min '45', '44' is rejected even though the HA range starts at 40
    - min_power/max_power cross-check: setting min_power above the current max_power (from snapshot) is rejected and logged
    - button: payload 'PRESS' on reset_alarm -> db.set_value('reset_alarm', '0'); any other payload rejected
    - set_value raising ValueError or IOError -> warning log with result 'rejected' / 'failed' and readback still republished
    - every accepted or rejected command produces exactly one log line containing topic, item, value and result (D-09); caplog never contains the configured password sentinel
    - rate limits with the injected clock: same item and value within 2 s dropped (logged as duplicate); same item again within 5 s dropped; more than 10 writes in 60 s dropped; command queue beyond 20 pending drops with a warning
    - on_message runs no db call (callback test: after on_message, db.writes is empty until handle_all())
  </behavior>
  <action>
    Tests first, then extend bridge.py. Constants (discretion, documented in a short comment as tunable, RESEARCH A5): CMD_PAYLOAD_MAX = 32, CMD_DEDUPE_SECONDS = 2, CMD_ITEM_INTERVAL = 5, CMD_GLOBAL_PER_MINUTE = 10, CMD_QUEUE_MAX = 20. on_message: if topic equals the birth topic enqueue a birth event on the main queue; otherwise put_nowait (message.topic, bytes payload, retain) on the command queue, catching queue.Full with a warning. Worker command handling order per RESEARCH Pattern 6: commands disabled gate (D-06: the explicit /set topics are always subscribed, so while allow_commands is off every message is dropped here with logger.warning('Home Assistant command %s ignored: commands are disabled', topic), throttled to one warning per topic per CMD_OFF_LOG_INTERVAL = 60 s of the injected clock, further drops at debug; no db call, no readback) -> retained ignore -> size and UTF-8 decode -> object id parsed from '<prefix>/<obj>/set' and mapped via entities.entity_by_object_id to a number or button entity whose item is in the db (never burner_on/burner_off, D-07) -> buttons need 'PRESS' and write '0' -> numbers: float, finite, integral, inside entities.effective_range(entity, db item) -> min_power <= max_power cross-check using the snapshot value of the other item (discretion, RESEARCH A6) -> rate limit -> db.set_value(item, str(int(value))) catching ValueError and IOError/OSError -> log one line: logger.info('Home Assistant command %s item=%s value=%s result=%s', ...) for accepted, logger.warning with the same shape for rejected/ignored/failed; value is the decoded payload truncated to 32 chars via %r -> readback via db.get_text(item) in try/except and publish it to the state topic bypassing dedupe (D-08), also after failure or rejection of a valid entity. The explicit command topics are subscribed on every connect independent of allow_commands (reconfigure reconnects, Task 3, and resubscribes for the current present set); allow_commands only controls the gate and the D-18 discovery payloads. Never run serial I/O on the paho thread.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_homeassistant_bridge.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CMD_QUEUE_MAX\|CMD_PAYLOAD_MAX\|CMD_DEDUPE_SECONDS\|CMD_ITEM_INTERVAL\|CMD_GLOBAL_PER_MINUTE\|CMD_OFF_LOG_INTERVAL" src/Pellmonsrv/plugins/homeassistant/bridge.py` >= 6
    - A test asserts that with allow_commands False a /set message produces zero db writes and exactly one 'commands are disabled' warning
    - A test asserts no subscription topic contains '#' or '+'
    - All command behaviour tests pass
  </acceptance_criteria>
  <done>Home Assistant commands are gated, validated, rate-limited, logged and always snapped back (D-06..D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live reconfigure, status and the start-and-poll connection test</name>
  <files>src/Pellmonsrv/plugins/homeassistant/bridge.py, src/Pellmonsrv/plugins/homeassistant/tester.py, tests/Pellmonsrv/plugins/test_homeassistant_bridge.py, tests/Pellmonsrv/plugins/test_homeassistant_tester.py</files>
  <read_first>.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md (Pitfall 2, Corrections C7, D-Bus methods table), src/Pellmonsrv/plugins/homeassistant/bridge.py, src/Pellmonsrv/plugins/homeassistant/tester.py</read_first>
  <behavior>
    - reconfigure with a new host while connected: old client disconnect() and loop_stop(), a new client created and connected to the new host, no daemon restart (D-13)
    - reconfigure toggling allow_commands on -> after reconnect the 12 number/button configs are published non-empty and a /set message writes; toggling off -> their configs published '' (D-18), the explicit /set topics are still subscribed after the reconnect, and a /set message is logged and dropped with no write (D-06)
    - reconfigure changing node_id or discovery_prefix while connected: every previously published non-empty config topic that is not in the new set receives an empty retained payload before disconnecting
    - reconfigure changing prefix while connected: retained 'offline' published on the old '<old prefix>/status' before disconnecting
    - start_test(cfg, 'pw') returns True and test_result_dict() is {'state': 'running', ...} until done; a second start_test while running returns False
    - with FakeClientFactory(auto_connect=0) the tester's run ends in state 'ok' with message ''; factory call used client_id starting 'pellmon-test-', will_topic None; the test client never publishes or subscribes and is disconnected and loop-stopped; the live client's published list is unchanged
    - connect error -> state 'error' with the mapped reason; auto_connect=134 -> 'wrong username or password'; no CONNACK within the timeout -> 'timed out'
    - total timeout (checker warning 2): with an injected clock that advances 6 s inside connect() (use a small wrapper factory defined in test_homeassistant_tester.py around FakeClientFactory whose client's connect advances the clock; fake_mqtt.py is owned by 06-01 and is not edited here), the CONNACK wait is given at most 2 s (injected event records the wait timeout; assert timeout <= 8.0 - 6.0); with a clock that advances 9 s inside connect() the result is 'timed out' without waiting; in no test is any wait timeout plus elapsed time above TEST_TOTAL_SECONDS (8.0), which is below the 10 s web cap
    - tester tests live in tests/Pellmonsrv/plugins/test_homeassistant_tester.py and import from Pellmonsrv.plugins.homeassistant.tester; the bridge delegation tests stay in test_homeassistant_bridge.py
    - a blank password passed to start_test by the caller is used as-is (the plugin decides stored vs typed, 06-07)
    - status_dict() is safe to call from another thread while the worker runs (lock) and never contains the password
  </behavior>
  <action>
    Tests first, then implement. reconfigure(cfg, password) enqueues a reconfigure event; the worker applies settings.effective(cfg), computes the new desired config topics, and while still connected with the old cfg publishes empty retained payloads for previously published non-empty config topics missing from the new set, publishes retained 'offline' on the old availability topic when the prefix changes or the new cfg is disabled, then disconnect()+loop_stop() the old client and (if enabled with a host) builds a new client via the factory and connects (Task 1 path). Stored password is held only in a private attribute, never logged or put in status. In src/Pellmonsrv/plugins/homeassistant/tester.py (its own module per checker warning 6; bridge.py imports it) add constants CONNECT_TIMEOUT = 5.0 and TEST_TOTAL_SECONDS = 8.0 (timeout chain: tester total 8 s < web poll cap 10 s in 06-06 < JS abort 12 s in 06-08) and ConnectionTester(client_factory, clock=time.monotonic, total_timeout=TEST_TOTAL_SECONDS, thread_factory=threading.Thread, event_factory=threading.Event): start(cfg, password) returns False if running, else sets state running and starts a daemon thread running run_once(cfg, password); run_once takes deadline = clock() + total_timeout, builds a client via the factory with client_id 'pellmon-test-' + secrets.token_hex(4) and will_topic None (the factory applies connect_timeout = CONNECT_TIMEOUT, bounding the socket connect), calls connect(host, port, 60) (exceptions -> map_reason), then remaining = deadline - clock(); if remaining <= 0 the result is 'timed out' without waiting, else loop_start() and wait on the event set by its own on_connect for at most remaining seconds, then always disconnect() and loop_stop() in a finally block (DNS resolution inside connect() is not bounded by paho; if it overruns, the web cap reports 'timed out' and a new test is refused until this one finishes - acceptable, one test at a time); result state 'ok' or 'error' with the mapped reason (D-13, C7, Pitfall 2). Tests call run_once directly or pass a thread_factory that runs the target synchronously. Bridge.start_test/test_result_dict delegate to its tester (created with the same client factory when tester=None).
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pellmon-test-" src/Pellmonsrv/plugins/homeassistant/tester.py` >= 1 and `grep -c "class ConnectionTester" src/Pellmonsrv/plugins/homeassistant/bridge.py` prints 0 (tester in its own module)
    - `grep -c "TEST_TOTAL_SECONDS = 8.0" src/Pellmonsrv/plugins/homeassistant/tester.py` prints 1 and `grep -c "CONNECT_TIMEOUT = 5.0" src/Pellmonsrv/plugins/homeassistant/tester.py` prints 1
    - A test asserts the test client made zero publish and zero subscribe calls
    - All tests in tests/Pellmonsrv/plugins pass
    - Full suite `./venv-py3/Scripts/python.exe -m pytest tests/ -q` shows only the 3 known baseline failures
  </acceptance_criteria>
  <done>Saving reconnects live and the connection test is safe, isolated and start-and-poll (D-13, D-18, D-19/C7).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MQTT broker -> bridge | Any client that can publish on the broker can send /set messages |
| bridge -> burner (db.set_value) | Validated values become serial writes to burner memory |
| bridge -> broker | Credentials and TLS settings are used to connect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Elevation of privilege | /set handling | mitigate | Commands off by default; explicit per-entity subscriptions (never wildcards) whose messages are logged and dropped by the worker gate while commands are off; whitelist of 10 numbers + 2 buttons; Burner ON/OFF never subscribed or accepted |
| T-06-15b | Denial of service (log flood) | commands-off drops | mitigate | One warning per topic per 60 s while commands are off, bounded command queue of 20 |
| T-06-16 | Tampering | command values | mitigate | Finite integral numbers inside the HA range intersected with the datamap item range; min/max power cross-check |
| T-06-17 | Tampering (replay) | retained /set | mitigate | Retained command messages ignored and logged |
| T-06-18 | Denial of service | command flood / EEPROM wear | mitigate | 32-byte payload cap, bounded queue of 20, 2 s dedupe, 5 s per item, 10 writes per minute |
| T-06-19 | Denial of service | paho network thread | mitigate | Callbacks only enqueue; serial I/O only on the worker |
| T-06-20 | Information disclosure | logs / status | mitigate | Password never logged or in status; reasons are a fixed mapped set; sentinel caplog test |
| T-06-21 | Spoofing | broker identity | mitigate | TLS CERT_REQUIRED by default; insecure only when the user unticks Verify |
| T-06-22 | Denial of service | test-connection client | mitigate | Random client id so the live session is not kicked; one test at a time; 5 s connect timeout and 8 s total deadline |
| T-06-23 | Repudiation | commands | mitigate | One log line per accepted/rejected command with topic, item, value, result (D-09) |
</threat_model>

<verification>
- `./venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins -q` green; full suite at the 3 known baseline failures.
- No test opens a socket (pytest --disable-socket stays on).
- Commit only the listed files; `mqtt-*.json` stays untracked; do not push.
</verification>

<success_criteria>
bridge.py implements the full publish/command/availability/reconfigure/test behaviour and every D-04..D-19 bridge row of 06-VALIDATION.md has a passing test.
</success_criteria>

<output>
Create `.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-05-SUMMARY.md` when done.
</output>
