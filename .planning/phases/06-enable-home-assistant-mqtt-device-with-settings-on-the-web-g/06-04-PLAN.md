---
phase: 06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Pellmonweb/html/homeassistant.html
  - src/Pellmonweb/html/layout.html
  - src/Pellmonweb/media/css/pellmon.css
  - tests/Pellmonweb/test_homeassistant_ui.py
autonomous: true
requirements: [D-06, D-10, D-11, D-12, D-13, D-19]

must_haves:
  truths:
    - "A Home Assistant item appears in the main menu right after Settings and is highlighted on its own page"
    - "The page shows the status line, then one form with the five UI-SPEC panels (plus the two D-19 takeover fields in Device), then Save settings and Test connection"
    - "The rendered page never contains the stored password; the password input has no value attribute, placeholder 'set' when a password is stored and a Remove-stored-password checkbox only then"
    - "Allow commands is unchecked by default and its warning appears when it is on; TLS-off disables Verify and shows the reason"
    - "The page stacks in one column on phones and meets the 44px tap and 16px input-font rules, without breaking any Phase 10 CSS test"
  artifacts:
    - path: "src/Pellmonweb/html/homeassistant.html"
      provides: "Mako page per 06-UI-SPEC"
      contains: "Home Assistant / MQTT"
    - path: "src/Pellmonweb/html/layout.html"
      provides: "Navbar entry"
      contains: "/homeassistant/"
    - path: "src/Pellmonweb/media/css/pellmon.css"
      provides: "Phase 6 .mqtt-* block placed before the Phase 10 header"
      contains: "/* Phase 6: Home Assistant / MQTT */"
    - path: "tests/Pellmonweb/test_homeassistant_ui.py"
      provides: "UI-SPEC structural acceptance checks 1-10 (Windows-safe, Mako render)"
  key_links:
    - from: "src/Pellmonweb/html/homeassistant.html"
      to: "src/Pellmonweb/media/js/homeassistant.js"
      via: "<%! scripts = ['js/homeassistant.js'] %> picked up by layout.scriptIncludes"
      pattern: "js/homeassistant.js"
    - from: "src/Pellmonweb/html/homeassistant.html"
      to: "/homeassistant/save and /homeassistant/test"
      via: "form action and Test button formaction"
      pattern: "homeassistant/test"
---

<objective>
Build the "Home Assistant / MQTT" page markup, its menu entry and its styles exactly per the approved 06-UI-SPEC.md, with structural tests that render the Mako template directly (no controller needed).

Purpose: D-10/D-11/D-12/D-13 user-facing surface; D-06 command switch and warning; D-19 (C2) takeover fields. Rendering from a fixed template context lets this run in parallel with the controller (06-06), which fills the same context.
Output: `homeassistant.html`, a `layout.html` nav item, a Phase 6 CSS block, `tests/Pellmonweb/test_homeassistant_ui.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-UI-SPEC.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-PATTERNS.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-CONTEXT.md
@CLAUDE.md

<interfaces>
Template context contract (06-06's HomeAssistant._render must pass exactly these keys; this plan's tests render with them):
- username (str), webroot (str), active_page = 'homeassistant'
- settings (dict): canonical non-secret keys enabled, host, port, username, tls, tls_verify, prefix, discovery_prefix, device_id, device_name, node_id, uid_prefix, allow_commands, refresh (values as typed by the user on an error re-render, else stored values)
- has_password (bool), password_reentered (bool: validation failed after the user typed a password)
- errors (dict field -> message; empty when none)
- msg (str), msg_level ('success'|'danger'|'warning'|'')
- status (dict): level ('well'|'success'|'warning'|'danger'), glyph ('ok'|'remove'|'warning-sign'|''), word (str, may be ''), text (str, the rest of the sentence), title (str, full date/time or '')
- test_msg (str), test_level ('success'|'danger'|'')
- auth_configured (bool), available (bool: plugin loaded), daemon_down (bool)

Existing patterns: src/Pellmonweb/html/settings.html (layout inherit, title def, panel markup, `${msg | h}` alert, auth-disabled button, `${' checked' if ... else ''}`); src/Pellmonweb/html/layout.html Settings `<li>` at lines ~54-55 and scriptIncludes (template-level `<%! scripts = [...] %>`).

CSS constraints (tests/Pellmonweb/test_mobile_css.py): phase10_block() is everything from "/* Phase 10: mobile / responsive */" to end of file; it enforces a 0/4/8/16/24 px spacing scale and uses dict(media_blocks(block)) so a second "(max-width: 767px)" block after it would shadow the Phase 10 one. Therefore the Phase 6 block MUST be inserted immediately BEFORE the Phase 10 header line (currently line ~101), after all pre-Phase-10 rules. Allowed media queries: "(max-width: 767px)", "(min-width: 768px) and (max-width: 991px)", "(min-width: 768px)". No new stylesheet file; no overflow-x: hidden anywhere.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Template and menu entry with structural tests</name>
  <files>src/Pellmonweb/html/homeassistant.html, src/Pellmonweb/html/layout.html, tests/Pellmonweb/test_homeassistant_ui.py</files>
  <read_first>.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-UI-SPEC.md (whole file), src/Pellmonweb/html/settings.html, src/Pellmonweb/html/layout.html, tests/Pellmonweb/test_settings_page.py (render() helper, navbar-active regex), tests/Pellmonweb/test_mobile_forms.py (existing template-wide form rules the new template must satisfy)</read_first>
  <behavior>
    - UI-SPEC acceptance checks 1-9 as tests: inherit line, h1 "Home Assistant / MQTT", one form method post with action ending /homeassistant/save, the five panel headings Connection, Security, Topics and discovery, Device, Behaviour
    - Inputs named enabled, host, port, username, password, clear_password (only when has_password), tls, tls_verify, prefix, discovery_prefix, device_id, device_name, node_id, uid_prefix, allow_commands, refresh; every input has a matching label for=id
    - Password input: type password, autocomplete new-password, no value= attribute; has_password True -> placeholder "set", help "A password is stored. Leave this field blank to keep it." and clear_password checkbox "Remove the stored password"; has_password False -> empty placeholder, help "No password stored.", no clear_password; password_reentered True -> help "Re-enter the password. It is not kept when the form is redisplayed."
    - Secret render test: render with has_password True and a context that also smuggles the sentinel 's3cret-XYZ' in an unexpected key (e.g. settings['password']) -> output does not contain the sentinel (template never reads a password key)
    - allow_commands False -> no checked attribute on it and #mqtt-commands-warning carries the hidden attribute; True -> checked, #mqtt-commands-warning without hidden, and "Anyone who can reach your Home Assistant or MQTT broker can change burner settings while this is on. Use a broker password and TLS." present
    - tls False -> tls_verify input has disabled and help "Turn on TLS to change this."; #mqtt-verify-warning is hidden unless tls True and tls_verify False; in that case "Certificate verification is off. The connection is encrypted, but PellMon does not check who the broker is." present
    - Buttons "Save settings" (btn btn-primary mqtt-save) and "Test connection" (btn btn-default mqtt-test, formaction ending /homeassistant/test); auth_configured False -> both disabled and "Saving is disabled until web login credentials are configured." present
    - #mqtt-status has role="status" and aria-live="polite", class alert-<level> or well, contains glyphicon-<glyph> when set, word in a 600-weight element and text; appears before the first panel in the output
    - errors {'port': msg}: port form-group has has-error, input aria-invalid="true", span id="port-error" with the message, aria-describedby includes port-error; msg_level 'danger' alert has role="alert" and tabindex="-1"
    - Every col-sm-/col-md- class in the template has a col-xs- sibling; no inline style width >= 361px and no inline height
    - Escaping: a host value '<script>x</script>' renders escaped
    - layout.html: "Home Assistant" link to ${webroot}/homeassistant/ right after the Settings li; active_page 'homeassistant' marks it active; Settings li still present
    - The template declares scripts = ['js/homeassistant.js']
  </behavior>
  <action>
    Tests first in tests/Pellmonweb/test_homeassistant_ui.py (Windows-safe: render with mako TemplateLookup(directories=[HTML_DIR]) and a ctx built from the contract above; never import Pellmonweb.pellmonweb, dbus or gi). Then create src/Pellmonweb/html/homeassistant.html per 06-UI-SPEC exactly (layout, copy, ids, aria, column classes, help texts, placeholders, 16-field keyboard order), declaring `<%! scripts = ['js/homeassistant.js'] %>` and `<%inherit file="layout.html"/>`, title def "Home Assistant / MQTT". Structure: div.container.mqtt-page > h1 > p.mqtt-intro > optional msg alert (`${msg | h}`, role status for success else alert with tabindex -1) > #mqtt-status (well or alert-<level>, glyph span when glyph, `<strong>` word then text, title attribute) > form.mqtt-form method post action "${webroot | h}/homeassistant/save" > five section.panel.panel-default panels > div.mqtt-actions (Save, Test with formaction "${webroot | h}/homeassistant/test", p#mqtt-test-result role status aria-live polite showing test_msg with text-success/text-danger when set) > auth hint. When daemon_down or not available, keep the form visible (status dict carries the warning copy from the controller). Device panel: after Device identifier and Device name add two planner-added D-19/C2 fields, each col-xs-12 col-sm-6, autocomplete off: node_id labelled "Discovery node ID" with help "Only needed to take over an existing device: the node ID used in its discovery topics. Leave empty to use the device identifier." and uid_prefix labelled "Unique ID prefix" with help "Only needed to take over an existing device: the part of each entity's unique ID before the item name. Leave empty to use the device identifier."; keyboard order becomes ... device id, device name, node id, unique id prefix, allow commands, refresh, Save, Test. Checkbox groups in fieldset with legend.sr-only; Verify row inside div.mqtt-indent; the TLS and allow-commands warnings are rendered server-side from settings and carry ids mqtt-verify-warning and mqtt-commands-warning (hidden via the `hidden` attribute when not applicable, so JS in 06-08 can toggle them); prefix help contains `<code class="mqtt-prefix-code">${settings['prefix'] | h}</code>/&lt;item&gt;/state`. Every dynamic value uses `| h`. Password input never gets a value attribute. In layout.html add the `<li class="${'active' if context.get('active_page', '') == 'homeassistant' else ''}"><a href="${webroot}/homeassistant/">Home Assistant</a></li>` immediately after the Settings li, same indentation.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_homeassistant_ui.py tests/Pellmonweb/test_settings_page.py tests/Pellmonweb/test_mobile_forms.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '<input[^>]*type="password"' src/Pellmonweb/html/homeassistant.html | grep -c "value="` prints 0
    - `grep -c "homeassistant/" src/Pellmonweb/html/layout.html` prints 1 and the Settings link line is still present
    - `grep -c "Home Assistant / MQTT" src/Pellmonweb/html/homeassistant.html` >= 1
    - New tests pass; existing settings/mobile-form tests still pass
  </acceptance_criteria>
  <done>The page and menu entry match the UI-SPEC and the password cannot appear in the rendered HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 6 CSS block before the Phase 10 block</name>
  <files>src/Pellmonweb/media/css/pellmon.css, tests/Pellmonweb/test_homeassistant_ui.py</files>
  <read_first>src/Pellmonweb/media/css/pellmon.css, tests/Pellmonweb/test_mobile_css.py, .planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-UI-SPEC.md (Spacing, Typography, Mobile Behaviour, Navigation, acceptance check 10)</read_first>
  <action>
    Insert, immediately before the line "/* Phase 10: mobile / responsive */", a block headed "/* Phase 6: Home Assistant / MQTT */" with only .mqtt-* selectors plus the one tablet navbar rule, exactly per the UI-SPEC: base rules .mqtt-page { padding-bottom: 32px; }, .mqtt-page h1 { margin: 0 0 16px; }, .mqtt-intro { margin: 0 0 16px; }, #mqtt-status { margin-bottom: 24px; }, .mqtt-page .panel-body { padding: 16px; }, .mqtt-page .alert { padding: 16px; }, .mqtt-form .panel { margin-bottom: 16px; }, .mqtt-form .form-group { margin-bottom: 16px; }, .mqtt-actions { margin-top: 24px; }, .mqtt-indent { margin-left: 24px; }, .mqtt-form label { font-weight: 600; }, .mqtt-form .checkbox label { font-weight: 400; }, .mqtt-form .panel-heading { font-size: 18px; font-weight: 600; line-height: 1.2; }, .mqtt-form code { font-size: 14px; word-break: break-all; }, .mqtt-form .help-block { font-size: 14px; }, .mqtt-page h1 { font-weight: 600; } and inline buttons from 768px (.mqtt-actions .btn with 8px right margin); inside @media (max-width: 767px): .mqtt-form .form-control { height: 44px; font-size: 16px; }, .mqtt-form .checkbox label { min-height: 44px; padding: 8px 0 8px 32px; line-height: 28px; display: block; }, the checkbox input at margin-left: -32px with width/height 24px, .mqtt-form .btn { display: block; width: 100%; min-height: 44px; margin: 0 0 8px; }, .mqtt-page h1 { font-size: 24px; line-height: 1.2; }; inside @media (min-width: 768px) and (max-width: 991px): .navbar-nav > li > a { padding-left: 8px; padding-right: 8px; }. No 10px or 15px padding in any .mqtt-* rule, no #337ab7 or any hex color in the block, no overflow-x: hidden, no new media query. Append to tests/Pellmonweb/test_homeassistant_ui.py the UI-SPEC acceptance check 10 as text assertions on the slice between the Phase 6 header and the Phase 10 header, plus an assertion that the Phase 6 header index is lower than the Phase 10 header index.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_homeassistant_ui.py tests/Pellmonweb/test_mobile_css.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase 6: Home Assistant / MQTT\|Phase 10: mobile / responsive" src/Pellmonweb/media/css/pellmon.css` shows the Phase 6 line number lower than the Phase 10 one
    - `grep -c "overflow-x: hidden" src/Pellmonweb/media/css/pellmon.css` prints 0
    - tests/Pellmonweb/test_mobile_css.py passes unchanged (all 7 tests)
    - Full suite `./venv-py3/Scripts/python.exe -m pytest tests/ -q` shows only the 3 known baseline failures
  </acceptance_criteria>
  <done>The page is styled per UI-SPEC and every Phase 8/10 CSS guard still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored settings / form input -> HTML | User-controlled strings are rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Information disclosure | password input | mitigate | No value attribute, template never reads a password key, sentinel render test |
| T-06-13 | Tampering (XSS) | homeassistant.html | mitigate | Every dynamic value `| h`; escape test with a script tag in host |
| T-06-14 | Elevation of privilege | Allow commands switch | mitigate | Off by default (no checked), inline warning when on (D-06) |
</threat_model>

<verification>
- `./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb -q` green; full suite at the 3 known baseline failures.
- Browser layout checks for this page run later from 06-08 (WSL/CI).
- Commit only the listed files; `mqtt-*.json` stays untracked; do not push.
</verification>

<success_criteria>
The page, nav entry and CSS exist per UI-SPEC with the D-19 takeover fields, and all structural and Phase 10 CSS tests pass.
</success_criteria>

<output>
Create `.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-04-SUMMARY.md` when done; note the two planner-added D-19 fields as a deliberate UI-SPEC extension.
</output>
