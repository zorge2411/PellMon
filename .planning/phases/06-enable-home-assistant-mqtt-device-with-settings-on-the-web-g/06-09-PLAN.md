---
phase: 06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g
plan: 09
type: execute
wave: 4
depends_on: ["06-07", "06-08"]
files_modified:
  - DEPLOY-PI.md
  - HARDWARE-BRINGUP.md
  - tests/test_homeassistant_docs.py
  - .planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-VALIDATION.md
autonomous: false
requirements: [D-03, D-07, D-12, D-14, D-15, D-18]

must_haves:
  truths:
    - "A user can follow DEPLOY-PI.md to enable the plugin on an existing install, take over the existing Home Assistant device, and knows the old publisher must be switched off first"
    - "The docs state that Burner ON/OFF are no longer available from Home Assistant, that setpoint numbers and reset buttons disappear while commands are off, and how to remove the stale entities"
    - "The docs state that the MQTT password lives in the settings database on the data volume and in backups, so both must be protected, and recommend a dedicated broker user with an ACL limited to the topic prefix"
    - "Every automated gate is green (full suite at the known baseline; browser suite run by the orchestrator in WSL/CI), and the user has signed off the manual UAT list against a real broker, Home Assistant and burner"
  artifacts:
    - path: "DEPLOY-PI.md"
      provides: "Home Assistant / MQTT section"
      contains: "p15 = HomeAssistant"
    - path: "HARDWARE-BRINGUP.md"
      provides: "Supervised Home Assistant takeover and command checklist"
      contains: "Home Assistant"
    - path: "tests/test_homeassistant_docs.py"
      provides: "Doc guard for the required statements"
    - path: ".planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-VALIDATION.md"
      provides: "nyquist_compliant true, statuses filled"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tests/test_homeassistant_docs.py"
      to: "DEPLOY-PI.md and HARDWARE-BRINGUP.md"
      via: "text assertions"
      pattern: "DEPLOY-PI.md"
---

<objective>
Document the feature for deployment and hardware bring-up, run the final automated gate, and hand over for the blocking human UAT against a real broker, Home Assistant and burner.

Purpose: D-03 and D-07 require documentation (old publisher off, ON/OFF removal); D-12 requires documenting that the data volume and backups hold the password; D-14/D-15/D-18 and the takeover can only be confirmed on real systems (06-VALIDATION "Manual-Only Verifications").
Output: doc sections, a doc guard test, an updated 06-VALIDATION.md, and the user's UAT sign-off. The executor must not push or open a PR; the user reviews via a PR they open, and merging publishes a release (Phase 9 pipeline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-CONTEXT.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-VALIDATION.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-07-SUMMARY.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-08-SUMMARY.md
@CLAUDE.md

<interfaces>
DEPLOY-PI.md sections: 1 hardware, 2 SFTP, 3 Docker, 4 Configure (incl. "Where PellMon keeps its data"), 5 Build and start, 6 Updating/backups/troubleshooting (incl. "Backup and restore"), 7 Caveats. HARDWARE-BRINGUP.md sections: 1 Pre-flight, 2 Access to the burner, 3 Bring-up order, 4 Not verified, 5 Safety. tests/test_ci_docker_config.py already guards some DEPLOY-PI statements (test_deploy_guide_documents_persistent_data) - keep them passing. DOCKER.md is untracked in git; do not edit or stage it (put the Docker networking note into DEPLOY-PI.md instead). CHANGELOG.md is unmaintained since 2017; do not edit it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deployment and bring-up documentation with a doc guard test</name>
  <files>DEPLOY-PI.md, HARDWARE-BRINGUP.md, tests/test_homeassistant_docs.py</files>
  <read_first>DEPLOY-PI.md, HARDWARE-BRINGUP.md, tests/test_ci_docker_config.py (doc test style), .planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md (sections "Takeover mechanics", "Docker / CI impact", Pitfalls 7 and 8, Security Domain)</read_first>
  <behavior>
    - DEPLOY-PI.md contains a "Home Assistant / MQTT" heading, the literal line "p15 = HomeAssistant" with the path config/conf.d/enabled_plugins.conf, the words "switch off the old publisher" (or "Switch off the old publisher"), "Burner ON" and "Burner OFF" with an explanation that they are not published, "Allow commands", "ACL", "pellmon_settings.db" or "settings database" together with "backup", and "IP address" in the broker host advice
    - HARDWARE-BRINGUP.md contains a Home Assistant checklist mentioning tools/burner_sim.py, the last will ("offline"), unplugging the burner serial link, and changing a setpoint from Home Assistant while watching the burner display
    - Neither file contains a real device identifier: a test asserts no 32-hex-character run appears in either file
  </behavior>
  <action>
    Test first (tests/test_homeassistant_docs.py, plain text assertions). In DEPLOY-PI.md add a section "Home Assistant / MQTT" (inside 4 Configure or as its own numbered subsection, keep numbering consistent) covering: enabling the plugin on existing installs by adding "p15 = HomeAssistant" to config/conf.d/enabled_plugins.conf and restarting (new installs get it from the shipped conf.d); opening the Home Assistant page from the menu; broker host as an IP address or DNS name because mDNS names like homeassistant.local usually do not resolve inside the container, with an extra_hosts hint when the broker runs on the Docker host; TLS with certificate verification and the image's CA store; taking over the existing device (D-03): switch off the old publisher first, read the device identifier, the discovery node id (the segment between the component and object id in its discovery topics) and the unique-id prefix (the part before the object id) from Home Assistant (Settings > Devices > the device > MQTT INFO, or the diagnostics download) and enter them on the page, optionally clear the old publisher's retained topics; D-07: Burner ON and Burner OFF are no longer available from Home Assistant, PellMon clears their discovery entries, remove the leftover entities in Home Assistant if they remain; D-18: while "Allow commands from Home Assistant" is off the ten setpoint numbers and the two reset buttons are removed from Home Assistant and come back when it is switched on; availability (D-14/D-15): entities show unavailable when the burner is not connected, in demo mode, or when PellMon stops; security (D-12): the password is stored in the settings database on the data volume and is included in backups made with tools/pellmon_backup.py, so protect the data folder and backup archives; recommend a dedicated broker user whose ACL allows only the topic prefix (default scotte/#) and the discovery prefix (homeassistant/#). In "Backup and restore" add one sentence that the archive now also contains the MQTT password. In HARDWARE-BRINGUP.md add a supervised "Home Assistant" checklist: dry run with tools/burner_sim.py and a local mosquitto first; stop the old publisher; enable and save; confirm entities and history continue; unplug the burner serial link and confirm entities go unavailable and return; kill the pellmonsrv container and confirm the last will marks the device offline; enable commands and change one setpoint from Home Assistant while watching the burner display, confirm the snap-back on an out-of-range value; add the untested items to "4. Not verified" until done. Use no identifiers from the untracked export; use placeholders like <device identifier>.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/test_homeassistant_docs.py tests/test_ci_docker_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "p15 = HomeAssistant" DEPLOY-PI.md` >= 1
    - `grep -ci "old publisher" DEPLOY-PI.md` >= 1 and `grep -c "Burner ON" DEPLOY-PI.md` >= 1
    - `grep -c "burner_sim.py" HARDWARE-BRINGUP.md` >= 1
    - `grep -cE "[0-9a-f]{32}" DEPLOY-PI.md HARDWARE-BRINGUP.md` prints 0 for both files
    - Doc tests and existing CI/doc config tests pass
  </acceptance_criteria>
  <done>Deployment, takeover, behaviour changes and security handling are documented and guarded by a test.</done>
</task>

<task type="auto">
  <name>Task 2: Final automated gate and validation record</name>
  <files>.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-VALIDATION.md</files>
  <read_first>.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-VALIDATION.md, all 06-0*-SUMMARY.md files of this phase</read_first>
  <action>
    Run the full Windows suite: PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v. The only allowed failures are the 3 known baseline ones (test_backup_script::test_conf_d_overrides_pellmon_conf, test_plugin_loader::test_every_available_plugin_loads[consumption], [silolevel]); anything else must be fixed in the owning file before continuing (record the fix in the SUMMARY). Run the quick MQTT subset and confirm it is under ~10 s. Confirm no test file under tests/ uses enable_socket and pytest.ini still has --disable-socket. Confirm with git that mqtt-*.json and DOCKER.md and config/conf.d/ are still untracked and unstaged. Then update 06-VALIDATION.md: frontmatter status to executed, nyquist_compliant: true, wave_0_complete: true; fill the Status column with green (or the test file that proves it) for every automated row, mark the browser row as "pending orchestrator WSL/CI run" until the orchestrator reports it, tick the sign-off checklist items that are now true. Do not push, do not open a PR.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Full suite output shows exactly the 3 known baseline failures and no errors
    - `grep -c "nyquist_compliant: true" .planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-VALIDATION.md` prints 1
    - `grep -rln "enable_socket" tests/ --include=*.py` prints nothing, and `grep -c "disable-socket" pytest.ini` prints 1
    - `git status --porcelain` lists mqtt-*.json only with the `??` prefix
  </acceptance_criteria>
  <done>The automated gate is green and the validation record reflects the phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User acceptance on a real broker, Home Assistant and burner</name>
  <files>(none - verification only)</files>
  <read_first>.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-VALIDATION.md (Manual-Only Verifications), HARDWARE-BRINGUP.md (Home Assistant checklist)</read_first>
  <action>
    Stop and hand over. Present the steps below and wait. Before the user starts, the orchestrator runs the browser suite in WSL (`PELLMON_BROWSER_TESTS=1 PYTHONPATH=src pytest tests/browser -v -rs --allow-unix-socket`) and reports the result; the executor cannot run wsl.exe. The executor must not push or open a PR; the user opens the PR when satisfied, and merging publishes a release.
  </action>
  <what-built>A HomeAssistant pellmonsrv plugin (paho-mqtt 2.1.0) publishing the burner as the existing Home Assistant device with retained discovery, last will, availability tied to the burner connection, publish-on-change plus refresh, gated commands with readback, and a login-protected /homeassistant/ settings page with status and Test connection.</what-built>
  <how-to-verify>
    1. Browser suite result from the orchestrator is green (or failures are listed and accepted).
    2. On a phone and a desktop open /homeassistant/ from the menu: the page is readable, no sideways scroll, the password is never shown, Test connection and Save messages are clear.
    3. Switch off the old publisher. Enter broker, the device identifier, device name, node id and unique-id prefix of the existing device; save. In Home Assistant the same device shows current values, with history continuing (D-03, D-01, D-02).
    4. Burner ON and Burner OFF are gone or unavailable (D-07). With "Allow commands" off the numbers and reset buttons are removed; switch it on and they return. Note whether their history survived the removal (D-18, research assumption A4). (Removal while commands are off is the locked D-18 decision; this step only records how Home Assistant handles it.)
    5. With commands on, change one setpoint from Home Assistant while watching the burner display; the burner changes and Home Assistant shows the read-back value. Try an out-of-range value: Home Assistant snaps back and the PellMon log has a rejected line (D-08, D-09).
    6. Unplug the burner serial link: entities go unavailable and return when reconnected (D-14). Stop the pellmonsrv container: the device goes offline; kill it hard: the last will marks it offline (D-15).
    7. Restart Home Assistant: values reappear within a few seconds without waiting for the refresh interval (D-19 birth message).
  </how-to-verify>
  <verify>
    <human-check>User confirms steps 1-7 or lists what failed</human-check>
  </verify>
  <acceptance_criteria>
    - User replied "approved", or listed failures that are recorded in the SUMMARY for gap closure.
  </acceptance_criteria>
  <done>The feature is accepted on real systems or the gaps are recorded.</done>
  <resume-signal>Type "approved" or describe the issues (step number and what happened)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs -> operator | Operators follow the docs to configure broker access and protect secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-37 | Information disclosure | data volume and backups | mitigate | DEPLOY-PI documents that the settings DB and backup archives contain the MQTT password and must be protected |
| T-06-38 | Elevation of privilege | broker access | mitigate | Docs recommend a dedicated broker user with an ACL limited to the topic and discovery prefixes |
| T-06-39 | Information disclosure | private HA export | mitigate | No identifiers in docs (32-hex guard test); mqtt-*.json verified untracked before handover |
| T-06-40 | Tampering | release pipeline | mitigate | No push or PR by the executor; the user reviews via PR before merge publishes a release |
</threat_model>

<verification>
- Doc tests and full suite at the 3 known baseline failures; browser suite green in WSL/CI (orchestrator); user UAT approved.
- Commit only the listed files; `mqtt-*.json`, `DOCKER.md` and `config/conf.d/` untouched; do not push.
</verification>

<success_criteria>
Docs cover takeover, behaviour changes and secret handling; all automated gates pass; the user approves the real-system UAT.
</success_criteria>

<output>
Create `.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-09-SUMMARY.md` when done, including the UAT outcome and what Home Assistant did with history on D-18 removal.
</output>
