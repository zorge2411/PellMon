---
phase: 06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g
plan: 07
type: execute
wave: 3
depends_on: ["06-03", "06-05"]
files_modified:
  - src/Pellmonsrv/plugins/homeassistant/__init__.py
  - src/Pellmonsrv/plugins/homeassistant/Makefile.am
  - src/Pellmonsrv/plugins/homeassistant.pellmon-plugin
  - src/Pellmonsrv/plugins/Makefile.am
  - configure.ac
  - src/conf.d/enabled_plugins.conf
  - tests/Pellmonsrv/plugins/test_homeassistant_plugin.py
autonomous: true
requirements: [D-03, D-06, D-12, D-13, D-15, D-16]

must_haves:
  truths:
    - "pellmonsrv discovers and activates a HomeAssistant plugin that stays inert (no broker connection) until enabled on the web page"
    - "The plugin registers for Database change events, starts one worker thread, and publishes 'offline' on clean process exit via atexit"
    - "Saving settings through D-Bus validates, stores (password only when typed, cleared only when asked) and reconnects live without a restart"
    - "The plugin implements the five-method contract the D-Bus service calls, and never returns or logs the password"
    - "A broken MQTT setup or a missing paho install cannot abort daemon startup; the page then reports the plugin as unavailable"
    - "If the bridge could not be built (self.bridge is None) every contract method still answers with an available:false shape instead of raising AttributeError"
    - "apply_settings and start_test validate with current = the stored settings, so a tls_verify the browser did not submit keeps the stored value (default True)"
  artifacts:
    - path: "src/Pellmonsrv/plugins/homeassistant/__init__.py"
      provides: "class homeassistant(protocols) with activate/deactivate and get_settings_dict/apply_settings/status_dict/start_test/test_result_dict"
      contains: "class homeassistant(protocols)"
    - path: "src/Pellmonsrv/plugins/homeassistant.pellmon-plugin"
      provides: "yapsy descriptor Name = HomeAssistant, Module = homeassistant"
      contains: "Name = HomeAssistant"
    - path: "src/conf.d/enabled_plugins.conf"
      provides: "p15 = HomeAssistant enabled by default"
      contains: "p15 = HomeAssistant"
    - path: "tests/Pellmonsrv/plugins/test_homeassistant_plugin.py"
      provides: "Plugin activation/contract tests with FakeClientFactory and FakeDb"
  key_links:
    - from: "homeassistant.activate"
      to: "db.add_change_listener(bridge.on_changes)"
      via: "getattr(db, 'add_change_listener', None) (06-03 hook)"
      pattern: "add_change_listener"
    - from: "homeassistant.activate"
      to: "atexit.register(bridge.shutdown)"
      via: "graceful offline publish (C5)"
      pattern: "atexit.register"
    - from: "MyDBUSService._ha_plugin (06-03)"
      to: "homeassistant plugin object"
      via: "descriptor Name 'HomeAssistant'"
      pattern: "HomeAssistant"
---

<objective>
Turn the pure modules and the bridge into a real pellmonsrv plugin: the protocols subclass, its yapsy descriptor, the autotools lists, and default enablement in the shipped conf.d.

Purpose: Connects 06-03's D-Bus contract and Database hook to 06-05's bridge, completing the daemon side of D-13 (live reconnect), D-12 (password write-only storage), D-15 (atexit offline, C5) and D-16 (change listener).
Output: plugin `__init__.py`, `Makefile.am`, descriptor, autotools list edits, `enabled_plugins.conf` line, plugin tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-PATTERNS.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-03-SUMMARY.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Plugin-object contract required by 06-03 (MyDBUSService calls these on plugin.plugin_object where plugin.name == 'HomeAssistant'):
- get_settings_dict() -> dict (non-secret keys + has_password + available; never 'password')
- apply_settings(data) -> {'ok': bool, 'errors': {field: msg}}  (data may include 'password' str and 'clear_password' bool)
- status_dict() -> dict (see 06-03)
- start_test(data) -> bool
- test_result_dict() -> {'state', 'message'}

From 06-05 bridge.py: Bridge(db, client_factory, snapshot=None, clock=..., wall_clock=..., sw_version='_dev_', settle=2.0, tester=None); start(); stop(); shutdown(); reconfigure(cfg, password); on_changes(changed_params); handle_all(); tick(); status_dict(); start_test(cfg, password); test_result_dict(); default_paho_factory; paho_available().
From 06-02 settings.py: DEFAULTS, TLS_VERIFY_MARKER, validate(data, for_test=False, current=None), validate_password(pw), load(store), load_password(store), has_password(store), save(store, clean, password=None, clear_password=False), public_view(cfg, store), effective(cfg).
From 06-03 pellmonsrv.Database: add_change_listener(cb), snapshot().

Existing: src/Pellmonsrv/plugin_categories.py protocols (activate(self, conf, glob, db, *args, **kwargs) must be called first); src/Pellmonsrv/plugins/scottecom/scottecom.py (lowercase class, try/except logger.exception around setup); src/Pellmonsrv/database.py Keyval_storage.keyval_storage (module-level instance, may be None in tests); scottecom.pellmon-plugin descriptor format; plugins/Makefile.am SUBDIRS and nobase_dist_plugins_DATA lists; configure.ac AC_CONFIG_FILES plugin Makefile list (lines ~43-58); src/conf.d/enabled_plugins.conf pNN = Name blocks with a comment line above each.
tests/test_plugin_imports.py (descriptor-driven import check, >= 15 plugins) and tests/test_plugin_loader.py (real yapsy loader) will pick the new descriptor up automatically.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: homeassistant plugin class implementing the D-Bus contract</name>
  <files>src/Pellmonsrv/plugins/homeassistant/__init__.py, tests/Pellmonsrv/plugins/test_homeassistant_plugin.py</files>
  <read_first>src/Pellmonsrv/plugins/scottecom/scottecom.py (lines ~19-118), src/Pellmonsrv/plugin_categories.py, src/Pellmonsrv/plugins/homeassistant/bridge.py, src/Pellmonsrv/plugins/homeassistant/settings.py, tests/Pellmonsrv/plugins/fake_mqtt.py, tests/Pellmonsrv/plugins/conftest.py</read_first>
  <behavior>
    - With Keyval_storage.keyval_storage set to a tmp_path Keyval_storage, plugin.client_factory = FakeClientFactory(), plugin.autostart = False, and monkeypatched atexit.register recording callables: activate({}, {'__version__': '9.9'}, fake_db) registers bridge.on_changes via fake_db.add_change_listener, records bridge.shutdown with atexit, creates no client (defaults are disabled)
    - get_settings_dict() equals DEFAULTS plus has_password False and available True (available reflects bridge.paho_available() or an injected client_factory); never a 'password' key
    - apply_settings({'enabled': True, 'host': 'broker.lan', 'device_id': 'dev_1', 'device_name': 'Burner', 'password': 's3cret-XYZ'}) -> {'ok': True, 'errors': {}}; stored config loads back; has_password True; after bridge.handle_all() the fake factory was called with password 's3cret-XYZ' and host broker.lan (live reconnect, D-13)
    - apply_settings with password '' keeps the stored password; with clear_password True removes it
    - apply_settings with invalid port -> ok False with errors['port']; nothing stored, no reconfigure
    - tls_verify (blocker fix): after storing tls True/tls_verify True, apply_settings with tls False and neither tls_verify nor tls_verify_field -> stored tls_verify stays True; with stored tls_verify False and a TLS-off save -> stays False; apply_settings with tls True and tls_verify_field False and no tls_verify (TLS ticked on a TLS-off page) -> stored/default True kept; with tls True, tls_verify_field True and tls_verify False -> False stored; start_test candidates follow the same rule (the fake factory's cfg shows tls_verify True in the TLS-enabled-from-off case)
    - start_test({'host': 'broker.lan', 'port': 1883}) with blank password uses the stored password; with a typed password uses the typed one; invalid host -> False
    - status_dict() and test_result_dict() delegate to the bridge; neither contains the password
    - activate with Keyval_storage.keyval_storage None: no exception, settings are DEFAULTS, apply_settings returns ok False with errors {'_': 'settings store unavailable'}
    - activate when the bridge constructor raises: logged with logger.exception, no exception escapes, get_settings_dict()['available'] is False
    - bridge None guards (checker warning 3), with the bridge constructor monkeypatched to raise: status_dict() == {'available': False, 'state': 'off'}; start_test({...valid...}) returns False and makes no factory call; test_result_dict() == {'state': 'error', 'message': 'unknown error (see the server log)', 'available': False}; apply_settings(valid data) still validates and stores the settings (so they apply after the next restart) and returns {'ok': True, 'errors': {}, 'available': False} without calling reconfigure; apply_settings(invalid) returns ok False with errors and available False; deactivate() does not raise; none of them raises AttributeError
    - an unhandled exception inside a delegated bridge call (e.g. bridge.status_dict raising) is caught and logged with logger.exception, returning the same available:false shape
    - caplog across all of the above never contains 's3cret-XYZ'
    - deactivate() calls bridge.shutdown()
  </behavior>
  <action>
    Tests first. Create src/Pellmonsrv/plugins/homeassistant/__init__.py with the GPL header, imports: atexit, logging, from Pellmonsrv.plugin_categories import protocols, from Pellmonsrv.database import Keyval_storage, and relative "from . import settings as ha_settings" and "from . import bridge as ha_bridge" (bridge imports paho lazily, so this module imports without paho; keep it that way for tests/test_plugin_imports.py and for pellmonweb, which imports the settings submodule and therefore executes this __init__). class homeassistant(protocols): class attributes client_factory = None (None means ha_bridge.default_paho_factory) and autostart = True (tests set False). __init__ calls protocols.__init__. activate(self, conf, glob, db, *args, **kwargs): protocols.activate(...) first (plugin ordering constraint), self.logger = logging.getLogger('pellMon'), then everything else in try/except Exception logging self.logger.exception('Home Assistant plugin setup failed') (never re-raise; a bad MQTT setup must not abort startup): self.store = Keyval_storage.keyval_storage; self.available = self.client_factory is not None or ha_bridge.paho_available(); build self.bridge = ha_bridge.Bridge(db, self.client_factory or ha_bridge.default_paho_factory, snapshot=getattr(db, 'snapshot', None), sw_version=glob.get('__version__', '_dev_')); register listener via getattr(db, 'add_change_listener', None) when present; atexit.register(self.bridge.shutdown) (C5: Database.terminate is never called on SIGTERM); if autostart: bridge.start(); then load stored settings and, if enabled and available, bridge.reconfigure(clean, load_password(store)). Missing paho with enabled settings -> logger.error('Home Assistant MQTT needs paho-mqtt 2.x; the plugin stays inactive') once, no crash. Initialise self.bridge = None and self.available = False before the try block so every method can test for it. Contract methods as listed in the behaviour; apply_settings pops 'password' and 'clear_password' from a copy before validate, calls ha_settings.validate(data, current=ha_settings.load(self.store)) (blocker fix: tls_verify is only taken from data when TLS is on and TLS_VERIFY_MARKER is truthy, otherwise the stored value is kept; the marker itself is never stored), validates the password with validate_password, saves via ha_settings.save, then reconfigure with the effective password when the bridge exists; errors never contain the password. start_test validates the candidate the same way (for_test=True, current=stored). Bridge None guards (checker warning 3): status_dict() returns {'available': False, 'state': 'off'}; start_test() returns False; test_result_dict() returns {'state': 'error', 'message': 'unknown error (see the server log)', 'available': False}; apply_settings() stores valid settings and adds 'available': False without reconfiguring; each delegated call is wrapped in try/except Exception with self.logger.exception and the same fallback shapes (these match what 06-03's D-Bus layer and 06-06's status_view already handle for "plugin not active"). deactivate(): bridge.shutdown() if present. Never log settings dicts or the password; %-formatting; no type hints.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_homeassistant_plugin.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class homeassistant(protocols)" src/Pellmonsrv/plugins/homeassistant/__init__.py` prints 1
    - `grep -c "import paho\|from paho" src/Pellmonsrv/plugins/homeassistant/__init__.py` prints 0
    - `grep -c "atexit.register" src/Pellmonsrv/plugins/homeassistant/__init__.py` prints 1
    - Test file passes
  </acceptance_criteria>
  <done>The plugin fulfils the 06-03 contract and drives the bridge live from saved settings.</done>
</task>

<task type="auto">
  <name>Task 2: Descriptor, autotools lists and default enablement</name>
  <files>src/Pellmonsrv/plugins/homeassistant.pellmon-plugin, src/Pellmonsrv/plugins/homeassistant/Makefile.am, src/Pellmonsrv/plugins/Makefile.am, configure.ac, src/conf.d/enabled_plugins.conf</files>
  <read_first>src/Pellmonsrv/plugins/scottecom.pellmon-plugin, src/Pellmonsrv/plugins/scottecom/Makefile.am, src/Pellmonsrv/plugins/Makefile.am, configure.ac (plugin Makefile list), src/conf.d/enabled_plugins.conf, tests/test_plugin_imports.py, tests/test_plugin_loader.py</read_first>
  <action>
    Create src/Pellmonsrv/plugins/homeassistant.pellmon-plugin mirroring scottecom's: [Core] Name = HomeAssistant, Module = homeassistant; [Documentation] Author = PellMon, Version = 0.1, Website = http://github.com/motoz/PellMon, Description = Publishes the burner to Home Assistant as an MQTT device. Create src/Pellmonsrv/plugins/homeassistant/Makefile.am copying scottecom's shape: homeassistant_PYTHON listing entities.py, settings.py, bridge.py, tester.py, __init__.py and homeassistantdir = $(pythondir)/Pellmonsrv/plugins/homeassistant. In src/Pellmonsrv/plugins/Makefile.am append homeassistant to SUBDIRS and homeassistant.pellmon-plugin to nobase_dist_plugins_DATA (keep the trailing-backslash continuation style correct). In configure.ac add src/Pellmonsrv/plugins/homeassistant/Makefile to the AC_CONFIG_FILES plugin list next to the exec entry. In src/conf.d/enabled_plugins.conf add after the p14 Exec block: comment "# The HomeAssistant plugin publishes the burner to Home Assistant over MQTT. It stays inactive until it is enabled on the Home Assistant page of the web interface" and the uncommented line "p15 = HomeAssistant" (RESEARCH Open Question 2: shipped enabled, inert until the GUI switch). Do NOT edit the untracked user copy config/conf.d/enabled_plugins.conf (06-09 documents adding the line there). Run the descriptor-driven tests.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/test_plugin_imports.py tests/test_plugin_loader.py tests/Pellmonsrv -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^p15 = HomeAssistant$" src/conf.d/enabled_plugins.conf` prints 1
    - `grep -c "homeassistant" src/Pellmonsrv/plugins/Makefile.am` prints 2
    - `grep -c "src/Pellmonsrv/plugins/homeassistant/Makefile" configure.ac` prints 1
    - `git status --porcelain config/conf.d` shows no modification made by this plan
    - tests/test_plugin_imports.py and tests/test_plugin_loader.py pass for homeassistant (only the 2 known loader baseline failures remain: consumption, silolevel)
    - Full suite `./venv-py3/Scripts/python.exe -m pytest tests/ -q` shows only the 3 known baseline failures
  </acceptance_criteria>
  <done>The plugin is discoverable by yapsy, installable by autotools and enabled in the shipped configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D-Bus -> plugin contract | Settings JSON from the web process |
| plugin -> settings DB | Password persisted to the 0600 Keyval store on the data volume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-30 | Information disclosure | get_settings_dict / logs | mitigate | public_view only; password popped before validation; no settings/password logging; sentinel caplog test |
| T-06-31 | Denial of service | daemon startup | mitigate | All setup in try/except with logger.exception; missing paho leaves the plugin inactive; bridge-None guards so D-Bus calls never raise |
| T-06-31b | Spoofing (TLS downgrade) | apply_settings | mitigate | validate(current=stored) keeps tls_verify unless TLS is on and the marker is present |
| T-06-32 | Tampering | apply_settings | mitigate | settings.validate + validate_password authoritative in the daemon; invalid input stores nothing |
| T-06-33 | Information disclosure | settings DB and backups | accept | Password stored in the existing 0600 settings DB on the data volume per D-12; documented in 06-09 (volume and backups must be protected) |
</threat_model>

<verification>
- Plugin, loader and import tests green; full suite at the 3 known baseline failures.
- Commit only the listed files; `mqtt-*.json` and `config/conf.d/` stay untracked/untouched; do not push.
</verification>

<success_criteria>
pellmonsrv loads the HomeAssistant plugin by default, it stays inert until enabled, and the D-Bus settings, status and test methods reach a working bridge.
</success_criteria>

<output>
Create `.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-07-SUMMARY.md` when done.
</output>
