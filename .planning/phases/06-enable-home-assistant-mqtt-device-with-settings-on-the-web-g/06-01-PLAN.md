---
phase: 06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - requirements-wsl.txt
  - Dockerfile
  - tests/test_ci_docker_config.py
  - tests/Pellmonsrv/plugins/fake_mqtt.py
  - tests/Pellmonsrv/plugins/conftest.py
  - tests/Pellmonsrv/plugins/test_fake_mqtt.py
autonomous: false
requirements: [D-11, D-13]

must_haves:
  truths:
    - "paho-mqtt is human-verified as the genuine Eclipse package before it is pinned or installed anywhere"
    - "requirements.txt pins paho-mqtt==2.1.0 and the Docker image installs ca-certificates so TLS with certificate verification can work"
    - "Tests can drive an MQTT client and a burner item database entirely in memory, with no sockets"
    - "The full Windows suite still shows only the 3 known baseline failures"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned MQTT client dependency"
      contains: "paho-mqtt==2.1.0"
    - path: "Dockerfile"
      provides: "System CA store for TLS verification"
      contains: "ca-certificates"
    - path: "tests/Pellmonsrv/plugins/fake_mqtt.py"
      provides: "FakeMqttClient, FakeClientFactory, FakeReasonCode, FakeMessage, FakeItem, FakeDb"
      contains: "class FakeMqttClient"
    - path: "tests/Pellmonsrv/plugins/conftest.py"
      provides: "fake_factory and fake_db fixtures"
      contains: "def fake_db"
  key_links:
    - from: "tests/Pellmonsrv/plugins/conftest.py"
      to: "tests/Pellmonsrv/plugins/fake_mqtt.py"
      via: "from fake_mqtt import FakeClientFactory, FakeDb"
      pattern: "from fake_mqtt import"
    - from: "tests/test_ci_docker_config.py"
      to: "requirements.txt and Dockerfile"
      via: "text assertions for the pin and the apt package"
      pattern: "paho-mqtt==2.1.0"
---

<objective>
Gate and add the one new dependency (paho-mqtt), make the Docker image able to verify broker certificates, and build the in-memory test doubles every later MQTT test uses.

Purpose: RESEARCH.md tags paho-mqtt `[ASSUMED]` (slopcheck unavailable), so a human must verify it before any install or pin. The fakes are the Wave 0 test seam required by 06-VALIDATION.md: pytest runs with `--disable-socket`, and a real paho `loop_start()` is blocked there.
Output: human approval, `requirements.txt` pin, `Dockerfile` `ca-certificates`, guard assertions, `fake_mqtt.py` and a plugins `conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-CONTEXT.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-VALIDATION.md
@CLAUDE.md
@pytest.ini
@tests/README.md

<interfaces>
paho-mqtt 2.1.0 callback signatures (VERSION2), which the fake must reproduce exactly:
- on_connect(client, userdata, flags, reason_code, properties)
- on_disconnect(client, userdata, disconnect_flags, reason_code, properties)
- on_message(client, userdata, message)  with message.topic (str), message.payload (bytes), message.retain (bool), message.qos (int)
- reason_code objects expose `.value` (int; CONNACK success 0, 134 bad username or password, 135 not authorised) and `.is_failure` (bool); str(reason_code) is a readable name.
- publish(topic, payload=None, qos=0, retain=False) returns MQTTMessageInfo with wait_for_publish(timeout=None), is_published(), rc, mid.

Client-factory contract used by the bridge (06-05) and implemented for real in bridge.default_paho_factory:
factory(cfg, password, client_id, will_topic, on_connect, on_disconnect, on_message) -> configured client, not yet connected. When will_topic is None no will is set (connection-test client).

src/Pellmonsrv/database.py: Database.get_text(name), Database.set_value(name, value) returns 'OK'; Plainitem.value; Scotte items carry string `.min`/`.max` attributes.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the paho-mqtt PyPI package before it is pinned or installed</name>
  <files>(none - verification only; no files are modified by this task)</files>
  <read_first>.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md (section "Package Legitimacy Audit")</read_first>
  <action>
    Pause and hand control to the developer before anything installs paho-mqtt. Do not edit requirements.txt and do not run any pip install yet. Present the how-to-verify steps verbatim and wait for the resume signal. This gate is never auto-approvable (workflow.auto_advance does not apply). If the developer approves a different version, use that exact pin in Task 2 and in every later reference.
  </action>
  <what-built>Nothing yet. RESEARCH.md tags paho-mqtt [ASSUMED] because slopcheck could not run in the research session.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/paho-mqtt/ and confirm the name is exactly `paho-mqtt` (not `paho`, not `paho-mqtt-client`), version 2.1.0 exists, and the wheel is `paho_mqtt-2.1.0-py3-none-any.whl` (pure Python, no install scripts).
    2. Confirm the project links point to https://github.com/eclipse-paho/paho.mqtt.python (Eclipse Foundation organisation).
    3. Confirm the licence shown is EPL-2.0 OR BSD-3-Clause.
    4. If anything does not match, reply with the discrepancy; the plan stops and the dependency is re-planned.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms the PyPI project page, repository and version for the paho-mqtt pin</human-check>
  </verify>
  <acceptance_criteria>
    - Human replied "approved" (or named a different verified pin to use instead of 2.1.0).
  </acceptance_criteria>
  <done>The paho-mqtt pin is human-verified as the genuine Eclipse package.</done>
  <resume-signal>Type "approved" (optionally "approved, use X.Y.Z") or describe the discrepancy</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin paho-mqtt, add ca-certificates to the image, add guard assertions</name>
  <files>requirements.txt, requirements-wsl.txt, Dockerfile, tests/test_ci_docker_config.py</files>
  <read_first>requirements.txt, requirements-wsl.txt, Dockerfile (apt list lines ~14-39), tests/test_ci_docker_config.py (existing test style, REPO_ROOT constant)</read_first>
  <action>
    Only after Task 1 is approved. In requirements.txt append, under the existing "# Plugin dependencies" area, a comment line "# Home Assistant plugin" followed by "paho-mqtt==2.1.0" (the approved pin), matching the file's exact-pin style. requirements-wsl.txt duplicates the full dependency list with ">=" floors, so add "paho-mqtt>=2.1.0" under its "# Plugin dependencies" block in the same style (2.x is required: the code uses CallbackAPIVersion.VERSION2). In Dockerfile add "ca-certificates \" to the apt-get install list next to "curl \" in the network/utilities group (RESEARCH Pitfall 7: bookworm-slim with --no-install-recommends may lack /etc/ssl/certs/ca-certificates.crt, and paho tls_set(cert_reqs=CERT_REQUIRED) uses the system store; supports D-11 TLS verification). In tests/test_ci_docker_config.py add two tests in the existing style: test_requirements_pin_paho_mqtt (requirements.txt contains a line exactly "paho-mqtt==2.1.0" and no unpinned "paho-mqtt" line) and test_dockerfile_installs_ca_certificates (Dockerfile text contains "ca-certificates"). Then install into the dev venv: ./venv-py3/Scripts/python.exe -m pip install paho-mqtt==2.1.0 (it may already be present; confirm with pip show). Never install any other package.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/test_ci_docker_config.py -q -k "paho or ca_certificates"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^paho-mqtt==2.1.0$" requirements.txt` prints 1
    - `grep -c "ca-certificates" Dockerfile` prints at least 1
    - `grep -c "^paho-mqtt>=2.1.0$" requirements-wsl.txt` prints 1
    - `./venv-py3/Scripts/python.exe -m pip show paho-mqtt` reports Version: 2.1.0
    - The two new tests pass
  </acceptance_criteria>
  <done>The dependency is pinned, the image will carry a CA store, and regressions in either are caught by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: In-memory MQTT client and burner database fakes</name>
  <files>tests/Pellmonsrv/plugins/fake_mqtt.py, tests/Pellmonsrv/plugins/conftest.py, tests/Pellmonsrv/plugins/test_fake_mqtt.py</files>
  <read_first>tests/conftest.py (guardrail rule lines ~21-34, _restore_pellmon_logger), tests/Pellmonsrv/conftest.py, src/Pellmonsrv/database.py (Plainitem, Database.get_text/set_value), src/Scotteprotocol/datamap.py (setting min/max values for the 10 numbers)</read_first>
  <behavior>
    - FakeMqttClient.publish records (topic, payload, qos, retain) and returns an info object whose wait_for_publish() returns immediately
    - fire_connect(0) invokes on_connect with 5 positional arguments and a reason code whose .value is 0 and .is_failure is False
    - fire_message(topic, "42", retain=True) invokes on_message with message.payload == b"42" and message.retain is True
    - With auto_connect=0, loop_start() fires on_connect once; with connect_error=OSError(...) connect() raises it
    - FakeDb default contains the 21 published items, reset/burner command items, and burner_connection with value "connected"; set_value records writes and can be told to raise ValueError or IOError for an item
    - No test in this task opens a socket
  </behavior>
  <action>
    Create tests/Pellmonsrv/plugins/fake_mqtt.py (plain module, no GPL header needed for tests, no type hints, %-formatting) with:
    FakeReasonCode(value) with .value, .is_failure (value >= 128 or value not in (0,)), __str__ returning a readable name, __eq__ against ints.
    FakeMessage(topic, payload, retain=False, qos=0) where payload is encoded to bytes if given str.
    FakeMessageInfo(mid) with rc=0, mid, wait_for_publish(timeout=None), is_published() -> True.
    FakeMqttClient(client_id='', auto_connect=None, connect_error=None) recording: client_id, will (tuple topic, payload, qos, retain or None), username, password, tls (dict of tls_set kwargs or None), tls_insecure (bool), reconnect_delay (tuple), max_queued, connect_timeout attribute, published (list of tuples topic, payload, qos, retain), subscriptions (list of (topic, qos)), unsubscriptions (list), connect_calls (list of (host, port, keepalive)), loop_started, loop_stopped, disconnect_calls (int), connected (bool); callback attributes on_connect, on_disconnect, on_message default None. Methods mirroring paho 2.1: will_set(topic, payload=None, qos=0, retain=False), username_pw_set(username, password=None), tls_set(**kwargs), tls_insecure_set(flag), reconnect_delay_set(min_delay=1, max_delay=120), max_queued_messages_set(n), connect(host, port=1883, keepalive=60) (raises connect_error when set), connect_async (same recording), loop_start() (sets loop_started; if auto_connect is not None calls fire_connect(auto_connect)), loop_stop(), disconnect() (increments disconnect_calls, connected False), reconnect(), is_connected(), publish(...) (records, returns FakeMessageInfo), subscribe(topic, qos=0) returning (0, mid), unsubscribe(topic). Helpers: fire_connect(reason=0) (sets connected True when reason 0, calls on_connect(self, None, {}, FakeReasonCode(reason), None)), fire_disconnect(reason=0), fire_message(topic, payload, retain=False, qos=0), published_to(topic) returning the list of (payload, qos, retain) for one topic, last_payload(topic).
    FakeClientFactory(auto_connect=None, connect_error=None): callable with the factory contract from the interfaces block; records every call in .calls (dict with cfg, password, client_id, will_topic) and every created client in .clients; mirrors the real factory's configuration on the fake (will_set(will_topic, 'offline', qos=1, retain=True) when will_topic is not None, username_pw_set when cfg['username'], tls_set/tls_insecure_set when cfg['tls'], reconnect_delay_set(1, 60), max_queued_messages_set(200)); assigns the three callbacks; property .last returning the newest client.
    FakeItem(name, value, min=None, max=None) with .name, .value, .min/.max strings (or absent when None).
    FakeDb: dict-like (keys, __contains__, __getitem__, items, get) of FakeItem plus get_text(name) returning str(value), set_value(name, value) that records into .writes (list of (name, value)), raises the exception configured in .fail[name] if any, else stores the value and returns 'OK'; snapshot() returning {name: str(item.value)}; add_change_listener(cb) storing into .listeners. Classmethod/factory make_scotte_db(exclude=()) building: sensors power, power_kW, boiler_temp, chute_temp, smoke_temp, oxygen, light, feeder_time, mode with plausible values ('64', '6.8', '56.9', ..., 'Running'); numbers boiler_temp_set (min '40', max '85'), boiler_temp_min ('10','70'), boiler_temp_diff_down ('0','20'), boiler_temp_diff_up ('0','15'), chimney_draught ('0','10'), cleaning_interval ('1','120'), cleaning_time ('0','60'), feeder_capacity ('400','8000'), min_power ('10','100'), max_power ('10','100'); commands reset_alarm, reset_ignition, burner_on, burner_off with value '0'; burner_connection value 'connected'; items listed in exclude are omitted.
    Create tests/Pellmonsrv/plugins/conftest.py with fixtures fake_factory (FakeClientFactory()) and fake_db (FakeDb.make_scotte_db()), importing via "from fake_mqtt import FakeClientFactory, FakeDb" (pytest's default prepend import mode puts this directory on sys.path; there is no __init__.py here, keep it that way). Write the behaviour checks above first in tests/Pellmonsrv/plugins/test_fake_mqtt.py, run them red, then implement the fakes until green.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_fake_mqtt.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class FakeMqttClient" tests/Pellmonsrv/plugins/fake_mqtt.py` prints 1; same for FakeClientFactory, FakeDb, FakeReasonCode
    - `grep -c "import socket" tests/Pellmonsrv/plugins/fake_mqtt.py` prints 0
    - tests/Pellmonsrv/plugins/test_fake_mqtt.py passes and covers each behaviour bullet above
    - Full suite `./venv-py3/Scripts/python.exe -m pytest tests/ -q` shows only the 3 known baseline failures (test_backup_script::test_conf_d_overrides_pellmon_conf, test_plugin_loader::test_every_available_plugin_loads[consumption], [silolevel])
  </acceptance_criteria>
  <done>Every later MQTT test can run without a network, a broker, D-Bus or a burner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> dev venv / Docker image | Third-party code enters the daemon process |
| Broker TLS certificate -> paho | Server identity is checked against the system CA store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | pip install paho-mqtt | mitigate | Blocking human-verify checkpoint (Task 1) before any install; exact `==` pin; pure-Python wheel with no install scripts |
| T-06-01 | Spoofing | TLS verification in container | mitigate | `ca-certificates` in Dockerfile so CERT_REQUIRED verification has a CA store; guarded by test |
| T-06-02 | Information disclosure | test fakes | accept | Fakes hold only synthetic values; tests use a secret sentinel, never real credentials |
</threat_model>

<verification>
- Human approval recorded in the SUMMARY.
- `./venv-py3/Scripts/python.exe -m pytest tests/ -q`: only the 3 known baseline failures.
- Commit only the files listed in this plan (explicit paths, never `git add -A` or `git add .`). The untracked `mqtt-*.json` Home Assistant export must stay untracked: `git status --porcelain | grep "mqtt-"` still shows `??`. Do not push.
</verification>

<success_criteria>
paho-mqtt 2.1.0 is human-verified and pinned, the image has a CA store, and the in-memory fakes exist and are self-tested.
</success_criteria>

<output>
Create `.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-01-SUMMARY.md` when done (record the developer approval text).
</output>
