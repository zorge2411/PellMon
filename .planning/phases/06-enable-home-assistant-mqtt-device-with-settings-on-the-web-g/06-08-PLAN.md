---
phase: 06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g
plan: 08
type: execute
wave: 3
depends_on: ["06-04", "06-06"]
files_modified:
  - src/Pellmonweb/media/js/homeassistant.js
  - tests/Pellmonweb/test_homeassistant_js.py
  - tests/browser/fake_dbus.py
  - tests/browser/test_mobile_overflow.py
  - tests/browser/test_browser_harness.py
  - tests/browser/test_homeassistant_browser.py
autonomous: true
requirements: [D-06, D-10, D-11, D-13]

must_haves:
  truths:
    - "With JavaScript the status line refreshes every 5 seconds without a page load, and changes its text only when the status changes"
    - "Ticking TLS enables Verify, swaps 1883/8883, and shows the verification-off warning when Verify is unticked; ticking Allow commands shows its warning"
    - "Test connection shows Testing..., then the success or mapped failure message, never saves, and returns the button to its idle state"
    - "The script writes only text (never .html()/innerHTML), and the page still works without it"
    - "At 390px and 768px /homeassistant/ has no sideways scroll, 44px tap targets, single-column fields on phones and paired fields on tablets, and the navbar stays on one row at 768px"
  artifacts:
    - path: "src/Pellmonweb/media/js/homeassistant.js"
      provides: "Progressive enhancement: status poll, TLS/verify/port behaviour, commands warning, prefix mirror, Save/Test busy states, fetch-based test"
      contains: "homeassistant/status"
    - path: "tests/Pellmonweb/test_homeassistant_js.py"
      provides: "Source scan: no .html( / innerHTML / eval in the script"
    - path: "tests/browser/test_homeassistant_browser.py"
      provides: "UI-SPEC rendered checks H-A1/H-A2, H-B1..H-B10, H-E1..H-E3"
    - path: "tests/browser/fake_dbus.py"
      provides: "mqtt_get_settings / mqtt_set_settings / mqtt_status / mqtt_test_start / mqtt_test_result canned methods"
      contains: "def mqtt_status"
  key_links:
    - from: "src/Pellmonweb/media/js/homeassistant.js"
      to: "GET ${webroot}/homeassistant/status and POST ${webroot}/homeassistant/test (format=json)"
      via: "fetch / jQuery ajax using the global webroot variable from layout.html"
      pattern: "format"
    - from: "tests/browser/test_mobile_overflow.py and test_browser_harness.py"
      to: "/homeassistant/"
      via: "PAGES list entry"
      pattern: "/homeassistant/"
---

<objective>
Add the page's progressive-enhancement script and the rendered browser checks from 06-UI-SPEC (Rendered section), plus the fake D-Bus methods the browser stub needs.

Purpose: D-13 live status and Test connection feedback, D-06/D-11 interactive warnings, D-10 page usable on phones. The executor cannot run wsl.exe, so the browser tests are written and collected here (they skip on Windows without PELLMON_BROWSER_TESTS) and the orchestrator runs them in WSL/CI afterwards.
Output: `homeassistant.js`, a JS source-scan test, extended `tests/browser` files and a new browser test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-UI-SPEC.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-04-SUMMARY.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-06-SUMMARY.md
@tests/README.md
@CLAUDE.md

<interfaces>
From 06-04 (homeassistant.html): ids/classes #mqtt-status (role status, class well or alert alert-<level>), #mqtt-verify-warning and #mqtt-commands-warning (toggled with the hidden attribute), .mqtt-prefix-code (prefix mirror), buttons .mqtt-save and .mqtt-test (formaction .../homeassistant/test), #mqtt-test-result (role status), inputs by name: enabled, host, port, username, password, clear_password, tls, tls_verify, prefix, discovery_prefix, device_id, device_name, node_id, uid_prefix, allow_commands, refresh; error spans id "<field>-error", form-groups get has-error. layout.html defines global `var webroot` and loads jQuery 1.11 before page scripts.

From 06-06 (homeassistant.py): GET /homeassistant/status -> JSON {level, glyph, word, text, title}; POST /homeassistant/test with form field format=json -> JSON {state: 'ok'|'error'|'invalid'|'rejected'|'daemon_down'|'auth_disabled', message, level, errors}; save validates before the auth-configured refusal, so the auth-less stub can render field errors; buttons are disabled in the stub because credentials are empty (tests remove the disabled attribute via page.evaluate before clicking, and route /homeassistant/test).

tests/browser: conftest.py fixtures playwright_rt, browser, stub_url, page_at(path, width), helpers shot(page, name), wait_ready(page, path); stub_server.py mounts the real PellMonWeb over FakeDbus with web.credentials = [] and tools.auth.on False; fake_dbus.py FakeDbus plain canned methods (get_setting returns "system.svg"). PAGES lists in test_mobile_overflow.py and test_browser_harness.py. Browser command (WSL/CI): PELLMON_BROWSER_TESTS=1 PYTHONPATH=src pytest tests/browser -v -rs --allow-unix-socket. tests/Pellmonweb/test_source_js_no_html_sink.py is the pattern for the JS source scan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: homeassistant.js progressive enhancement with a source-scan test</name>
  <files>src/Pellmonweb/media/js/homeassistant.js, tests/Pellmonweb/test_homeassistant_js.py</files>
  <read_first>src/Pellmonweb/media/js/source.js (text-only DOM style), tests/Pellmonweb/test_source_js_no_html_sink.py, src/Pellmonweb/html/homeassistant.html, src/Pellmonweb/homeassistant.py, .planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-UI-SPEC.md (Live JS behaviours, Test connection table)</read_first>
  <behavior>
    - Source scan: the script contains no ".html(", "innerHTML", "outerHTML", "insertAdjacentHTML", "document.write" or "eval("; it references "homeassistant/status", "homeassistant/test", "format", "Testing...", "Saving...", "aria-busy" and a 5000 ms interval
    - Source scan: the script never reads or sends the password field value except by submitting the whole form (FormData of the form), and never writes a password value into the DOM
  </behavior>
  <action>
    Tests first (tests/Pellmonweb/test_homeassistant_js.py, regex scans like test_source_js_no_html_sink.py). Then write src/Pellmonweb/media/js/homeassistant.js (jQuery 1.11 is available; plain DOM is fine), wrapped in an IIFE that exits early when #mqtt-status is absent. Behaviours per 06-UI-SPEC "Live JS behaviours": (a) every 5000 ms GET webroot + '/homeassistant/status' (cache: false); if the JSON differs from the last one, rebuild #mqtt-status content with createElement/textContent (glyph span 'glyphicon glyphicon-' + glyph when glyph, strong for word, text node for text), set className to 'well' or 'alert alert-' + level, set title; on request failure leave the line unchanged. (b) TLS change toggles tls_verify.disabled, the muted help "Turn on TLS to change this." visibility, #mqtt-verify-warning hidden unless tls && !tls_verify, and swaps port 1883 <-> 8883 only when the port still holds the other standard value. (c) allow_commands change toggles #mqtt-commands-warning hidden. (d) prefix input mirrors its value into .mqtt-prefix-code via textContent. (e) form submit via Save: button text "Saving..." and disabled. Test click: preventDefault, set text "Testing...", disabled, aria-busy="true", set #mqtt-test-result text "Testing the connection...", POST FormData(form) plus format=json to the Test button's formaction with credentials same-origin and a 10 s AbortController timeout; on JSON response show message with class text-success + glyphicon-ok for state ok, else text-danger + glyphicon-remove; for state 'invalid' also apply has-error/aria-invalid and set the "<field>-error" span text for each errors entry (create the span with createElement if missing, link via aria-describedby); on timeout/failure show "Test failed: timed out. Nothing was saved."; always restore text "Test connection", enabled, aria-busy removed. Never touch /save from the test path. No framework, no new library.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_homeassistant_js.py tests/Pellmonweb/test_source_js_no_html_sink.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.html(\|innerHTML\|insertAdjacentHTML\|document.write\|eval(" src/Pellmonweb/media/js/homeassistant.js` prints 0
    - `grep -c "homeassistant/status" src/Pellmonweb/media/js/homeassistant.js` >= 1
    - Test file passes
  </acceptance_criteria>
  <done>The page's live behaviours exist as text-only progressive enhancement.</done>
</task>

<task type="auto">
  <name>Task 2: Browser stub methods and rendered layout/interaction tests</name>
  <files>tests/browser/fake_dbus.py, tests/browser/test_mobile_overflow.py, tests/browser/test_browser_harness.py, tests/browser/test_homeassistant_browser.py</files>
  <read_first>tests/browser/conftest.py, tests/browser/stub_server.py, tests/browser/fake_dbus.py, tests/browser/test_mobile_overflow.py (OVERFLOW_JS), tests/browser/test_mobile_interactions.py (rect/boxes helpers, tap patterns), tests/README.md, .planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-UI-SPEC.md (Rendered H-A1..H-E3)</read_first>
  <action>
    In tests/browser/fake_dbus.py add plain methods to FakeDbus: mqtt_get_settings() returning a full canonical settings dict with enabled True, a long host (60+ chars), port 1883, username 'pellmon', tls False, tls_verify True, prefix 'scotte', discovery_prefix 'homeassistant', device_id 'demo_device_identifier_0001', device_name 'Pellet burner', node_id '', uid_prefix '', allow_commands False, refresh 60, has_password True, available True; mqtt_set_settings(d) returning {'ok': True, 'errors': {}}; mqtt_status() returning {'state': 'connected', 'host': <same host>, 'port': 1883, 'reason': '', 'last_publish': a fixed epoch, 'last_connect': a fixed epoch, 'availability': 'online', 'burner': 'connected', 'commands_enabled': False, 'entities': 9}; mqtt_test_start(d) returning True; mqtt_test_result() returning {'state': 'ok', 'message': ''}. Never put a real identifier from the untracked export in any file. Append "/homeassistant/" to PAGES in test_mobile_overflow.py and test_browser_harness.py. Create tests/browser/test_homeassistant_browser.py (import helpers from conftest like the sibling modules) implementing the UI-SPEC rendered checks: H-A1 (overflow JS from test_mobile_overflow imported or duplicated, at 390 and 768, also after filling the longest allowed values: 64-char prefix, 253-char host, 64-char identifier, and after rendering the error state); H-A2 (page.route on '**/homeassistant/status' fulfilling each of Off, Connected, Disconnected with the longest reason, burner-offline JSON in the status_view shape, waiting for #mqtt-status text to change, then the overflow check); H-B1..H-B10 at 390x844 (H-B5 with the stored-password stub: value '' and placeholder 'set' and body innerHTML free of any secret; the no-password variant is covered by the Windows render test in 06-04; H-B8 removes the disabled attribute via page.evaluate, routes '**/homeassistant/test' to JSON ok then to JSON error with reason 'connection refused', asserts "Testing..."/disabled/aria-busy during a delayed route, the final messages, the restored button, and that no request URL contains '/save'; H-B9 removes disabled and the form's HTML5 validation (form.noValidate = true), types port 70000, submits Save, asserts .has-error on the port group, role alert summary focused, aria-invalid on the port input; H-B10 routes status Connected then Disconnected and asserts the #mqtt-status text/class change within 6 s without navigation); H-E1..H-E3 at 768x1024 (paired rows by equal top, Save/Test on one row with >= 8px gap, navbar li all on one row, height <= 60, computed padding-left 8px). Every test uses page_at and skips through the existing env gate when PELLMON_BROWSER_TESTS is unset. Do not add @pytest.mark.enable_socket and do not remove --disable-socket.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/browser -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - On Windows without PELLMON_BROWSER_TESTS the browser tests are collected and skipped with the harness reason (no errors, no failures)
    - `grep -c "/homeassistant/" tests/browser/test_mobile_overflow.py tests/browser/test_browser_harness.py` shows 1 in each file
    - `grep -c "def mqtt_status\|def mqtt_get_settings\|def mqtt_set_settings\|def mqtt_test_start\|def mqtt_test_result" tests/browser/fake_dbus.py` prints 5
    - `grep -c "enable_socket" tests/browser/test_homeassistant_browser.py` prints 0
    - Full suite `./venv-py3/Scripts/python.exe -m pytest tests/ -q` shows only the 3 known baseline failures
    - SUMMARY states that the browser run is pending for the orchestrator: `PELLMON_BROWSER_TESTS=1 PYTHONPATH=src pytest tests/browser -v -rs --allow-unix-socket` in WSL (the executor cannot run wsl.exe)
  </acceptance_criteria>
  <done>The rendered UI-SPEC checks for /homeassistant/ exist and run in WSL/CI; the stub serves the page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status/test JSON -> DOM | Server strings (host, reason) are written into the page by script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-34 | Tampering (DOM XSS) | homeassistant.js | mitigate | textContent/createElement only; source scan forbids .html(), innerHTML, insertAdjacentHTML, document.write, eval |
| T-06-35 | Information disclosure | password field | mitigate | Script never reads the password value except inside FormData on Test, never writes it to the DOM; H-B5 checks body HTML |
| T-06-36 | Tampering (CSRF) | fetch to /test | mitigate | Same-origin fetch; server check_same_origin remains authoritative |
</threat_model>

<verification>
- Windows: JS scan tests pass, browser tests collected and skipped; full suite at the 3 known baseline failures.
- WSL/CI (orchestrator, after this plan): `PELLMON_BROWSER_TESTS=1 PYTHONPATH=src pytest tests/browser -v -rs --allow-unix-socket` green.
- Commit only the listed files; `mqtt-*.json` stays untracked; do not push.
</verification>

<success_criteria>
The script provides all UI-SPEC live behaviours with text-only DOM writes, and the rendered checks for /homeassistant/ are in the browser suite.
</success_criteria>

<output>
Create `.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-08-SUMMARY.md` when done.
</output>
