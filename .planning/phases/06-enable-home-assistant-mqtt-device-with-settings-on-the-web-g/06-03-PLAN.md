---
phase: 06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Pellmonsrv/pellmonsrv.py
  - tests/Pellmonsrv/test_homeassistant_dbus.py
  - tests/Pellmonsrv/test_database_change_listener.py
autonomous: true
requirements: [D-12, D-13, D-16, D-19]

must_haves:
  truths:
    - "Plugins can register a change listener on the daemon Database before the change loop starts, and a failing listener never stops the loop or the D-Bus signal"
    - "The web process can read non-secret MQTT settings, save settings, read the MQTT status and start/poll a connection test over five D-Bus methods, all JSON strings"
    - "No D-Bus method ever returns the MQTT password, GetSetting('mqtt.password') returns '' and no mqtt.* key is in ALLOWED_SETTINGS"
    - "When the Home Assistant plugin is not loaded every method answers with available: false instead of raising"
  artifacts:
    - path: "src/Pellmonsrv/pellmonsrv.py"
      provides: "Database.listeners/add_change_listener/_notify/snapshot; _ha_plugin(); GetMqttSettings, SetMqttSettings, GetMqttStatus, StartMqttTest, GetMqttTestResult"
      contains: "def GetMqttSettings"
    - path: "tests/Pellmonsrv/test_homeassistant_dbus.py"
      provides: "D-12/D-13 D-Bus contract tests via daemon_module"
    - path: "tests/Pellmonsrv/test_database_change_listener.py"
      provides: "D-16 listener hook tests"
  key_links:
    - from: "MyDBUSService.GetMqttSettings and siblings"
      to: "plugin_object.get_settings_dict / apply_settings / status_dict / start_test / test_result_dict"
      via: "_ha_plugin() lookup over conf.database.protocols where plugin.name == 'HomeAssistant'"
      pattern: "_ha_plugin"
    - from: "Database.run"
      to: "change listeners"
      via: "self._notify(changed_params) after the diff is built"
      pattern: "self._notify\\(changed_params\\)"
---

<objective>
Add the two daemon-side seams the MQTT plugin and the web page need: a change-listener hook on the pellmonsrv Database thread (publish-on-change, D-16) and five JSON D-Bus methods for the settings page (D-12, D-13, D-19/C7 start-and-poll test).

Purpose: Keeps pellmonweb stateless and away from the settings DB, and keeps the password write-only: the Phase 8 `ALLOWED_SETTINGS` whitelist stays untouched so `GetSetting` can never return it.
Output: edits to `src/Pellmonsrv/pellmonsrv.py` plus two test files. This plan codes against the plugin-object contract below; 06-07 implements that contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-CONTEXT.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-RESEARCH.md
@.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-PATTERNS.md
@CLAUDE.md

<interfaces>
Existing in src/Pellmonsrv/pellmonsrv.py:
- class Database(threading.Thread, _Database): __init__ sets self.dbus_service = None, self.values = {}, self.protocols = [], self.daemon = True, then builds the PluginManager and activates plugins (plugin.plugin_object.activate(conf.plugin_conf[plugin.name], globals(), self)), then self.start().
- run(): every 2 s builds changed_params (list of {'name', 'value'}) from item.get_text(item.value) and updates self.values; then `if changed_params: if self.dbus_service: ... self.dbus_service.changed_parameters(s)`.
- ALLOWED_SETTINGS = {'web.system_image': _valid_system_image}  (DO NOT CHANGE)
- MyDBUSService methods use @dbus.service.method('org.pellmon.int', in_signature=..., out_signature=...); getPlugins iterates `conf.database.protocols` (plugin.name, plugin.plugin_object).
- GetSetting logs 'rejected settings read for unknown key %s' and returns '' for keys outside ALLOWED_SETTINGS.

Plugin-object contract (implemented by 06-07 in src/Pellmonsrv/plugins/homeassistant/__init__.py, class homeassistant):
- get_settings_dict() -> dict: all non-secret canonical settings keys + has_password (bool) + available (bool). Never a 'password' key.
- apply_settings(data_dict) -> {'ok': bool, 'errors': {field: message}}; data may contain 'password' (str) and 'clear_password' (bool).
- status_dict() -> dict: state ('off'|'connecting'|'connected'|'disconnected'), host, port, reason (fixed reason string or ''), last_publish (epoch float or None), last_connect (epoch or None), availability ('online'|'offline'), burner ('connected'|'no_connection'|'demo'|'unsupported'), commands_enabled (bool), entities (int).
- start_test(data_dict) -> bool (False if a test is already running or input invalid)
- test_result_dict() -> {'state': 'idle'|'running'|'ok'|'error', 'message': str}

tests/Pellmonsrv/conftest.py: fixture daemon_module imports Pellmonsrv.pellmonsrv with dbus/gi/pwd/grp stubs (identity decorators). Methods are called unbound: daemon_module.MyDBUSService.GetMqttSettings(None).
tests/Pellmonsrv/test_settings_store.py lines ~41-65: svc fixture pattern with init_keyval_storage(str(tmp_path / "settings.db")).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database change-listener hook and snapshot</name>
  <files>src/Pellmonsrv/pellmonsrv.py, tests/Pellmonsrv/test_database_change_listener.py</files>
  <read_first>src/Pellmonsrv/pellmonsrv.py (class Database, lines ~73-150), tests/Pellmonsrv/conftest.py (daemon_module), tests/Pellmonsrv/test_sigterm_handling.py (do not break its guards)</read_first>
  <behavior>
    - A Database built with Database.__new__ and listeners/values set by the test: add_change_listener(cb) appends cb; _notify([{'name': 'boiler_temp', 'value': '56'}]) calls every listener once with that list
    - A listener that raises does not prevent the next listener from being called; the exception is logged once at ERROR (logger.exception) on 'pellMon'
    - snapshot() returns a new dict equal to values; mutating the returned dict does not change values
    - Source order check (ast/inspect on Database.__init__): 'self.listeners = []' appears before 'collectPlugins' so plugins can register during activate
    - Source check on Database.run: 'self._notify(changed_params)' is inside the 'if changed_params:' block and is not nested under 'if self.dbus_service:' (listeners run even without a D-Bus service)
  </behavior>
  <action>
    Tests first (tests/Pellmonsrv/test_database_change_listener.py using the daemon_module fixture, inspect.getsource for the order checks). Then in class Database: add self.listeners = [] next to self.values = {} (before the PluginManager block), add methods add_change_listener(self, callback) (append), _notify(self, changed_params) (iterate over list(self.listeners); each call in try/except Exception with logger.exception('change listener failed') and continue), and snapshot(self) returning dict(self.values). In run(), inside the existing `if changed_params:` block, call self._notify(changed_params) after the dbus_service signal block (not nested in it). Keep the existing loop and exception handling otherwise untouched; match surrounding indentation and %-formatting. Listeners must only enqueue; document that with a one-line comment on add_change_listener. Do not edit the SIGTERM handlers or Database.terminate.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_database_change_listener.py tests/Pellmonsrv/test_sigterm_handling.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def add_change_listener" src/Pellmonsrv/pellmonsrv.py` prints 1; same for `def _notify` and `def snapshot`
    - `grep -c "self._notify(changed_params)" src/Pellmonsrv/pellmonsrv.py` prints 1
    - New tests pass; test_sigterm_handling.py still passes
  </acceptance_criteria>
  <done>Plugins can subscribe to the existing 2 s change loop safely (D-16).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Five MQTT D-Bus methods with write-only password</name>
  <files>src/Pellmonsrv/pellmonsrv.py, tests/Pellmonsrv/test_homeassistant_dbus.py</files>
  <read_first>src/Pellmonsrv/pellmonsrv.py (MyDBUSService, getPlugins, GetSetting, SetSetting, ALLOWED_SETTINGS), tests/Pellmonsrv/test_settings_store.py (svc fixture, unknown-key caplog test), tests/Pellmonsrv/conftest.py</read_first>
  <behavior>
    - With monkeypatch.setattr(daemon_module, 'conf', SimpleNamespace(database=SimpleNamespace(protocols=[SimpleNamespace(name='HomeAssistant', plugin_object=fake)]))): GetMqttSettings(None) returns a JSON string equal to fake.get_settings_dict(); SetMqttSettings(None, json.dumps(d)) calls fake.apply_settings with the parsed dict and returns its JSON; GetMqttStatus returns fake.status_dict() JSON; StartMqttTest returns fake.start_test(d) as bool; GetMqttTestResult returns fake.test_result_dict() JSON
    - If get_settings_dict() mistakenly includes a 'password' key, GetMqttSettings strips it (defence in depth)
    - Plugin absent (protocols empty or conf None): GetMqttSettings and GetMqttStatus return '{"available": false}'; SetMqttSettings returns ok false with available false; StartMqttTest returns False; GetMqttTestResult returns state 'error' with available false
    - SetMqttSettings / StartMqttTest with invalid JSON or a non-object return ok false / False and do not call the plugin
    - A plugin method that raises returns a safe error JSON (ok false) and logs one error line naming the method and exception type only; with a request containing password 's3cret-XYZ', caplog (DEBUG, 'pellMon') never contains 's3cret-XYZ'
    - GetSetting(None, 'mqtt.password') == '' and SetSetting(None, 'mqtt.password', 'x') is False (with init_keyval_storage on tmp_path)
    - not any(k.startswith('mqtt.') for k in daemon_module.ALLOWED_SETTINGS)
  </behavior>
  <action>
    Tests first. Add a module-level helper _ha_plugin() in pellmonsrv.py that returns plugin.plugin_object for the first entry in conf.database.protocols with plugin.name == 'HomeAssistant', or None when conf/database/protocols is missing (use getattr defensively). Add five methods to MyDBUSService following the GetSetting/SetSetting decorator and docstring style (D-12, D-13, D-19/C7): GetMqttSettings (in_signature='', out_signature='s'), SetMqttSettings (in 's', out 's'), GetMqttStatus (in '', out 's'), StartMqttTest (in 's', out 'b'), GetMqttTestResult (in '', out 's'). Each parses input with json.loads inside try/except (non-dict -> reject), delegates to the plugin-object contract, serialises with json.dumps, pops any 'password' key from GetMqttSettings output, and wraps the plugin call in try/except Exception logging logger.error('%s failed: %s' % (method_name, type(e).__name__)) only (never the arguments, never str(e), never logger.exception here because a traceback line could echo request data). Do NOT add anything to ALLOWED_SETTINGS and do not change GetSetting/SetSetting. These methods never block: the connection test is start-and-poll (C7), so no method waits on the network.
  </action>
  <verify>
    <automated>./venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_homeassistant_dbus.py tests/Pellmonsrv/test_settings_store.py tests/Pellmonsrv/test_database_change_listener.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def GetMqttSettings\|def SetMqttSettings\|def GetMqttStatus\|def StartMqttTest\|def GetMqttTestResult" src/Pellmonsrv/pellmonsrv.py` prints 5
    - `grep -c "mqtt" <(sed -n '/^ALLOWED_SETTINGS = {/,/^}/p' src/Pellmonsrv/pellmonsrv.py)` prints 0
    - New tests pass including the sentinel caplog test
    - Full suite `./venv-py3/Scripts/python.exe -m pytest tests/ -q` shows only the 3 known baseline failures
  </acceptance_criteria>
  <done>The web page has a complete, password-safe D-Bus API for settings, status and the connection test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pellmonweb -> pellmonsrv D-Bus | JSON from the web process (after login) enters the daemon |
| plugins -> Database thread | Listener callbacks run on the change-detection thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information disclosure | GetMqttSettings / GetSetting | mitigate | Password key stripped from output; ALLOWED_SETTINGS unchanged so GetSetting('mqtt.password') is ''; regression tests |
| T-06-08 | Information disclosure | D-Bus error logging | mitigate | Log method name and exception type only; sentinel caplog test |
| T-06-09 | Denial of service | Database thread listeners | mitigate | Each listener in try/except; listeners only enqueue (documented contract) |
| T-06-10 | Denial of service | Blocking D-Bus test call | mitigate | Start-and-poll methods, no network wait inside a D-Bus method (C7) |
| T-06-11 | Tampering | Malformed JSON | mitigate | json.loads in try/except, dict type check, plugin validates every field (settings.validate) |
</threat_model>

<verification>
- Targeted tests green; full suite at the 3 known baseline failures.
- Commit only the listed files by explicit path; `mqtt-*.json` stays untracked; do not push.
</verification>

<success_criteria>
The Database thread exposes a safe listener hook and snapshot, and MyDBUSService exposes the five JSON methods with a proven write-only password.
</success_criteria>

<output>
Create `.planning/phases/06-enable-home-assistant-mqtt-device-with-settings-on-the-web-g/06-03-SUMMARY.md` when done.
</output>
