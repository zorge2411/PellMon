---
phase: 10-optimize-for-mobile-devices-screen-size
plan: 06
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - .github/workflows/ci.yml
  - tests/test_ci_docker_config.py
  - tests/README.md
  - .gitignore
autonomous: true
requirements: [D-02, D-06]

must_haves:
  truths:
    - "Every CI run on push and pull request installs Playwright's Chromium and runs tests/browser with PELLMON_BROWSER_TESTS=1, so a layout regression or a missing browser fails the test job"
    - "The publish job is still gated on the single test job, so no release is published when the browser checks fail"
    - "Layout screenshots are uploaded as a CI artifact even when the browser step fails"
    - "All existing ci.yml guard assertions still pass unchanged (no setup-python, no --force, no pip self-upgrade, needs: test, PYTHONPATH: src)"
    - "The test README documents why the browser run uses the socket flag and how to run it locally"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Browser install, browser test, and screenshot upload steps inside the existing test job"
      contains: "PELLMON_BROWSER_TESTS: \"1\""
    - path: "tests/test_ci_docker_config.py"
      provides: "New guard test that the mandatory browser step cannot be silently removed"
      contains: "def test_ci_runs_mandatory_browser_layout_tests"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "requirements-browser.txt"
      via: "pip install -r requirements-browser.txt in the test job"
      pattern: "requirements-browser.txt"
    - from: ".github/workflows/ci.yml publish job"
      to: ".github/workflows/ci.yml test job"
      via: "needs: test (browser step is part of test)"
      pattern: "needs: test"
---

<objective>
Make the headless-browser layout checks a mandatory part of CI (D-06(b)) without breaking any existing workflow guard, and document the socket-flag exception.

Purpose: Browser tests skip locally by design; CI is where they must always run and block releases.
Output: Three new steps in the `test` job of `ci.yml`, one new guard test in `tests/test_ci_docker_config.py`, README and .gitignore updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-01-SUMMARY.md
@CLAUDE.md
@.github/workflows/ci.yml
@tests/test_ci_docker_config.py

<interfaces>
ci.yml `test` job today: checkout, apt system packages, venv with `--system-site-packages` on `$RUNNER_TEMP/venv` added to `$GITHUB_PATH`, "Install Python dependencies" (`pip install -r requirements.txt`, `pip install pytest pytest-mock pytest-socket`), binding check, "Run import tests", "Run test suite" (`env: PYTHONPATH: src`, `pytest tests/ -v`). `publish` job has `needs: test`. Indentation: 2-space YAML, steps at 6 spaces, no tabs.

Existing guard assertions in tests/test_ci_docker_config.py that must keep passing (do not edit them): no tabs; "actions/setup-python" absent; "--system-site-packages" present; "install --upgrade pip" absent; "/usr/lib/python3/dist-packages" absent; "PYTHONPATH: src" present; `pytest\s+tests/` matches; "needs: test" present; "git tag -f" absent; "--force" absent anywhere in the file (so `--force-enable-socket` is impossible); "workflow_run" absent.

Browser pytest flag: `BROWSER_PYTEST_FLAGS` from 10-01-SUMMARY "Spike result" (expected `--allow-unix-socket`; fallback `-o addopts=""`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard test first, then the browser steps in the existing test job</name>
  <files>tests/test_ci_docker_config.py, .github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - tests/test_ci_docker_config.py
    - requirements-browser.txt
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-01-SUMMARY.md (Spike result)
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md (Pitfall 2, Pitfall 3, "CI step (fits the guard tests)")
  </read_first>
  <behavior>
    - test_ci_runs_mandatory_browser_layout_tests: ci.yml contains "requirements-browser.txt", "playwright install --with-deps", "tests/browser", `PELLMON_BROWSER_TESTS: "1"`, the recorded BROWSER_PYTEST_FLAGS string, "-rs", "actions/upload-artifact@", "if: always()"; the browser step text appears after "Run test suite" and before "  publish:" (i.e. inside the test job); "needs: test" still present; requirements-browser.txt pins playwright with `==`; "playwright" does not appear in requirements.txt or the Dockerfile.
  </behavior>
  <action>
    Append a new function `test_ci_runs_mandatory_browser_layout_tests` to `tests/test_ci_docker_config.py` with a docstring tagged "D-06(b)" and assertion messages in the file's existing style; do not modify any existing function. Run it (RED).

    Then add three steps to the `test` job in `.github/workflows/ci.yml`, after "Run test suite" and before the `publish:` job, spaces only, same indentation as existing steps (RESEARCH "CI step" recipe):
    1. `- name: Install browser test dependencies` running `pip install -r requirements-browser.txt` then `playwright install --with-deps --only-shell chromium` (if the Plan 01 spike or the first CI run shows `--only-shell` misbehaving, drop only that flag; keep `--with-deps`).
    2. `- name: Run mobile layout browser tests` with `env:` `PYTHONPATH: src`, `PELLMON_BROWSER_TESTS: "1"`, `PELLMON_SHOTS_DIR: ${{ runner.temp }}/pellmon-shots`, running `pytest tests/browser -v -rs` followed by the recorded BROWSER_PYTEST_FLAGS (expected `--allow-unix-socket`).
    3. `- name: Upload layout screenshots` with `if: always()`, `uses: actions/upload-artifact@v4` (v4 is a long-established major; RESEARCH could not confirm the v7 tag date, so do not guess newer), `with:` `name: mobile-layout-screenshots`, `path: ${{ runner.temp }}/pellmon-shots`, `retention-days: 7`, `if-no-files-found: ignore`.
    Do not add `actions/setup-python`, a new job, a `needs:` list, `--force-enable-socket` or any `--force` string, or `install --upgrade pip`. The existing `pytest tests/ -v` step stays and will report the browser tests as skipped (env var unset there); the new step is the mandatory run. Keeping it a step of `test` means `publish` (needs: test) is blocked by a browser failure with zero edits to the existing guard.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/test_ci_docker_config.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Every test in `tests/test_ci_docker_config.py` passes, including all pre-existing ones (`git diff tests/test_ci_docker_config.py` shows only added lines).
    - `grep -c "\-\-force" .github/workflows/ci.yml` = 0; `grep -c "actions/setup-python" .github/workflows/ci.yml` = 0; `grep -c "needs: test" .github/workflows/ci.yml` = 1; `grep -cE "^  (test|publish):$" .github/workflows/ci.yml` = 2 and no other 2-space-indented key appears after the `jobs:` line (no new job).
    - `grep -n 'PELLMON_BROWSER_TESTS: "1"' .github/workflows/ci.yml` matches exactly once.
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` succeeds if PyYAML is available in venv-py3 (skip this check otherwise and say so in the SUMMARY).
  </acceptance_criteria>
  <done>CI runs the browser layout tests mandatorily inside the gated test job, with a guard test preventing silent removal.</done>
</task>

<task type="auto">
  <name>Task 2: Document the browser tests and ignore local screenshots</name>
  <files>tests/README.md, .gitignore</files>
  <read_first>
    - tests/README.md (section "Anything a later phase must not do", lines ~60-70)
    - .gitignore
    - tests/test_socket_guardrail.py (docstring on the guardrail)
  </read_first>
  <action>
    In `tests/README.md` add a short section "Headless-browser layout tests (Phase 10)" stating: tests live in `tests/browser/` and skip unless `PELLMON_BROWSER_TESTS=1`; with it set, a missing Playwright or browser is a failure, not a skip; they run in CI as a mandatory step of the `test` job; the run uses the recorded BROWSER_PYTEST_FLAGS (`--allow-unix-socket` lets Playwright's asyncio self-pipe work while TCP stays blocked in the test process; the stub web server runs as a subprocess and reports its port on stdout); this is a CLI flag on that one invocation, not an `enable_socket` marker, and `--disable-socket` stays in `pytest.ini`; local run recipe for WSL (scratch venv with `--system-site-packages`, `pip install -r requirements-browser.txt`, `python -m playwright install chromium`, then the exact command); screenshots go to `PELLMON_SHOTS_DIR` (default `tests/browser/_shots/`). Keep the existing "must not do" bullets unchanged.

    In `.gitignore` add `tests/browser/_shots/` under the existing "Python 3 migration additions" area.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PELLMON_BROWSER_TESTS" tests/README.md` matches; `grep -n "allow-unix-socket\|addopts" tests/README.md` matches the recorded flag.
    - `grep -n "Do not add \`@pytest.mark.enable_socket\`" tests/README.md` still matches (existing rule kept).
    - `grep -n "tests/browser/_shots/" .gitignore` matches.
    - Full Windows suite: only the 3 known baseline failures (test_backup_script::test_conf_d_overrides_pellmon_conf, test_plugin_loader::test_every_available_plugin_loads[consumption|silolevel]).
  </acceptance_criteria>
  <done>Browser test policy is documented next to the socket guardrail rules, and local screenshots never get committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> PyPI / Playwright CDN | CI downloads playwright and a Chromium build on every run |
| CI test job -> publish job | A green test job authorizes a Docker Hub release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | pip install -r requirements-browser.txt in CI | mitigate | Exact `==` pin human-verified in Plan 01 Task 1; guard test asserts the pin and that playwright is absent from requirements.txt and Dockerfile |
| T-10-12 | Elevation of Privilege | publish gating | mitigate | Browser run is a step of `test`; `needs: test` unchanged and still asserted |
| T-10-13 | Repudiation (silent skip) | CI browser step | mitigate | `PELLMON_BROWSER_TESTS: "1"` turns missing prerequisites into failures; `-rs` shows any skip reasons; guard test asserts both |
| T-10-14 | Information Disclosure | screenshot artifact | accept | Screenshots show stub data only (no real burner data or credentials); 7-day retention |
</threat_model>

<verification>
- `pytest tests/test_ci_docker_config.py -v` all green (old and new tests).
- Full Windows suite only the 3 baseline failures.
</verification>

<success_criteria>
- D-06(b) is enforced by CI and protected by a guard test; documentation explains the socket-flag exception.
</success_criteria>

<output>
Create `.planning/phases/10-optimize-for-mobile-devices-screen-size/10-06-SUMMARY.md` when done.
</output>
