---
phase: 10-optimize-for-mobile-devices-screen-size
plan: 07
type: execute
wave: 4
depends_on: ["10-05", "10-06"]
files_modified: []
autonomous: false
requirements: [D-02, D-03, D-04, D-05, D-06, D-07]

must_haves:
  truths:
    - "Before handing over, every automated gate is green locally: structural tests, full suite at the known baseline, and the browser suite where a browser can launch"
    - "CI on the pull request is green including the mandatory browser step, and its screenshot artifact exists"
    - "The user has confirmed on a real phone via https://stoker.schoeler.pro/ that the dashboard, graph controls, events toggle, Parameters sections and Settings 2-up gallery work with no sideways scroll"
    - "The user has confirmed the desktop dashboard now shows side-by-side widget rows and otherwise looks as before (D-07)"
  artifacts: []
  key_links:
    - from: "pull request CI run"
      to: "tests/browser"
      via: "Run mobile layout browser tests step with PELLMON_BROWSER_TESTS=1"
      pattern: "PELLMON_BROWSER_TESTS"
---

<objective>
Run the final automated phase gate, then hand over to the user for the D-06(c) real-device check (plus the D-07 desktop confirmation) on the live Raspberry Pi deployment.

Purpose: CI covers Chromium only; iOS/Android Safari/Chrome rendering, the `aspect-ratio` on `<object>` (RESEARCH A5) and the actual feel of the tap targets need a human on a real phone.
Output: A gate report in the SUMMARY and the user's approval (or a list of issues that become gap-closure plans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-CONTEXT.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-UI-SPEC.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-VALIDATION.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-05-SUMMARY.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated phase gate</name>
  <files>(none modified; report goes in the SUMMARY)</files>
  <read_first>
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-VALIDATION.md
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-01-SUMMARY.md (Spike result, BROWSER_PYTEST_FLAGS)
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-05-SUMMARY.md
  </read_first>
  <action>
    Run, in order, and paste the pass/fail counts into the SUMMARY:
    1. Quick structural run: `PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_css.py tests/Pellmonweb/test_mobile_dashboard.py tests/Pellmonweb/test_mobile_forms.py tests/Pellmonweb/test_mobile_stub_guard.py tests/test_ci_docker_config.py -v` (must be all green).
    2. Full Windows suite: `PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v`; the only allowed failures are the 3 known baseline ones (test_backup_script::test_conf_d_overrides_pellmon_conf, test_plugin_loader::test_every_available_plugin_loads[consumption], [silolevel]).
    3. Browser suite in the WSL scratch venv with the recorded flag: `wsl.exe -e bash -lc 'cd /mnt/d/Antigravity/PellMon-master && PELLMON_BROWSER_TESTS=1 PYTHONPATH=src $HOME/pellmon-browser-venv/bin/python -m pytest tests/browser -v -rs --allow-unix-socket'` (substitute the recorded flag if different). If chromium cannot launch in WSL, record that CI is the authority.
    4. Hygiene greps: no new `.py2bak` files (`git status --porcelain | grep py2bak` empty); `grep -rn "visible-xs-block" src/Pellmonweb/html` empty; `grep -rn "height:400px" src/Pellmonweb/html src/Pellmonsrv/plugins/consumption src/Pellmonsrv/plugins/silolevel | grep -v __pycache__` empty; `grep -c "overflow-x" src/Pellmonweb/media/css/pellmon.css` = 0; `grep -c playwright Dockerfile requirements.txt` reports 0 for both (test tooling stays out of the image).
    Do not push, open a PR, or touch master; the user owns the branch/PR flow.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains the counts for runs 1-3 and the results of all hygiene greps.
    - Run 1 all green; run 2 failures are exactly the 3 baseline ones; run 3 has 0 failed and 0 skipped, or CI-only is recorded with the reason from 10-01-SUMMARY.
  </acceptance_criteria>
  <done>All local automated evidence is collected and green before the human check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-phone check through stoker.schoeler.pro plus desktop confirmation (D-06(c), D-07)</name>
  <files>(none - verification only; no files are modified by this task)</files>
  <read_first>.planning/phases/10-optimize-for-mobile-devices-screen-size/10-07-SUMMARY.md (Task 1 gate report, draft)</read_first>
  <action>
    Pause and hand control to the developer. Do not push, open a PR, merge, or deploy yourself. Present the Task 1 gate results and the how-to-verify steps below verbatim, wait for the resume signal, and record the reply (approval or failing step numbers) in the SUMMARY.
  </action>
  <what-built>Responsive PellMon UI: single-column phone dashboard with collapsed events, CSS-sized graph and charts (260/320/400px), 44px graph/navbar/form targets, collapsible Parameters sections with two-up tag pills, two-up Settings gallery, restored side-by-side desktop rows (D-07), and a mandatory headless-browser layout step in CI with screenshot artifacts.</what-built>
  <how-to-verify>
    A. CI and release
    1. Push the current branch and open a pull request to master (your usual flow). On the PR's CI run, confirm the "Run mobile layout browser tests" step is green with 0 skipped, and download the "mobile-layout-screenshots" artifact to glance at the 390/768/1280 shots.
    2. Merge. The publish job releases a new image (this phase's commits include fix/feat prefixes).
    3. On the Pi, pull the new image and recreate BOTH containers (`docker compose pull && docker compose up -d --force-recreate`). Both are needed: the consumption/silo chart templates come from the daemon container and the web process caches them (RESEARCH Pitfall 9).
    B. On your phone, open https://stoker.schoeler.pro/ (log in through Pangolin), then hard-refresh:
    4. Main page: no sideways scrolling anywhere; order is System, Events, Graph, Consumption, Silo level; the burner diagram fills the width and is readable.
    5. Events shows a short preview and a "Show all events" button; tapping expands it and the button reads "Show fewer events"; new events keep appearing in both states.
    6. Graph is shorter (about 260px), line and time links are easy to tap and wrap onto lines, Back/Forward sit side by side and work; Autorefresh toggles.
    7. Hamburger menu opens, links are easy to tap.
    8. Parameters: tags are two per row; Control and Data open, Settings closed with a chevron; tapping Settings opens it; tapping a setting shows a full-width input and a full-width Set button; setting a harmless value still works.
    9. Settings: gallery shows two tiles per row; Save image button is full width; saving an image still works.
    10. Consumption page and View log: no sideways scroll.
    11. Rotate to landscape (roughly tablet width): events fully shown without the button, Parameters sections all open.
    C. On a desktop browser (wide window, >= 992px):
    12. Main page: System and Events are side by side, Consumption and Silo level are side by side (this is the intended D-07 change); graph 400px tall; everything else looks as before.
    13. Parameters: sections look like plain headings as before, Data and Settings side by side, no chevrons.
  </how-to-verify>
  <verify>
    <human-check>Developer completes steps 1-13 on the PR CI run, a real phone and a desktop browser</human-check>
  </verify>
  <acceptance_criteria>
    - User replies "approved", or lists issues by step number (issues become gap-closure plans via /gsd-plan-phase 10 --gaps).
  </acceptance_criteria>
  <done>User approval (or the list of failing steps) is recorded in the SUMMARY.</done>
  <resume-signal>Type "approved" or list failing step numbers with what you saw</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> Pangolin auth proxy -> pellmonweb | The phone check goes through the existing authenticated reverse proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Elevation of Privilege | release pipeline | mitigate | Release only after the PR's test job (including the browser step) is green and the user merges; no direct push to master by the executor |
| T-10-16 | Spoofing | live check via stoker.schoeler.pro | accept | Existing Pangolin auth in front of the app is unchanged by this phase |
</threat_model>

<verification>
- Task 1 gate report in the SUMMARY; user approval recorded.
</verification>

<success_criteria>
- D-06(a), D-06(b) green; D-06(c) and D-07 confirmed by the user on real devices.
</success_criteria>

<output>
Create `.planning/phases/10-optimize-for-mobile-devices-screen-size/10-07-SUMMARY.md` when done.
</output>
