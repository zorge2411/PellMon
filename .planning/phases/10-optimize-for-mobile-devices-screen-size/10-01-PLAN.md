---
phase: 10-optimize-for-mobile-devices-screen-size
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements-browser.txt
  - tests/browser/conftest.py
  - tests/browser/fake_dbus.py
  - tests/browser/plugin_templates.py
  - tests/browser/stub_server.py
  - tests/browser/test_browser_harness.py
  - tests/Pellmonweb/test_mobile_stub_guard.py
autonomous: false
requirements: [D-06]

must_haves:
  truths:
    - "The Playwright package pin has been verified as the genuine Microsoft package by a human before it is installed anywhere"
    - "Whether Playwright's sync client can start under pytest.ini's --disable-socket with --allow-unix-socket is known from a real run, and the chosen pytest flag is recorded in the SUMMARY"
    - "A stub web server that uses the real PellMonWeb handlers over a fake D-Bus serves /, /parameters/Overview, /settings/, /consumptionview/consumption, /logview/logView and /auth/login without a daemon"
    - "Browser tests skip with a reason when PELLMON_BROWSER_TESTS is unset and fail (not skip) on any missing prerequisite when it is 1"
    - "The full Windows suite still shows only the 3 known baseline failures"
  artifacts:
    - path: "requirements-browser.txt"
      provides: "Test-only Playwright pin, kept out of the Docker image"
      contains: "playwright==1.63.0"
    - path: "tests/browser/conftest.py"
      provides: "Env gate, stub-server session fixture, Playwright browser fixture, per-width page factory, CDN jQuery route"
      contains: "PELLMON_BROWSER_TESTS"
    - path: "tests/browser/stub_server.py"
      provides: "Subprocess CherryPy server mounting real PellMonWeb handlers over FakeDbus"
      contains: "READY port="
    - path: "tests/browser/fake_dbus.py"
      provides: "Canned getFullDB/getItem/getdb/getMenutags/getPlugins/get_setting"
      contains: "class FakeDbus"
    - path: "tests/Pellmonweb/test_mobile_stub_guard.py"
      provides: "AST drift guard: every global assigned in pellmonweb.run() is set by the stub"
      contains: "ast.Global"
  key_links:
    - from: "tests/browser/conftest.py"
      to: "tests/browser/stub_server.py"
      via: "subprocess.Popen with stdout pipe, reads READY port=N"
      pattern: "READY port="
    - from: "tests/browser/stub_server.py"
      to: "src/Pellmonweb/pellmonweb.py"
      via: "import Pellmonweb.pellmonweb and assign module globals, mount PellMonWeb subclass"
      pattern: "PellMonWeb"
    - from: "tests/browser/fake_dbus.py"
      to: "src/Pellmonsrv/plugins/*/templates and _insert_template strings"
      via: "plugin_templates.py lookup used by getPlugins"
      pattern: "_insert_template"
---

<objective>
Build the headless-browser test harness for D-06(b) and settle the one unverified risk first: can Playwright run inside this suite's `--disable-socket` guardrail.

Purpose: Every later browser assertion (Plan 05) and the CI step (Plan 06) sit on this harness. The spike result decides which pytest flag CI uses.
Output: `requirements-browser.txt`, `tests/browser/` harness (gate + fixtures + stub server + fake D-Bus + plugin-template lookup + harness smoke tests), and a Windows-safe stub drift guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-CONTEXT.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-PATTERNS.md
@CLAUDE.md
@pytest.ini
@tests/README.md
@tests/Pellmonweb/test_settings_page.py

<interfaces>
From src/Pellmonweb/pellmonweb.py (read the file for exact line numbers; summary only):
- `class PellMonWeb` (line ~250): exposed handlers `index`, `parameters(t1..t4, **args)`, `export(**args)`, `systemimage`, `getparamlist`, child apps for consumptionview, logview, auth, settings (see `run()` near line 1000-1050 for how `app_conf` and `/media` staticdir are configured).
- `class myLookup(TemplateLookup)`: `__init__(self, directories, dbus=None)`; `get_template(uri)` falls back to `self.dbus.getPlugins(uri)` then `put_string(uri, plugin)`.
- `resolve_connection_state(dbus_handler)`: calls `getItem('burner_connection')` and `getItem('burner_connection_reason')`.
- `parameters()` classifies `getFullDB` items by `item['type']` in {'R', 'R/W', 'W'} (lines ~575-615) and calls `getMenutags()`.
- `run()` declares `global` for: dbus, lookup, polling, db, colorsDict, polldata, graph_lines, logtick, credentials, logfile, system_image, system_image_dir, frontpage_widgets, timeChoices, timeNames, timeSeconds (also read websockets / consumption_graph handling near lines 985-995; enumerate from the AST, do not trust this list).
- `plugin_categories.protocols._insert_template(self, name, template)` is how plugins register widget templates (consumption, silolevel).

From tests/Pellmonweb/test_settings_page.py lines ~78-109: the established technique of `pytest.importorskip("gi")`, then setting `web.dbus = _FakeDbus()` and `web.system_image_dir`/`web.system_image` module globals.

pytest.ini: `pythonpath = src`, `testpaths = tests`, `addopts = --disable-socket`. tests/README.md forbids removing `--disable-socket` and forbids `@pytest.mark.enable_socket`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the playwright PyPI package before it is pinned or installed</name>
  <files>(none - verification only; no files are modified by this task)</files>
  <read_first>.planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md (section "Package Legitimacy Audit")</read_first>
  <action>
    Pause and hand control to the developer before anything installs playwright. Do not create `requirements-browser.txt` or run any `pip install` yet. Present the how-to-verify steps below verbatim and wait for the resume signal. If the developer approves a different pin, use that exact pin in Task 2 and every later reference.
  </action>
  <what-built>Nothing yet. RESEARCH.md tags `playwright` as [ASSUMED] because slopcheck could not run. This gate must be passed by a human before any `pip install playwright` happens, locally or in CI. It is never auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/playwright/ and confirm version 1.63.0 exists and "Requires: Python >=3.10".
    2. Confirm the "Project links" / homepage point to https://github.com/microsoft/playwright-python and the maintainers are Microsoft accounts.
    3. Confirm https://playwright.dev/python/docs/intro documents `pip install playwright` then `playwright install`.
    4. If any of these do not match, reply with the discrepancy; the plan stops and the harness is re-planned.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms the PyPI project page, maintainer and repository for the playwright pin</human-check>
  </verify>
  <acceptance_criteria>
    - Human replied "approved" (or named a different verified pin to use instead of 1.63.0).
  </acceptance_criteria>
  <done>The playwright pin is human-verified as the genuine Microsoft package.</done>
  <resume-signal>Type "approved" (optionally "approved, use X.Y.Z") or describe the discrepancy</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin playwright, write the env-gated browser fixtures, and run the --disable-socket spike</name>
  <files>requirements-browser.txt, tests/browser/conftest.py, tests/browser/test_browser_harness.py</files>
  <read_first>
    - pytest.ini
    - tests/README.md
    - tests/conftest.py (fixture and docstring style)
    - requirements-dev.txt (header style to copy)
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md (Pitfall 1, Pitfall 3, Pattern 2, "Browser gate fixture")
  </read_first>
  <behavior>
    - With PELLMON_BROWSER_TESTS unset, every test in tests/browser that uses the browser or stub fixtures is SKIPPED with a reason mentioning PELLMON_BROWSER_TESTS=1 (Windows full suite stays green).
    - With PELLMON_BROWSER_TESTS=1 and playwright missing, the fixture calls pytest.fail (never skip).
    - test_playwright_starts_under_socket_guard: sync_playwright().start() then .stop() succeeds in the test process.
    - test_chromium_launches_and_renders_about_blank: chromium launches headless and page.evaluate("1+1") == 2.
  </behavior>
  <action>
    Create `requirements-browser.txt` with the same comment header style as `requirements-dev.txt` ("Test/CI-only dependencies -- NOT installed in production Docker image") and the single pin `playwright==1.63.0` (or the pin the human approved in Task 1). Do not add it to `requirements.txt` or `requirements-dev.txt`; the Dockerfile must never see it.

    Create `tests/browser/conftest.py` (one-line module docstring, no GPL header, no `__init__.py` in the directory, untyped). Provide:
    - Module constants: `ROOT` (repo root via `Path(__file__).resolve().parents[2]`), `SRC`, `MEDIA` (`src/Pellmonweb/media`), `JQUERY_MIN` (`MEDIA/jquery/jquery.min.js`), `WIDTHS = {390: (390, 844), 768: (768, 1024), 1280: (1280, 900)}`, `SHOTS_DIR` from env `PELLMON_SHOTS_DIR` defaulting to `tests/browser/_shots`.
    - A helper `_unavailable(msg)` that calls `pytest.fail(msg)` when `os.environ.get("PELLMON_BROWSER_TESTS") == "1"`, else `pytest.skip(msg)`.
    - Session fixture `playwright_rt`: if the env var is not "1", `pytest.skip("set PELLMON_BROWSER_TESTS=1 to run headless-browser layout tests")`; import `playwright.sync_api.sync_playwright` inside the fixture (never at module level, so Windows collection works without the package), `_unavailable` on ImportError; start it, yield, stop.
    - Session fixture `browser` (depends on `playwright_rt`): `chromium.launch(headless=True)`, `_unavailable("chromium not launchable: %s" % e)` on failure, close at teardown.
    - Task 3 adds the `stub_url` session fixture to this same file; until then `page_at` takes an absolute URL or `about:blank`.
    - Function-scoped factory fixture `page_at(browser)` returning a callable `open_page(path, width)` that creates a context with `viewport={"width": w, "height": h}` from `WIDTHS`, and for width 390 also `device_scale_factor=2, is_mobile=True, has_touch=True`; for 768 `has_touch=True`; routes `"**/ajax.googleapis.com/**"` to `route.fulfill(path=JQUERY_MIN, content_type="application/javascript")` so CI never loads the CDN; navigates to the stub URL + path with `wait_until="load"`; returns the page. Close all contexts created by the factory at teardown.
    - Helper `shot(page, name)` that writes a full-page PNG into `SHOTS_DIR` (mkdir parents) named `<name>.png`.
    Use `%`-formatting in messages (repo convention); f-strings are acceptable in test tooling but keep one style per file.

    Create `tests/browser/test_browser_harness.py` with the two browser tests from the behavior block (they use `playwright_rt` / `browser`, not the stub).

    Spike (the decision this plan exists for): in WSL, build a scratch venv that mirrors CI, outside the repo, without sudo:
    `wsl.exe -e bash -lc 'python3 -m venv --system-site-packages $HOME/pellmon-browser-venv && $HOME/pellmon-browser-venv/bin/pip install -r /mnt/d/Antigravity/PellMon-master/requirements.txt pytest pytest-mock pytest-socket -r /mnt/d/Antigravity/PellMon-master/requirements-browser.txt && $HOME/pellmon-browser-venv/bin/python -m playwright install chromium'`
    (system python3 in WSL has gi/dbus; `venv-wsl` does not, so do not use `venv-wsl`). Then run
    `wsl.exe -e bash -lc 'cd /mnt/d/Antigravity/PellMon-master && PELLMON_BROWSER_TESTS=1 PYTHONPATH=src $HOME/pellmon-browser-venv/bin/python -m pytest tests/browser/test_browser_harness.py -v -rs --allow-unix-socket'`.
    Record in the SUMMARY under a heading "Spike result":
    - If `test_playwright_starts_under_socket_guard` passes: `BROWSER_PYTEST_FLAGS = --allow-unix-socket`.
    - If it fails with SocketBlockedError: re-run with `-o addopts=""` instead; if that passes record `BROWSER_PYTEST_FLAGS = -o addopts=""`. Never use `--force-enable-socket` (ci.yml guard forbids `--force`) and never add an `enable_socket` marker.
    - If only the chromium launch test fails because of missing shared libraries (no sudo for `playwright install-deps` in WSL), record the exact missing library error and state "chromium launch is verified in CI only (`playwright install --with-deps` runs there)". This does not block the plan; the socket decision is answered by the first test.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/browser -v -rs</automated>
    <automated>wsl.exe -e bash -lc 'cd /mnt/d/Antigravity/PellMon-master && PELLMON_BROWSER_TESTS=1 PYTHONPATH=src $HOME/pellmon-browser-venv/bin/python -m pytest tests/browser/test_browser_harness.py -v -rs --allow-unix-socket'</automated>
  </verify>
  <acceptance_criteria>
    - `requirements-browser.txt` contains exactly one non-comment line matching `^playwright==\d+\.\d+\.\d+$`; `grep -c playwright requirements.txt requirements-dev.txt Dockerfile` reports 0 for each.
    - On Windows (`venv-py3`, env var unset) `pytest tests/browser -v -rs` exits 0 and every browser test is reported SKIPPED with "PELLMON_BROWSER_TESTS=1" in the reason.
    - `grep -n "enable_socket\|force-enable-socket" tests/browser/*.py` returns nothing.
    - `grep -n "^from playwright\|^import playwright" tests/browser/conftest.py` returns nothing (import only inside the fixture).
    - SUMMARY contains a "Spike result" section stating the exact `BROWSER_PYTEST_FLAGS` value and the WSL test output lines for both harness tests.
  </acceptance_criteria>
  <done>Harness fixtures exist, skip cleanly on Windows, and the socket-guard question is answered by a real run with the chosen flag recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stub server with real handlers over FakeDbus, plugin-template lookup, and the drift guard</name>
  <files>tests/browser/stub_server.py, tests/browser/fake_dbus.py, tests/browser/plugin_templates.py, tests/browser/conftest.py, tests/browser/test_browser_harness.py, tests/Pellmonweb/test_mobile_stub_guard.py</files>
  <read_first>
    - src/Pellmonweb/pellmonweb.py (entire `run()`, `PellMonWeb.index`, `parameters`, `export`, `settings`-related handlers, `myLookup`, `resolve_connection_state`)
    - src/Pellmonweb/consumption.py and src/Pellmonweb/logview.py (item names and routes the consumption and log pages call)
    - src/Pellmonsrv/plugins/consumption/__init__.py (the `_insert_template('consumption', ...)` string, line ~87) and src/Pellmonsrv/plugins/silolevel/__init__.py (line ~109)
    - src/Pellmonsrv/plugins/consumption/templates/consumption7d (script that fetches bar data; copy the expected JSON shape)
    - tests/Pellmonweb/test_settings_page.py (fake-dbus-on-module-globals idiom, `_class`/`_method` AST helpers)
    - tests/browser/conftest.py (from Task 2)
  </read_first>
  <behavior>
    - test_mobile_stub_guard (runs on Windows, no gi import): parse src/Pellmonweb/pellmonweb.py with ast, collect every name in every ast.Global inside FunctionDef `run`, and assert each name appears as an assignment target `web.<name> =` in tests/browser/stub_server.py text; message names the missing globals.
    - test_mobile_stub_guard: plugin_templates.find_template("consumption") and find_template("silolevel") return non-empty strings, and find_template("consumption7d") returns the file text of plugins/consumption/templates/consumption7d.
    - test_stub_serves_key_pages (browser-gated): for each of the six paths, page.goto returns status 200 and the page contains `.navbar`.
  </behavior>
  <action>
    Create `tests/browser/plugin_templates.py`: function `find_template(name)` that first returns the text of any file `src/Pellmonsrv/plugins/*/templates/<name>` (read with `encoding="utf-8"`), else walks `src/Pellmonsrv/plugins/*/__init__.py` with `ast.parse` and returns `args[1].value` of the first `ast.Call` whose `func` is an `ast.Attribute` with `attr == "_insert_template"`, `args[0]` an `ast.Constant` equal to `name` and `args[1]` an `ast.Constant` str; else raises `KeyError(name)`. No plugin import (plugins need `grp`/`pwd`).

    Create `tests/browser/fake_dbus.py` with `class FakeDbus` implementing exactly what the real handlers call (derive from the read_first files, do not guess):
    - `getItem(name)`: `'connected'` for `burner_connection`, `''` for `burner_connection_reason`, canned flot/bar JSON strings for the consumption and silo data items the consumption/silolevel templates fetch (same shape the real daemon returns; read consumption.py and the template scripts), a numeric string for everything else.
    - `setItem(name, value)`: return `'OK'`.
    - `getdb()`: list of all item names used below.
    - `getFullDB(tags)`: for the dashboard, items whose `name` match the stub's `graph_lines` names with long `label` values; for Parameters, about 12 `type` 'R' items, 8 'R/W' items (some with `get_enum_list` of 3 tuples, some with `min`/`max`), and 4 'W' commands, each with a long `longname` (40+ chars), a `description`, `unit`; include every key `parameters()` reads.
    - `getMenutags()`: about 8 tags including `Overview` and at least two long ones (e.g. `Blower settings`, `Oxygen regulation`).
    - `getPlugins(name)`: `plugin_templates.find_template(name)`.
    - `get_setting(key)`: `'system.svg'`.
    - One log-related fixture: a module-level `write_sample_log(path)` that writes about 40 realistic log lines plus one line containing a 200-character token with no spaces (overflow stress for `#lines`), in the format `logview.py` parses.

    Create `tests/browser/stub_server.py` (a script, not collected: name does not start with `test_`). It inserts `src` and its own directory on `sys.path`, imports `Pellmonweb.pellmonweb as web`, and assigns every global that `run()` declares, as `web.<name> = ...` statements (one per name, so the drift guard can grep them): `websockets = False` (forces `setupPolling`, no ws upgrade), `dbus = FakeDbus()`, `lookup = web.myLookup(directories=[<html dir>], dbus=web.dbus)`, `polling = True`, `db = ""`, `logtick = None`, `consumption_graph = True`, `credentials = []`, `logfile = <temp file written by write_sample_log>`, `system_image_dir = <media>/img`, `system_image = <media>/img/system.svg`, `frontpage_widgets = [["systemimage", "events"], ["graph"], ["consumption7d", "silolevel"]]`, `timeChoices`/`timeNames`/`timeSeconds` exactly as `run()` builds them (timeNames with spaces replaced by `&nbsp;`), `polldata = []`, `colorsDict = {}` (or matching entries), `graph_lines` = 8 dicts with `name`, `color`, `ds_name` (names matching FakeDbus items), plus any other global the AST lists. Subclass `web.PellMonWeb` as `StubWeb` overriding only `export` (exposed) to return a canned flot JSON string for the selected lines with `Content-Type: application/json`. Mirror `run()`'s mount config for `/media` staticdir and any child apps, with `tools.sessions.on: True`, `tools.auth.on: False` (test-only; the stub binds 127.0.0.1 only), `log.screen: False`, `engine.autoreload.on: False`, `server.socket_host: "127.0.0.1"`. Port: try `server.socket_port: 0` and read the bound port from `cherrypy.server.httpserver.socket.getsockname()[1]` after `engine.start()`; if CherryPy rejects port 0, bind a throwaway socket to `("127.0.0.1", 0)` in the stub, read the port, close it, then configure. Print `READY port=%d` with `flush=True`, then `cherrypy.engine.block()`.

    Append to `tests/browser/conftest.py` a session fixture `stub_url(playwright_rt)`: spawn `[sys.executable, <stub_server.py>]` with `env` = `os.environ` plus `PYTHONPATH` prefixed by `src`, `stdout=PIPE`, `stderr=STDOUT`, text mode; read lines until one starts with `READY port=` (deadline 30 s; on timeout or early exit, `_unavailable` with the collected output); yield `"http://127.0.0.1:%d" % port`; terminate and wait at teardown. The test process never opens a TCP socket itself (pytest-socket stays enforced). Make `page_at` use `stub_url` for relative paths.

    Add `test_stub_serves_key_pages` to `tests/browser/test_browser_harness.py` over the six paths `/`, `/parameters/Overview`, `/settings/`, `/consumptionview/consumption`, `/logview/logView`, `/auth/login` at width 390.

    Create `tests/Pellmonweb/test_mobile_stub_guard.py` (Windows-safe: imports only `ast`, `pathlib`, `sys`; loads `plugin_templates.py` via `sys.path.insert(0, str(ROOT / "tests" / "browser"))`) with the two guard tests from the behavior block. Must not import `Pellmonweb.pellmonweb`.

    Run the stub pages test in the WSL scratch venv with the flag recorded in Task 2. If chromium cannot launch in WSL, instead run the stub standalone there (`$HOME/pellmon-browser-venv/bin/python tests/browser/stub_server.py` in the background, wait for READY, then fetch each path with `curl -s -o /dev/null -w "%{http_code}"`, then kill it) and record the six status codes in the SUMMARY.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_stub_guard.py tests/browser -v -rs</automated>
    <automated>wsl.exe -e bash -lc 'cd /mnt/d/Antigravity/PellMon-master && PELLMON_BROWSER_TESTS=1 PYTHONPATH=src $HOME/pellmon-browser-venv/bin/python -m pytest tests/browser/test_browser_harness.py -v -rs --allow-unix-socket'</automated>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Pellmonweb/test_mobile_stub_guard.py` passes on Windows and contains no `import Pellmonweb` / `from Pellmonweb` line.
    - `grep -c "tools.auth.on" tests/browser/stub_server.py` >= 1 and `grep -n "127.0.0.1" tests/browser/stub_server.py` shows the bind host.
    - `grep -n "^web\.dbus = \|^web\.frontpage_widgets = \|^web\.websockets = False" tests/browser/stub_server.py` shows all three.
    - SUMMARY records either the passing `test_stub_serves_key_pages` output or six HTTP 200 codes from the standalone stub in WSL.
    - Full Windows suite: failures limited to the known baseline 3 (test_backup_script::test_conf_d_overrides_pellmon_conf, test_plugin_loader::test_every_available_plugin_loads[consumption], [silolevel]); no new failures, browser tests skipped.
  </acceptance_criteria>
  <done>A stub server built from the real handlers serves all six pages to a headless browser, and a Windows-safe guard fails loudly if run() gains a global the stub does not set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> test environment | A new third-party package (playwright) and a downloaded browser binary enter local and CI test runs |
| Test stub -> network | The stub CherryPy server listens on a TCP port with auth disabled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | pip install playwright | mitigate | Task 1 blocking human legitimacy check; exact pin `playwright==1.63.0` in a separate `requirements-browser.txt` |
| T-10-01 | Elevation of Privilege | tests/browser/stub_server.py (`tools.auth.on: False`) | mitigate | Binds `127.0.0.1` only, lives under `tests/` (excluded by `.dockerignore`), never imported by `src/` |
| T-10-02 | Tampering | CDN jQuery during tests | mitigate | `page_at` routes `ajax.googleapis.com` to the vendored `media/jquery/jquery.min.js` |
| T-10-03 | Tampering | pytest-socket guardrail | mitigate | No `enable_socket` marker, no `--force-enable-socket`; test process opens no TCP socket (port read from stdout pipe) |
</threat_model>

<verification>
- Windows: `PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v` shows only the 3 baseline failures; all `tests/browser` tests skipped with the env-var reason.
- WSL scratch venv: harness tests run with the recorded `BROWSER_PYTEST_FLAGS`.
</verification>

<success_criteria>
- Playwright pin human-verified, harness in place, spike decision recorded in the SUMMARY, stub drift guard green on Windows.
</success_criteria>

<output>
Create `.planning/phases/10-optimize-for-mobile-devices-screen-size/10-01-SUMMARY.md` when done (must include the "Spike result" section with `BROWSER_PYTEST_FLAGS`).
</output>
