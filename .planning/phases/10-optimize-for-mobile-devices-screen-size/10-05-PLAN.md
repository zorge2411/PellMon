---
phase: 10-optimize-for-mobile-devices-screen-size
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-03", "10-04"]
files_modified:
  - tests/browser/test_mobile_overflow.py
  - tests/browser/test_mobile_interactions.py
  - tests/browser/fake_dbus.py
  - tests/browser/conftest.py
autonomous: true
requirements: [D-02, D-03, D-04, D-05, D-06, D-07]

must_haves:
  truths:
    - "A headless browser renders the six key pages at 390px and 768px and fails if anything scrolls or sticks out sideways, or if overflow is hidden on body/html"
    - "At 390px the dashboard order, system image size, collapsed events with working toggle, 260px graph and charts, and 44px graph and navbar targets are asserted"
    - "At 390px Parameters pills are two per row, the Settings section starts collapsed and opens on tap, and form controls are full width and 44px tall"
    - "At 390px and 768px the Settings gallery shows two tiles per row and Save image is at least 44px tall"
    - "At 768px events are fully shown without a toggle, the graph is 320px, widgets are single column, and Parameters sections are all open without chevrons"
    - "At 1280px the graph is 400px, dashboard rows are side by side (D-07) and Parameters Data/Settings sit side by side"
    - "Every browser test writes a full-page screenshot for review"
  artifacts:
    - path: "tests/browser/test_mobile_overflow.py"
      provides: "A1-A3 across 6 pages x 2 widths, plus 1280 desktop smoke incl. D-07"
      contains: "OVERFLOW_JS"
    - path: "tests/browser/test_mobile_interactions.py"
      provides: "B1-B7, C1-C3, D1-D2, E1-E3"
      contains: "def test_"
  key_links:
    - from: "tests/browser/test_mobile_overflow.py"
      to: "tests/browser/conftest.py"
      via: "page_at(path, width) fixture over the stub server"
      pattern: "page_at"
    - from: "tests/browser/test_mobile_interactions.py"
      to: "src/Pellmonweb/media/js/index.js"
      via: "clicking .events-toggle exercises the delegated handler"
      pattern: "events-toggle"
---

<objective>
Write the rendered acceptance tests (UI-SPEC A1-A3, B1-B7, C1-C3, D1-D2, E1-E3 and the 1280 smoke) on top of the Plan 01 harness, and run them against the Plan 02-04 implementation.

Purpose: D-06(b). These tests are the authority on "no horizontal scroll" (D-02) and on the rendered sizes that structural tests cannot prove.
Output: `tests/browser/test_mobile_overflow.py`, `tests/browser/test_mobile_interactions.py` (and FakeDbus/conftest tweaks if a page needs more canned data).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-UI-SPEC.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-01-SUMMARY.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-03-SUMMARY.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-04-SUMMARY.md
@CLAUDE.md

<interfaces>
From tests/browser/conftest.py (Plan 01): fixtures `playwright_rt`, `browser`, `stub_url`, `page_at` (callable `open_page(path, width)` returning a loaded Playwright page; widths 390/768/1280 from `WIDTHS`; CDN jQuery routed to the vendored copy), helper `shot(page, name)`, constant `SHOTS_DIR`. Gate: skip unless PELLMON_BROWSER_TESTS=1, fail on missing prerequisites when it is 1. Pytest flag for the browser run: the `BROWSER_PYTEST_FLAGS` value recorded in 10-01-SUMMARY "Spike result" (expected `--allow-unix-socket`).

Markup hooks from Plans 03/04: `#systemimage`, `#events-wrap.events-collapsed`, `.events-toggle` (data-label-*), `#lines`, `#graph.pellmon-graph`, `.pellmon-chart` (ids consumption7d, silolevel on the dashboard; consumption24h/7d/8w/1y on /consumptionview/consumption), `.graph-lines a.lineselection`, `.graph-times a.timeChoice`, `a.autorefresh`, `.graph-nav .btn`, `.navbar-toggle`, `.navbar-collapse`, `.navbar-nav > li > a`, `ul.param-tags > li > a`, `a.param-section-toggle`, `#param-control`, `#param-data`, `#param-settings`, `.details`, `.form-control`, `input[type=submit]`, `.sysimg-tile`, `.sysimg-save`.

Waits (RESEARCH Pattern 4): never `networkidle` or fixed sleeps. Dashboard: `page.wait_for_selector("#graph canvas")`, `#consumption7d canvas`, `#silolevel canvas`, and `page.wait_for_function` for `document.getElementById('systemimage').contentDocument.querySelector('svg')`. Collapse animations (~350ms): wait with `page.wait_for_function` on the `in` class / computed height, not immediate reads.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overflow tests A1-A3 on six pages at 390 and 768, plus 1280 desktop smoke (D-07)</name>
  <files>tests/browser/test_mobile_overflow.py, tests/browser/fake_dbus.py, tests/browser/conftest.py</files>
  <read_first>
    - tests/browser/conftest.py
    - tests/browser/fake_dbus.py
    - tests/browser/stub_server.py
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-01-SUMMARY.md (Spike result)
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md (Pattern 3 overflow probe, Pattern 4 waits)
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-UI-SPEC.md (Rendered acceptance A1-A3 and the 1280 smoke)
  </read_first>
  <behavior>
    - test_no_horizontal_overflow[path-width] for path in (/, /parameters/Overview, /settings/, /consumptionview/consumption, /logview/logView, /auth/login) x width in (390, 768): documentElement.scrollWidth <= width and body.scrollWidth <= width (compare to the intended constant, not window.innerWidth, RESEARCH Pattern 3); offenders list empty (message lists them); viewport meta present; computed overflowX of body and html is not "hidden".
    - test_desktop_smoke_1280: on / the #graph offsetHeight is 400 (+/-1); #systemimage's column and the events column have equal top (+/-1) and different left (side by side, D-07); .events-toggle is not visible. On /parameters/Overview #param-data and #param-settings are visible with equal top (+/-1) and different left.
  </behavior>
  <action>
    Create `tests/browser/test_mobile_overflow.py` (one-line docstring, untyped). Define `OVERFLOW_JS` as the probe from RESEARCH Pattern 3 (returns scrollW, bodyScrollW, overflowX of body/html, viewport-meta presence, offenders excluding elements clipped by an in-bounds ancestor with overflow-x auto/hidden/scroll). Parametrize `test_no_horizontal_overflow` over the 6 paths x (390, 768) with readable ids; for each: open via `page_at`, wait for the page's concrete readiness signal (dashboard: `#graph canvas`, SVG contentDocument; consumption page: one chart canvas; others: `.navbar`), call `shot(page, "overflow-<slug>-<width>")` before asserting (so a failure still leaves a screenshot), evaluate the probe with the width, assert the A1-A3 conditions with messages naming the check, page and offenders. Add `test_desktop_smoke_1280` per the behavior block (this is the automated half of D-07; the human confirms the look in Plan 07).

    Run in the WSL scratch venv from Plan 01 with the recorded flag. If a page cannot render because FakeDbus lacks an item or route, extend `fake_dbus.py`/`conftest.py` minimally (canned data only; never change `src/` to satisfy the stub). If a real overflow is found, fix it at the source in `src/Pellmonweb/media/css/pellmon.css` inside the Phase 10 block (or the offending template), never with `overflow-x: hidden` on body/html, then re-run the structural suites for those files. If chromium cannot launch in WSL (recorded in 10-01-SUMMARY), state in the SUMMARY that these tests are first executed by CI in Plan 07 and run the Windows collection check only.
  </action>
  <verify>
    <automated>wsl.exe -e bash -lc 'cd /mnt/d/Antigravity/PellMon-master && PELLMON_BROWSER_TESTS=1 PYTHONPATH=src $HOME/pellmon-browser-venv/bin/python -m pytest tests/browser/test_mobile_overflow.py -v -rs --allow-unix-socket'</automated>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/browser -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - 12 parametrized overflow cases plus `test_desktop_smoke_1280` collected (`pytest --collect-only -q tests/browser/test_mobile_overflow.py` lists 13 items).
    - WSL run: all 13 pass (or, only if chromium cannot launch in WSL per 10-01-SUMMARY, the SUMMARY says so explicitly and CI is the first execution).
    - `grep -n "networkidle\|time.sleep\|wait_for_timeout" tests/browser/test_mobile_overflow.py` returns nothing.
    - `grep -c "overflow-x: hidden" src/Pellmonweb/media/css/pellmon.css` = 0.
    - Windows run skips every browser test with the PELLMON_BROWSER_TESTS reason.
  </acceptance_criteria>
  <done>No-sideways-scroll is proven in a real browser at both target widths on all six pages, and the desktop side-by-side restoration is asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Interaction and geometry tests B1-B7, C1-C3, D1-D2, E1-E3</name>
  <files>tests/browser/test_mobile_interactions.py, tests/browser/fake_dbus.py</files>
  <read_first>
    - tests/browser/conftest.py
    - tests/browser/test_mobile_overflow.py (from Task 1, reuse waits and shot naming)
    - src/Pellmonweb/html/events, src/Pellmonweb/html/graph, src/Pellmonweb/html/parameters.html, src/Pellmonweb/html/settings.html
    - src/Pellmonweb/media/js/index.js (events-toggle handler)
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-UI-SPEC.md (B1-B7, C1-C3, D1-D2, E1-E3 exact thresholds)
  </read_first>
  <behavior>
    - test_main_390_order_and_sizes (B1, B2, B4, B7): tops strictly increase for #systemimage, #events-wrap, #graph, #consumption7d, #silolevel; #systemimage width 340-360 and height >= 230; #graph offsetHeight 260 (+/-1) and offsetWidth <= 360; every .pellmon-chart offsetHeight 260 (+/-1).
    - test_main_390_events_toggle (B3, D-03): #events-wrap has events-collapsed and offsetHeight <= 96; .events-toggle visible, height >= 44, text "Show all events"; after tap: class removed, text "Show fewer events", aria-expanded "true", #lines still present; tap again restores collapsed state and "Show all events".
    - test_main_390_graph_tap_targets (B5, D-04): every a.lineselection, a.timeChoice, a.autorefresh, .graph-nav .btn has bounding box height >= 44 and width >= 44 and right edge <= 390.
    - test_main_390_navbar (B6): .navbar-toggle visible, 44x44 or larger, aria-label "Toggle navigation"; after tap .navbar-collapse gets class "in" (wait), toggle aria-expanded becomes "true", each .navbar-nav > li > a height >= 44 and right edge <= 390.
    - test_parameters_390_pills (C1): ul.param-tags > li widths equal 50% of the ul minus 8px (+/-2); items 0 and 1 share a top, item 2 is lower; horizontal gap between li 0 and 1 >= 8; vertical gap between rows >= 8; each link height >= 44.
    - test_parameters_390_sections (C2): #param-control and #param-data visible initially, #param-settings offsetHeight 0; all three toggles height >= 44; tapping the Settings toggle makes #param-settings visible (wait for class "in") and its aria-expanded becomes "true".
    - test_parameters_390_controls (C3): after opening Settings and clicking a param dt to reveal its .details, every visible .form-control height >= 44 and width >= 300, and the visible input[type=submit] height >= 44.
    - test_settings_390_gallery (D1, D2): .sysimg-tile left values form exactly two distinct columns (+/-1), each tile width 150-190; .sysimg-save height >= 44.
    - test_main_768 (E1): #events-wrap offsetHeight equals scrollHeight, .events-toggle not visible, #graph offsetHeight 320 (+/-1), all dashboard widget column lefts equal (+/-1).
    - test_parameters_768 (E2): all three sections visible without tapping; no .param-section-toggle .glyphicon visible.
    - test_settings_768 (E3): two distinct tile columns.
  </behavior>
  <action>
    Create `tests/browser/test_mobile_interactions.py` implementing the eleven tests above with the thresholds exactly as stated (they come from UI-SPEC B/C/D/E). Use `locator.bounding_box()` and `page.evaluate` for geometry, `locator.tap()` at 390 (context has touch) and `click()` at 768, `page.wait_for_function` for collapse/`in` class transitions, and `shot(page, ...)` before the assertions of each test (and after each toggle state change in B3/C2). Names containing "main" keep the VALIDATION `-k main` selector working. Messages cite the check id, e.g. "B4: phone graph height must be 260px, got %s".

    If FakeDbus data is insufficient for a check (for example fewer than 3 tags, or no R/W param without an enum), extend `tests/browser/fake_dbus.py` with canned data only. If an assertion reveals a real layout defect, fix it at the source (pellmon.css Phase 10 block or the owning template), keep the structural tests from Plans 02-04 green, and record the fix in the SUMMARY as a deviation. Never weaken a threshold to make a test pass.

    Run all browser tests in the WSL scratch venv with the recorded flag, then the full Windows suite.
  </action>
  <verify>
    <automated>wsl.exe -e bash -lc 'cd /mnt/d/Antigravity/PellMon-master && PELLMON_BROWSER_TESTS=1 PYTHONPATH=src $HOME/pellmon-browser-venv/bin/python -m pytest tests/browser -v -rs --allow-unix-socket'</automated>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only -q tests/browser/test_mobile_interactions.py` lists 11 tests.
    - WSL browser run of all of `tests/browser`: 0 failed, 0 skipped (or, only if chromium cannot launch in WSL per 10-01-SUMMARY, stated explicitly in the SUMMARY with CI as first execution).
    - Screenshots exist in `tests/browser/_shots/` after the WSL run (at least one per test), listed by name in the SUMMARY.
    - `grep -n "networkidle\|time.sleep\|wait_for_timeout" tests/browser/*.py` returns nothing.
    - Full Windows suite: only the 3 known baseline failures (test_backup_script::test_conf_d_overrides_pellmon_conf, test_plugin_loader::test_every_available_plugin_loads[consumption|silolevel]); browser tests skipped.
  </acceptance_criteria>
  <done>Every rendered acceptance condition from the UI-SPEC is an automated browser test, green locally where a browser can launch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> stub server | Browser talks to a local auth-disabled stub (from Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | tests/browser/* third-party script loading | mitigate | CDN jQuery routed to vendored file by `page_at`; tests load only repo assets from the 127.0.0.1 stub |
| T-10-11 | Repudiation (silent pass) | browser gate | mitigate | With PELLMON_BROWSER_TESTS=1 missing prerequisites fail, not skip; verify step asserts 0 skipped |
</threat_model>

<verification>
- WSL (or CI): `PELLMON_BROWSER_TESTS=1 PYTHONPATH=src pytest tests/browser -v -rs <BROWSER_PYTEST_FLAGS>` 0 failed, 0 skipped.
- Windows full suite: only the 3 baseline failures.
</verification>

<success_criteria>
- D-02 (no overflow at 390/768), D-03/D-04/D-05 rendered behavior and D-07 desktop layout are all asserted by automated browser tests.
</success_criteria>

<output>
Create `.planning/phases/10-optimize-for-mobile-devices-screen-size/10-05-SUMMARY.md` when done.
</output>
