---
phase: 10-optimize-for-mobile-devices-screen-size
plan: 03
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - src/Pellmonweb/html/index.html
  - src/Pellmonweb/html/events
  - src/Pellmonweb/html/graph
  - src/Pellmonweb/html/layout.html
  - src/Pellmonweb/media/js/index.js
  - src/Pellmonweb/html/consumption.html
  - src/Pellmonsrv/plugins/consumption/__init__.py
  - src/Pellmonsrv/plugins/silolevel/__init__.py
  - src/Pellmonsrv/plugins/consumption/templates/consumption24h
  - src/Pellmonsrv/plugins/consumption/templates/consumption7d
  - src/Pellmonsrv/plugins/consumption/templates/consumption8w
  - src/Pellmonsrv/plugins/consumption/templates/consumption1y
  - tests/Pellmonweb/test_mobile_dashboard.py
autonomous: true
requirements: [D-01, D-03, D-04, D-06, D-07]

must_haves:
  truths:
    - "On a phone the dashboard is one column in the order system image, events, graph, consumption, silo level"
    - "On desktop (>= 992px) the dashboard rows render side by side again because the column class is col-md-6, not col-md-6.0 (D-07)"
    - "On a phone the events list starts collapsed to a short preview with a Show all events button that expands it and reads Show fewer events, while live log updates keep arriving"
    - "The graph and all six plugin chart sources have no inline 400px height and take their height from CSS classes"
    - "The graph line, time-range and autorefresh links and Back/Forward buttons keep every class and data attribute index.js binds to"
    - "The navbar hamburger has an accessible name and its aria-expanded follows the menu state on every page"
  artifacts:
    - path: "src/Pellmonweb/html/index.html"
      provides: "Responsive dashboard grid with integer column width"
      contains: "col-xs-12 col-md-${width}"
    - path: "src/Pellmonweb/html/events"
      provides: "Collapsible events wrapper and toggle"
      contains: "events-toggle visible-xs"
    - path: "src/Pellmonweb/html/graph"
      provides: "CSS-sized graph and wrapped controls"
      contains: "pellmon-graph"
    - path: "src/Pellmonweb/media/js/index.js"
      provides: "Delegated events-toggle handler"
      contains: ".events-toggle"
    - path: "tests/Pellmonweb/test_mobile_dashboard.py"
      provides: "Structural checks 1, 2, 3, 3b, 4, 7, 9 and the D-07 render regression"
      contains: "col-md-6.0"
  key_links:
    - from: "src/Pellmonweb/html/events"
      to: "src/Pellmonweb/media/js/index.js"
      via: "button.events-toggle data-label-collapsed/data-label-expanded read by a delegated click handler"
      pattern: "data-label-expanded"
    - from: "src/Pellmonweb/html/graph"
      to: "src/Pellmonweb/media/css/pellmon.css"
      via: "#graph class pellmon-graph sized by the Phase 10 block"
      pattern: "class=\"pellmon-graph\""
    - from: "src/Pellmonsrv/plugins/consumption/templates/consumption7d"
      to: "src/Pellmonweb/media/css/pellmon.css"
      via: "div class pellmon-chart"
      pattern: "pellmon-chart"
---

<objective>
Make the dashboard, graph widget, events widget, navbar and the consumption/silo charts responsive by changing markup and adding the events toggle JS, and fix the Python 3 float-division column bug (D-07).

Purpose: Delivers D-03 (single column, collapsed events, scaling SVG), D-04 (CSS-sized graph/charts, tap-target-ready controls) and D-07 on top of the CSS contract from Plan 02.
Output: Edited templates, index.js handler, six plugin chart sources, and `tests/Pellmonweb/test_mobile_dashboard.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-CONTEXT.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-UI-SPEC.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-PATTERNS.md
@CLAUDE.md

<interfaces>
CSS classes provided by Plan 02 (pellmon.css Phase 10 block): `.pellmon-graph`, `.pellmon-chart`, `#events-wrap.events-collapsed` (phone only, max-height 96px), `.events-toggle`, `.graph-lines`, `.graph-times`, `.graph-nav`, `object#systemimage` aspect rule, `.navbar-toggle` 44px rule.

Current markup (verbatim facts):
- index.html line 17: `<% width = 12 / len(row) %>`; line 19: `<div class="col-md-${width}">`. Under Python 3 this renders `col-md-6.0` (matches no Bootstrap rule).
- graph line 3: `<div id="graph" style="height:400px"></div>`; line 7: `<p style="text-align: left; line-height: 180%;">` holding `.lineselection` links; line 17: `<div class="pull-left">` holding `.timeChoice` and `.autorefresh`; line 22: malformed `</div   >`; line 23: `<div class="btn-grp pull-right">` holding `a.btn.btn-info.left` (Back) and `a.btn.btn-info.right` (Forward).
- events: `<h4>Events</h4>` then `<div id="lines" data-url="${webroot}/logview/getlines?linenum=7"></div>`. `logview.js getLog()` replaces `#lines` contents with `container.html(data)`.
- layout.html line 35: `<button data-target=".navbar-collapse" data-toggle="collapse" class="navbar-toggle" type="button">` with three `span.icon-bar`; line 74-76: CDN jQuery, fallback, then `bootstrap.min.js`; line 77 `${self.scriptIncludes()}`. The `events` widget is rendered inside body BEFORE jQuery loads, so no inline `$` script may live in the events template.
- index.js binds `$('.timeChoice').click` (140), `$('.lineselection').click` (163), `$('.left').click` (191), `$('.right').click` (206), `$('.autorefresh').click` (222); writes text with `.text()` (e.g. line 278 `$('h4.graphtitle').text(title)`).
- Plugin chart sources with `style="height:400px"`: consumption/__init__.py line ~87 `<div class="image-responsive" id="consumption" style="height:400px">`, silolevel/__init__.py line ~109 `<div class="image-responsive" id="silolevel" style="height:400px">`, and line 2 of each of consumption/templates/consumption{24h,7d,8w,1y}: `<div id="consumptionXX" style="height:400px">`.
- consumption.html lines 11, 14, 19, 22: `<div class="col-md-6">`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard grid (D-07), events toggle, graph controls, navbar aria</name>
  <files>tests/Pellmonweb/test_mobile_dashboard.py, src/Pellmonweb/html/index.html, src/Pellmonweb/html/events, src/Pellmonweb/html/graph, src/Pellmonweb/html/layout.html, src/Pellmonweb/media/js/index.js</files>
  <read_first>
    - src/Pellmonweb/html/index.html
    - src/Pellmonweb/html/events
    - src/Pellmonweb/html/graph
    - src/Pellmonweb/html/systemimage
    - src/Pellmonweb/html/connectionbanner
    - src/Pellmonweb/html/layout.html
    - src/Pellmonweb/media/js/index.js
    - src/Pellmonweb/media/js/logview.js
    - tests/Pellmonweb/test_settings_page.py (Mako `render` helper idiom, lines ~150-175)
    - src/Pellmonweb/media/css/pellmon.css (Phase 10 block from Plan 02)
  </read_first>
  <behavior>
    - test_viewport_meta_exact_and_zoom_allowed (check 1): layout.html contains `<meta name="viewport" content="width=device-width, initial-scale=1.0">` and neither "user-scalable" nor "maximum-scale".
    - test_dashboard_columns_use_integer_division_and_xs12 (check 2, D-07): index.html contains "12 // len(row)" and "col-xs-12 col-md-${width}", and does not contain "12 / len(row)" nor a bare `class="col-md-${width}"`.
    - test_dashboard_render_emits_integer_md_classes (D-07 regression): render index.html via mako TemplateLookup over src/Pellmonweb/html with widgets [["systemimage", "events"], ["graph"]] and a full context (username None, webroot "", connection_state "connected", connection_reason "", rand 0.5, websockets False, graphlines with 2 lines, selectedlines, timeSeconds/timeNames/timeChoice/timeName, autorefresh False); assert the output contains `class="col-xs-12 col-md-6"` and `class="col-xs-12 col-md-12"` and does not contain "col-md-6.0" or "col-md-12.0".
    - test_navbar_toggle_accessible (check 3): layout.html button.navbar-toggle has `aria-label="Toggle navigation"`, `aria-expanded="false"`, and a `<span class="sr-only">Toggle navigation</span>`; layout.html contains an aria-expanded sync bound to `shown.bs.collapse` and `hidden.bs.collapse` placed after the bootstrap.min.js script tag.
    - test_graph_template_css_sized (check 3b): graph contains `class="pellmon-graph"`, no `style="height:` and no `</div   >`; contains "graph-lines", "graph-times pull-left", "graph-nav pull-right", and still contains "lineselection", "timeChoice", "autorefresh", "data-linename", "data-selected", "data-time-choice", "data-title-text", "btn btn-info left", "btn btn-info right".
    - test_events_template_collapsible (check 4, D-03): events contains `id="events-wrap"`, "events-collapsed", `id="lines"`, `data-url=`, "events-toggle", "visible-xs", no "visible-xs-block", `aria-expanded="false"`, "Show all events", "Show fewer events"; the `#lines` div is inside `#events-wrap` and the button is outside `#lines`; no `<script` in the file.
    - test_index_js_events_toggle_uses_text (security convention): index.js contains "'.events-toggle'" in a `$(document).on('click'` delegated handler, and the handler block (from the `.events-toggle` match to the next line that is exactly `});`) contains ".text(" and "aria-expanded" and no ".html(".
  </behavior>
  <action>
    Write `tests/Pellmonweb/test_mobile_dashboard.py` first with the seven tests above (one-line docstring, untyped, `read_text(encoding="utf-8")`, Mako `TemplateLookup(directories=[str(HTML_DIR)])`, no import of `Pellmonweb.pellmonweb` so it runs on Windows) and confirm they fail (RED). Then implement:

    index.html (D-07, D-03): change line 17 to `<% width = 12 // len(row) %>` and line 19 to `<div class="col-xs-12 col-md-${width}">`. This deliberately restores side-by-side desktop rows (CONTEXT D-07 overrides D-02's "desktop unchanged" for this one regression). DOM order already gives system image, events, graph, consumption, silo on phones.

    events (D-03, RESEARCH C1/C5): wrap the existing `#lines` div (id and `data-url` unchanged) in `<div id="events-wrap" class="events-collapsed">`, and after the wrapper add `<button type="button" class="btn btn-default btn-block events-toggle visible-xs" aria-expanded="false" aria-controls="events-wrap" data-label-collapsed="Show all events" data-label-expanded="Show fewer events">Show all events</button>`. Use `visible-xs` (Bootstrap 3.0.0 has no `visible-xs-block`), so the toggle is hidden from 768px and the collapse CSS only applies below 768px. No inline script (jQuery is not loaded yet at this point in the body). aria-controls points at the wrapper because that is the element whose state changes (discretion, documented in SUMMARY).

    index.js: add a delegated handler near the other click handlers: `$(document).on('click', '.events-toggle', function(e) { ... })` that calls `e.preventDefault()`, toggles `events-collapsed` on `#events-wrap`, sets the button's `aria-expanded` to "false" when collapsed and "true" when expanded, and sets the button label with `.text()` from `.attr('data-label-collapsed')` / `.attr('data-label-expanded')`. It must not touch `#lines` or its children (so `getLog()` updates keep working in both states), must not use `.html(`, and matches the file's existing jQuery style. Close the handler with a line that is exactly `});`.

    graph (D-04): line 3 becomes `<div id="graph" class="pellmon-graph"></div>` (no inline height; flot needs the CSS height, which Plan 02 provides). Add `class="graph-lines"` to the existing `<p style="text-align: left; line-height: 180%;">` (keep the p and its style so desktop is unchanged). `<div class="pull-left">` becomes `<div class="graph-times pull-left">`. Fix `</div   >` to `</div>`. `<div class="btn-grp pull-right">` becomes `<div class="btn-grp graph-nav pull-right">`. Keep every `%`-formatted link string, every class token (`lineselection`, `timeChoice`, `autorefresh`, `selected`, `left`, `right`) and every `data-*` attribute exactly as is. No touch/pan handlers (deferred).

    layout.html: on the `.navbar-toggle` button add `aria-label="Toggle navigation" aria-expanded="false"` and insert `<span class="sr-only">Toggle navigation</span>` as its first child (the three icon-bars stay). Do not change the viewport meta. After the `bootstrap.min.js` script tag and before `${self.scriptIncludes()}`, add a short inline script (RESEARCH C2: Bootstrap 3.0.0 collapse does not manage aria-expanded) that binds `$('.navbar-collapse').on('shown.bs.collapse', ...)` and `.on('hidden.bs.collapse', ...)` to set `aria-expanded` on `.navbar-toggle` to "true"/"false". It lives in layout.html so it works on every page (index.js loads only on the dashboard). No `.html(`.

    systemimage: no markup change (the `object#systemimage` CSS from Plan 02 is the fix; `image-responsive` is a harmless no-op token per RESEARCH C6, leave it).
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_dashboard.py tests/Pellmonweb/test_source_js_no_html_sink.py tests/Pellmonweb/test_connection_banner.py -v</automated>
  </verify>
  <acceptance_criteria>
    - All seven Task 1 tests pass.
    - `grep -n "12 // len(row)" src/Pellmonweb/html/index.html` matches; `grep -c "12 / len(row)" src/Pellmonweb/html/index.html` = 0.
    - `grep -c "style=\"height" src/Pellmonweb/html/graph` = 0; `grep -c "</div   >" src/Pellmonweb/html/graph` = 0.
    - `grep -c "visible-xs-block" src/Pellmonweb/html/events` = 0 and `grep -c "<script" src/Pellmonweb/html/events` = 0.
    - `git diff src/Pellmonweb/media/js/index.js` shows only added lines.
    - Existing tests in tests/Pellmonweb that render templates still pass.
  </acceptance_criteria>
  <done>Dashboard markup, events toggle, graph controls and navbar aria are in place and structurally verified; the col-md-6.0 bug is fixed with a render-level regression test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CSS-sized charts in all six plugin sources and a stacking Consumption page</name>
  <files>tests/Pellmonweb/test_mobile_dashboard.py, src/Pellmonsrv/plugins/consumption/__init__.py, src/Pellmonsrv/plugins/silolevel/__init__.py, src/Pellmonsrv/plugins/consumption/templates/consumption24h, src/Pellmonsrv/plugins/consumption/templates/consumption7d, src/Pellmonsrv/plugins/consumption/templates/consumption8w, src/Pellmonsrv/plugins/consumption/templates/consumption1y, src/Pellmonweb/html/consumption.html</files>
  <read_first>
    - src/Pellmonsrv/plugins/consumption/__init__.py (template string around line 87)
    - src/Pellmonsrv/plugins/silolevel/__init__.py (template string around line 109)
    - src/Pellmonsrv/plugins/consumption/templates/consumption24h, consumption7d, consumption8w, consumption1y
    - src/Pellmonweb/html/consumption.html
    - tests/Pellmonweb/test_mobile_dashboard.py (from Task 1)
  </read_first>
  <behavior>
    - test_no_inline_400px_in_six_chart_sources (check 3b/9, RESEARCH C4, D-04): for plugins/consumption/__init__.py, plugins/silolevel/__init__.py and every file in plugins/consumption/templates/*, text with spaces removed contains no "height:400px"; each contains "pellmon-chart"; each still contains its original id (`id="consumption"`, `id="silolevel"`, `id="consumption24h"` etc.).
    - test_consumption_page_stacks (check 7): consumption.html contains "col-xs-12 col-md-6" exactly 4 times and no `class="col-md-6"`.
    - test_no_large_fixed_sizes_in_templates (check 9): across src/Pellmonweb/html/* and the six plugin sources, no inline `style` attribute declares `width:` of 361px or more or `height:` of 260px or more (regex on `style="..."` contents, values in px).
  </behavior>
  <action>
    Add the three tests to `tests/Pellmonweb/test_mobile_dashboard.py` (RED), then:
    - consumption/__init__.py: in the `_insert_template('consumption', ...)` string change `<div class="image-responsive" id="consumption" style="height:400px">` to `<div class="image-responsive pellmon-chart" id="consumption">`. String edit only; no other Python changes, no f-strings, no `.py2bak`.
    - silolevel/__init__.py: same change for `id="silolevel"`.
    - consumption/templates/consumption24h, consumption7d, consumption8w, consumption1y: line 2 `<div id="consumptionXX" style="height:400px">` becomes `<div id="consumptionXX" class="pellmon-chart">` (keep ids, keep the following `<script>` blocks and `insertAfter` summaries unchanged). These are the dashboard default `consumption7d` widget and the Consumption page charts (RESEARCH C4).
    - consumption.html: the four `<div class="col-md-6">` become `<div class="col-xs-12 col-md-6">` (charts stack on phones and tablets; desktop unchanged).
    Note for the SUMMARY: plugin templates are served by the daemon over D-Bus and cached by the web process (RESEARCH Pitfall 9), so deployment needs both containers recreated; the Plan 07 checkpoint repeats this.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_dashboard.py tests/Pellmonsrv -v</automated>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "height:400px" src/Pellmonsrv/plugins/consumption src/Pellmonsrv/plugins/silolevel src/Pellmonweb/html --include=* | grep -v __pycache__` returns nothing.
    - `grep -c "pellmon-chart" src/Pellmonsrv/plugins/consumption/templates/*` reports 1 per file.
    - `grep -c "col-xs-12 col-md-6" src/Pellmonweb/html/consumption.html` = 4.
    - All ten tests in `tests/Pellmonweb/test_mobile_dashboard.py` pass.
    - Full Windows suite: only the 3 known baseline failures (test_backup_script::test_conf_d_overrides_pellmon_conf, test_plugin_loader::test_every_available_plugin_loads[consumption|silolevel]); no new failures.
  </acceptance_criteria>
  <done>All chart containers are CSS-sized, the Consumption page stacks on small screens, and the full suite has no new failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> browser DOM | Log lines (server-rendered) arrive in #lines; new toggle JS runs beside them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering (DOM injection) | index.js events-toggle handler, layout.html aria script | mitigate | Static labels from data attributes written with `.text()`/`.attr()` only; test_index_js_events_toggle_uses_text asserts no `.html(` in the handler |
| T-10-06 | Denial of Service (UI) | events toggle vs getLog() | mitigate | Toggle lives outside `#lines` and never touches its children; asserted structurally, exercised in Plan 05 B3 |
| T-10-07 | Tampering | plugin template strings | accept | Only a class attribute changes inside static template text; no new interpolation |
</threat_model>

<verification>
- `PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_dashboard.py -v` all green.
- Full suite shows only the 3 baseline failures.
</verification>

<success_criteria>
- D-03, D-04 markup and D-07 fix delivered; index.js bindings preserved; six chart sources CSS-sized.
</success_criteria>

<output>
Create `.planning/phases/10-optimize-for-mobile-devices-screen-size/10-03-SUMMARY.md` when done.
</output>
