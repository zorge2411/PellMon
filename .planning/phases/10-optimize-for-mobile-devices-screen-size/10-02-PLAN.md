---
phase: 10-optimize-for-mobile-devices-screen-size
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Pellmonweb/media/css/pellmon.css
  - tests/Pellmonweb/test_mobile_css.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06]

must_haves:
  truths:
    - "All responsive rules live in one appended block in the existing pellmon.css; no new stylesheet, no new link tag, Bootstrap stays the vendored 3.0.0"
    - "Graph and plugin charts are 260px tall below 768px, 320px from 768 to 991px, and 400px from 992px (unchanged desktop)"
    - "Every tap target this phase adds or restyles is at least 44px tall on phones"
    - "Nothing in the stylesheet hides horizontal overflow on body or html"
    - "Desktop-only rules that existed before (dl-horizontal min-width 768 block, sysimg-tile block, object width 100%) are unchanged"
  artifacts:
    - path: "src/Pellmonweb/media/css/pellmon.css"
      provides: "Phase 10 mobile/responsive block"
      contains: "/* Phase 10: mobile / responsive */"
    - path: "tests/Pellmonweb/test_mobile_css.py"
      provides: "Structural checks 8 and D-01 framework checks (Windows-safe text assertions)"
      contains: "def test_framework_bs3_only_no_new_stylesheet"
  key_links:
    - from: "src/Pellmonweb/media/css/pellmon.css"
      to: "src/Pellmonweb/html/layout.html"
      via: "existing <link href=.../media/css/pellmon.css>"
      pattern: "media/css/pellmon.css"
---

<objective>
Write the complete Phase 10 CSS contract into `pellmon.css` (the interface the template plans build against), test-first with Windows-safe structural checks.

Purpose: Plans 03 and 04 only add class names to markup; the visual behavior (heights, tap targets, stacking, collapse behavior per breakpoint) comes from this block. Doing it once avoids two parallel plans editing the same stylesheet.
Output: One appended `/* Phase 10: mobile / responsive */` block in `src/Pellmonweb/media/css/pellmon.css` and `tests/Pellmonweb/test_mobile_css.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-CONTEXT.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-UI-SPEC.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md
@CLAUDE.md
@src/Pellmonweb/media/css/pellmon.css

<interfaces>
Class names this block styles, which Plans 03/04 will emit in markup (contract; do not rename):
- Dashboard: `#pellmon-widgets` (exists), `object#systemimage` (exists), `#events-wrap`, `.events-collapsed`, `.events-toggle`, `#lines` (exists), `.loglink` (exists in log rows).
- Graph: `.pellmon-graph` on `#graph`, `.graph-lines` (on the existing `<p>` holding `.lineselection` links), `.graph-times` (added to the existing `.pull-left` div), `.graph-nav` (added to the existing `.btn-grp.pull-right` div), `#graphdiv .col-centered` (exists), `#tooltip` (created by index.js).
- Charts: `.pellmon-chart` on consumption/silolevel/consumption{24h,7d,8w,1y} divs.
- Navbar: `.navbar-toggle`, `.navbar-nav > li > a` (exist).
- Parameters: `.param-tags` (added to `nav nav-pills nav-stacked`), `.param-section-heading` (h3), `.param-section-toggle` (a.btn.btn-default.btn-block), `.param-section` (collapse body; ids `#param-control`, `#param-data`, `#param-settings`), `.details .form-inline` (exists), `.command.btn` (exists).
- Settings: `.sysimg-tile`, `.sysimg-tile-selected`, `.sysimg-thumb`, `.sysimg-caption` (exist, Phase 8), `.sysimg-save` (added to the Save image button).

Important cascade fact: `parameters.html` has a page-local `<style>` in `<body>` that sets `.details .form-inline .form-control { width: 60%; }`. It comes after `pellmon.css` in document order, so a same-specificity phone override in `pellmon.css` loses. Phone overrides for those controls must be prefixed with `.param-section` (higher specificity).

Bootstrap is v3.0.0 (RESEARCH C1/C2): no `visible-xs-block`, `.btn` has `white-space: nowrap`, `.btn-default:hover/:focus/:active` set backgrounds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Structural CSS and framework tests (RED)</name>
  <files>tests/Pellmonweb/test_mobile_css.py</files>
  <read_first>
    - src/Pellmonweb/media/css/pellmon.css
    - src/Pellmonweb/html/layout.html
    - tests/Pellmonweb/test_source_js_no_html_sink.py (text-assertion idiom)
    - tests/Pellmonweb/test_settings_page.py (header/paths idiom)
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-UI-SPEC.md (Acceptance "Structural" item 8, Spacing Scale, Tap Targets, Breakpoints)
  </read_first>
  <behavior>
    - test_framework_bs3_only_no_new_stylesheet (D-01): layout.html has exactly two stylesheet link tags, pointing at `media/bs3/css/bootstrap.min.css` and `media/css/pellmon.css`; `media/bs3/css/bootstrap.css` first 200 chars contain "v3.0.0"; no file under src/Pellmonweb/media/css other than the existing three (pellmon.css, pellmonconf.css, pellmonconf_print.css) exists.
    - test_css_block_header_and_values (UI-SPEC check 8): pellmon.css contains "/* Phase 10: mobile / responsive */", "max-width: 767px", "min-height: 44px", ".pellmon-graph", ".pellmon-chart", "260px", "320px", "400px", "#events-wrap.events-collapsed", "aspect-ratio: 11 / 7", ".param-section-toggle", ".param-tags", ".sysimg-save".
    - test_css_block_preserves_existing_rules: the pre-Phase-10 part of the file (text before the header) still contains ".sysimg-tile {", ".dl-horizontal dt", "@media (min-width: 768px)", "object {".
    - test_css_block_media_queries_are_bounded: every "@media" occurrence in the Phase 10 block is one of "(max-width: 767px)", "(min-width: 768px) and (max-width: 991px)", "(min-width: 768px)".
    - test_css_block_no_overflow_masking (D-02): no "overflow-x" declaration with value "hidden" anywhere in pellmon.css.
    - test_css_block_spacing_scale: every px value in padding/margin/gap declarations inside the Phase 10 block is one of 0, 4, 8, 16, 24 (exclude `calc(` expressions from the check).
    - test_css_block_chart_heights_per_breakpoint (D-04): inside the phone query the combined `.pellmon-graph, .pellmon-chart` rule sets 260px, inside the tablet query 320px, and outside any media query 400px (parse by splitting the block on "@media" and brace matching).
  </behavior>
  <action>
    Create `tests/Pellmonweb/test_mobile_css.py` (one-line module docstring, untyped, `read_text(encoding="utf-8")` everywhere, `%`-formatted assertion messages that name the UI-SPEC check or decision, e.g. "D-04: phone graph height must be 260px"). Paths via `ROOT = Path(__file__).resolve().parents[2]`. Do not import `Pellmonweb` (Windows-safe). Write a small helper `phase10_block()` returning the text from the header comment to end of file (empty string if the header is missing, so RED failures are assertion failures, not exceptions), and a helper `media_blocks(text)` that returns (query, body) pairs using brace counting. Implement the seven tests from the behavior block. Run them and confirm they fail against the current stylesheet (RED) except the framework and preservation tests, which must already pass.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_css.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the seven test functions named exactly as in the behavior block (`grep -c "^def test_" tests/Pellmonweb/test_mobile_css.py` = 7).
    - `test_framework_bs3_only_no_new_stylesheet` and `test_css_block_preserves_existing_rules` pass; the other five fail with assertion errors (not collection errors) before Task 2.
    - `grep -n "import Pellmonweb\|from Pellmonweb" tests/Pellmonweb/test_mobile_css.py` returns nothing.
  </acceptance_criteria>
  <done>Structural CSS contract is encoded as failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append the Phase 10 responsive block to pellmon.css (GREEN)</name>
  <files>src/Pellmonweb/media/css/pellmon.css</files>
  <read_first>
    - src/Pellmonweb/media/css/pellmon.css
    - src/Pellmonweb/html/parameters.html (page-local style block, lines 7-25)
    - src/Pellmonweb/media/bs3/css/bootstrap.css (confirm `.visible-xs`, `.collapse`, `.btn` white-space, `.navbar-toggle .icon-bar + .icon-bar` in v3.0.0)
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-UI-SPEC.md (Tap Targets, Layout Contract by Widget, Typography)
    - .planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md (Corrections C1-C6, "Desktop (>= 992px) regression risks")
    - tests/Pellmonweb/test_mobile_css.py
  </read_first>
  <action>
    Append (never edit lines above it) one block starting with the comment `/* Phase 10: mobile / responsive */`, 4-space indentation, plain selectors, matching the file's style. Per D-01 all rules go in this existing file. To keep the UI-SPEC's "exactly three media queries" while giving desktop 400px, the desktop height is the un-queried base and phone/tablet override it. Contents:

    Base (all viewports): `img, object, svg { max-width: 100%; }`; `.container { overflow-wrap: break-word; }`; `.pellmon-graph, .pellmon-chart { height: 400px; }` (D-04 desktop unchanged); `#tooltip { max-width: 200px; }`; `.loglink img { max-width: 100%; height: auto; }`; `#lines { overflow-wrap: anywhere; }` (keep `anywhere`, RESEARCH Pitfall 5); `.param-section-toggle .glyphicon { margin-left: 4px; transition: transform 0.2s; }`; `.param-section-toggle.collapsed .glyphicon-chevron-up { transform: rotate(180deg); }` (chevron state from the `collapsed` class, no extra JS).

    `@media (max-width: 767px)` (phones):
    - Typography: `h1 { font-size: 24px; font-weight: 600; line-height: 1.2; }`; `#pellmon-widgets h4 { font-weight: 600; line-height: 1.2; }`; `.form-control { height: 44px; font-size: 16px; }` (16px stops iOS focus zoom).
    - Dashboard (D-03): `#pellmon-widgets [class*="col-"] { margin-bottom: 16px; }`; `object#systemimage { width: 100%; height: auto; aspect-ratio: 11 / 7; min-height: 230px; }`; `#events-wrap.events-collapsed { max-height: 96px; overflow: hidden; }`; `.events-toggle { min-height: 44px; margin-top: 8px; }`.
    - Navbar: `.navbar-toggle { width: 44px; height: 44px; padding: 8px 0; margin: 4px 16px 4px 0; display: flex; flex-direction: column; justify-content: center; align-items: center; }`; `.navbar-nav > li > a { padding: 8px 16px; line-height: 28px; }`.
    - Graph (D-04): `.pellmon-graph, .pellmon-chart { height: 260px; }`; `#graphdiv .col-centered { display: block; }`; `.graph-lines a, .graph-times a { display: inline-block; min-height: 44px; line-height: 40px; padding: 0 16px; margin: 0 8px 8px 0; }`; `a.lineselection:not(.selected) { border-top: 3px solid transparent !important; }`; `.graph-times.pull-left, .graph-nav.pull-right { float: none !important; }`; `.graph-nav { display: flex; gap: 8px; }`; `.graph-nav .btn { flex: 1 1 50%; min-height: 44px; padding: 8px 16px; line-height: 28px; }`.
    - Parameters (D-05): `.param-tags > li, .param-tags > li + li { float: left; width: calc(50% - 8px); margin: 0 4px 8px; }`; `.param-tags > li > a { min-height: 44px; padding: 8px; line-height: 28px; }`; `.param-section-heading { margin: 0 0 8px; font-size: 18px; font-weight: 600; line-height: 1.2; }`; `.param-section-toggle { min-height: 44px; font-size: 18px; font-weight: 600; line-height: 1.2; white-space: normal; text-align: left; }`; `.param-section { margin-bottom: 24px; }`; `.param-section dt { margin-top: 16px; }`; `.param-section dd { margin-left: 0; }`; `.param-section .details .form-inline .form-control { width: 100%; }` (prefixed to beat the page-local 60% rule); `.param-section .details .form-inline .btn { display: block; width: 100%; margin-top: 8px; min-height: 44px; }`; `.btn-group > .command.btn { float: none; display: block; width: 100%; margin: 0 0 8px; border-radius: 4px; min-height: 44px; }`.
    - Settings (D-05): `.sysimg-tile { padding: 8px; margin-bottom: 16px; }`; `.sysimg-tile-selected { border: 1px solid #337ab7; padding: 8px; box-shadow: inset 0 0 0 2px #337ab7; }` (border reset so the selected tile is not 1px larger, RESEARCH note); `.sysimg-thumb { max-height: 96px; }`; `.sysimg-caption { display: block; font-size: 14px; }`; `.sysimg-save { display: block; width: 100%; min-height: 44px; }`.

    `@media (min-width: 768px) and (max-width: 991px)` (tablet): `.pellmon-graph, .pellmon-chart { height: 320px; }`; `object#systemimage { width: 100%; height: auto; aspect-ratio: 11 / 7; min-height: 230px; }`.

    `@media (min-width: 768px)` (new block, separate from the untouched existing `.dl-horizontal` block): sections always open and look like today's h3 (UI-SPEC Parameters, RESEARCH desktop risks): `.param-section.collapse { display: block !important; height: auto !important; }`; `.param-section-toggle, .param-section-toggle:hover, .param-section-toggle:focus, .param-section-toggle:active { background: none; border: 0; padding: 0; text-align: left; font: inherit; color: inherit; box-shadow: none; white-space: normal; pointer-events: none; }`; `.param-section-toggle .glyphicon { display: none; }`.

    Forbidden: any `overflow-x: hidden` on body/html (D-02), any `min-width: 992px` query, any new `@import`/stylesheet. Do not touch lines 1-99.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_css.py -v</automated>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - All 7 tests in `tests/Pellmonweb/test_mobile_css.py` pass.
    - `git diff -U0 src/Pellmonweb/media/css/pellmon.css` shows only added lines (no `-` lines other than the diff header).
    - `grep -c "overflow-x" src/Pellmonweb/media/css/pellmon.css` = 0.
    - `grep -c "min-height: 44px" src/Pellmonweb/media/css/pellmon.css` >= 8.
    - Full Windows suite: only the 3 known baseline failures (test_backup_script::test_conf_d_overrides_pellmon_conf, test_plugin_loader::test_every_available_plugin_loads[consumption|silolevel]).
  </acceptance_criteria>
  <done>The full responsive CSS contract exists in pellmon.css, verified structurally, with the desktop cascade unchanged for pre-existing selectors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Static stylesheet served from /media; no input handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | pellmon.css | accept | Presentation-only rules; no data, no external URLs, no `@import` (asserted by test_framework_bs3_only_no_new_stylesheet) |
</threat_model>

<verification>
- `PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_css.py -v` all pass.
- Full suite shows only the 3 baseline failures.
</verification>

<success_criteria>
- One appended Phase 10 block in pellmon.css covering D-02..D-05 rules; D-01 preserved (no new stylesheet or framework).
</success_criteria>

<output>
Create `.planning/phases/10-optimize-for-mobile-devices-screen-size/10-02-SUMMARY.md` when done.
</output>
