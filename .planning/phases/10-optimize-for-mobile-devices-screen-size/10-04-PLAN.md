---
phase: 10-optimize-for-mobile-devices-screen-size
plan: 04
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - src/Pellmonweb/html/parameters.html
  - src/Pellmonweb/media/js/parameters.js
  - src/Pellmonweb/html/settings.html
  - tests/Pellmonweb/test_mobile_forms.py
autonomous: true
requirements: [D-01, D-05, D-06]

must_haves:
  truths:
    - "On a phone the Parameters page shows the tag list as two pills per row, then Control, Data and Settings as collapsible sections (Control and Data open, Settings closed)"
    - "On a phone Parameters labels sit above values, inputs and selects are full width and 44px tall, and Set and command buttons are full-width 44px buttons"
    - "From 768px up the Parameters sections are always open and look like today's headings"
    - "Parameter values keep updating and the click-to-edit details keep working while a section is collapsed"
    - "The Settings gallery shows two tiles per row on phones and the Save image button is full width"
  artifacts:
    - path: "src/Pellmonweb/html/parameters.html"
      provides: "Responsive grid, param-tags pills, three collapsible sections"
      contains: "param-section"
    - path: "src/Pellmonweb/media/js/parameters.js"
      provides: "aria-expanded sync for section toggles"
      contains: "show.bs.collapse"
    - path: "src/Pellmonweb/html/settings.html"
      provides: "2-up gallery on phones, sysimg-save button class"
      contains: "col-xs-6 col-sm-6 col-md-4"
    - path: "tests/Pellmonweb/test_mobile_forms.py"
      provides: "Structural checks 5 and 6 plus render assertions"
      contains: "data-toggle=\"collapse\""
  key_links:
    - from: "src/Pellmonweb/html/parameters.html"
      to: "src/Pellmonweb/media/js/parameters.js"
      via: "a.param-section-toggle href=#param-* toggled by Bootstrap collapse; parameters.js syncs aria-expanded on show/hide.bs.collapse"
      pattern: "param-section"
    - from: "src/Pellmonweb/html/parameters.html"
      to: "src/Pellmonweb/media/css/pellmon.css"
      via: "param-tags, param-section-heading, param-section-toggle, param-section classes"
      pattern: "param-tags"
---

<objective>
Make the Parameters and Settings pages usable on phones (D-05): stacked controls, 44px targets, two-per-row tag pills and gallery tiles, and collapsible Parameters sections, without changing the desktop look.

Purpose: These are the two form-heavy pages; the CSS from Plan 02 does the sizing, this plan adds the Bootstrap 3.0.0 grid classes, collapse markup and aria sync.
Output: Edited `parameters.html`, `parameters.js`, `settings.html`, and `tests/Pellmonweb/test_mobile_forms.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-CONTEXT.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-UI-SPEC.md
@.planning/phases/10-optimize-for-mobile-devices-screen-size/10-RESEARCH.md
@.planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-UI-SPEC.md
@CLAUDE.md

<interfaces>
CSS classes provided by Plan 02: `.param-tags`, `.param-section-heading`, `.param-section-toggle` (+ `.collapsed` chevron rotation, desktop reset at min-width 768), `.param-section` (+ desktop `display:block !important`), `.param-section .details .form-inline .form-control|.btn`, `.btn-group > .command.btn`, `.sysimg-*` phone tuning, `.sysimg-save`.

parameters.html today (verbatim facts): sidebar `<div class="col-md-2">` (line 29) with `<ul class="nav nav-pills nav-stacked">` (line 31) looping `tags`; second navbar with the Less../More.. link (lines 40-53, untouched); content `<div class="col-md-10">` (55); Control block `%if len(commands)>0:` -> `<div class="col-md-12">` `<h3>Control</h3>` `<div class="btn-group">` of `button.command.btn.btn-primary` (57-66); Data block `<div class="col-md-5">` `<h3>Data</h3>` `<dl class="dl dl-horizontal">` (70-87); Settings block `<div class="col-md-5">` `<h3>Settings</h3>` `<dl class="dl dl-horizontal">` with `.details` forms (89-127). JS hooks that must not change: classes `editable`, `details hidden`, `select_enum`, `save`, `param value`, `command`; attributes `data-name`; ids `${key['name']}-value`, `-selectbox`, `-form`. Handler context vars: data, params, commands, values, level, heading, tags, websockets, webroot, from_page, username.

parameters.js: jQuery, tab-indented, `$('.editable').on('click', function(e) { e.preventDefault(); ... })` style, `.text()` writes.

settings.html line 23: `<div class="col-xs-12 col-sm-6 col-md-4">`; line 37: `<button type="submit" class="btn btn-primary"${'' if auth_configured else ' disabled'}>Save image</button>`. tests/Pellmonweb/test_settings_page.py asserts `re.search(r'<button[^>]*btn-primary[^>]*disabled', html)` and radio/checked/Current counts, which must keep passing.

Bootstrap 3.0.0 (RESEARCH C2): collapse data-api toggles only the `collapsed` class on the trigger at click time, never sets it initially, never manages aria-expanded; events `show.bs.collapse` / `hide.bs.collapse` exist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameters page grid, tag pills and collapsible sections with aria sync</name>
  <files>tests/Pellmonweb/test_mobile_forms.py, src/Pellmonweb/html/parameters.html, src/Pellmonweb/media/js/parameters.js</files>
  <read_first>
    - src/Pellmonweb/html/parameters.html
    - src/Pellmonweb/media/js/parameters.js
    - src/Pellmonweb/pellmonweb.py (the `parameters()` handler, lines ~555-615, for the exact render context keys)
    - src/Pellmonweb/media/css/pellmon.css (Phase 10 block)
    - tests/Pellmonweb/test_settings_page.py (Mako render helper idiom)
  </read_first>
  <behavior>
    - test_parameters_sections_collapsible (check 5): parameters.html contains `data-toggle="collapse"` at least 3 times, ids `param-control`, `param-data`, `param-settings`, class "param-section" and "param-section-heading"; the Settings trigger carries class `collapsed` and `aria-expanded="false"`; Control and Data triggers carry `aria-expanded="true"` and their bodies `collapse in param-section`; the Settings body is `collapse param-section` (no `in`).
    - test_parameters_grid_has_xs_sibling (check 5): parameters.html contains "col-xs-12"; every `class="..."` value containing a `col-md-` token also contains a `col-xs-` token; tag list ul has "param-tags" alongside "nav nav-pills nav-stacked".
    - test_parameters_js_hooks_preserved: parameters.html still contains "editable", "details hidden", "select_enum", "save form-inline", "param value", "command btn btn-primary", "-value", "-selectbox", "-form"; the page-local style still sets `.details .form-inline .form-control` width 60%.
    - test_parameters_render (render with Mako: 2 commands, 3 data items, 3 params of which one has get_enum_list and one has min/max, 8 tags, level "Basic"): rendered HTML contains 3 `param-section-toggle` anchors whose href targets exist as ids, labels "Control", "Data", "Settings", and all 8 tag links inside `ul.param-tags`.
    - test_parameters_js_aria_sync (C2): parameters.js contains "show.bs.collapse", "hide.bs.collapse", "aria-expanded", ".param-section"; no ".html(" in the file.
  </behavior>
  <action>
    Write the five tests in `tests/Pellmonweb/test_mobile_forms.py` (one-line docstring, untyped, Windows-safe, no `Pellmonweb.pellmonweb` import) and confirm RED. Then edit parameters.html (per D-05, RESEARCH C2):
    - Sidebar `col-md-2` -> `col-xs-12 col-md-2`; the tag ul gets `class="nav nav-pills nav-stacked param-tags"`. Leave the Less../More.. navbar block unchanged.
    - Content `col-md-10` -> `col-xs-12 col-md-10`; Control column `col-md-12` -> `col-xs-12 col-md-12`; Data and Settings columns `col-md-5` -> `col-xs-12 col-md-5`.
    - Replace each of the three `<h3>Label</h3>` with `<h3 class="param-section-heading"><a class="param-section-toggle btn btn-default btn-block" data-toggle="collapse" href="#param-X" aria-expanded="true" aria-controls="param-X">Label <span class="glyphicon glyphicon-chevron-up"></span></a></h3>` for X = control, data; for settings use `class="param-section-toggle btn btn-default btn-block collapsed"` and `aria-expanded="false"`. Labels stay exactly "Control", "Data", "Settings".
    - Wrap the section body that follows each heading: Control's `div.btn-group` in `<div id="param-control" class="collapse in param-section">`; Data's `dl` in `<div id="param-data" class="collapse in param-section">`; Settings' `dl` in `<div id="param-settings" class="collapse param-section">`. The spans with `param value` stay in the DOM when collapsed, so polling/websocket updates continue (display only).
    - Do not change the page-local `<style>` block (Plan 02's `.param-section`-prefixed phone rules out-rank its 60% width), the inline `z-index: 10000`, or any JS hook listed in the interfaces.
    Edit parameters.js: append a delegated handler `$(document).on('show.bs.collapse hide.bs.collapse', '.param-section', function(e) { ... })` that sets `aria-expanded` to "true" on show and "false" on hide on the `a.param-section-toggle[href="#" + this.id]` trigger (ignore events whose target is not the `.param-section` itself, e.g. check `e.target === this`). Tab indentation and jQuery style as in the file; no `.html(`.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_forms.py -v</automated>
  </verify>
  <acceptance_criteria>
    - All five Task 1 tests pass.
    - `grep -c 'data-toggle="collapse"' src/Pellmonweb/html/parameters.html` = 3.
    - `grep -c "width: 60%" src/Pellmonweb/html/parameters.html` = 1 (page-local rule untouched).
    - `git diff src/Pellmonweb/media/js/parameters.js` shows only added lines.
  </acceptance_criteria>
  <done>Parameters page emits the responsive grid, two-up pill list and three collapsible sections, with aria-expanded kept in sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings gallery two-up on phones and full-width Save image</name>
  <files>tests/Pellmonweb/test_mobile_forms.py, src/Pellmonweb/html/settings.html</files>
  <read_first>
    - src/Pellmonweb/html/settings.html
    - tests/Pellmonweb/test_settings_page.py
    - .planning/phases/08-expose-the-burner-svg-depiction-in-settings-to-make-the-visi/08-UI-SPEC.md (gallery states that must not change)
  </read_first>
  <behavior>
    - test_settings_gallery_two_up (check 6): settings.html tile column is `col-xs-6 col-sm-6 col-md-4` and contains no `col-xs-12 col-sm-6 col-md-4`.
    - test_settings_save_button_class: rendered settings.html (reuse the fixture data shape from test_settings_page.py: 3 images, one current, auth_configured True and False) has a button with classes `btn btn-primary sysimg-save`; with auth_configured False it still matches `<button[^>]*btn-primary[^>]*disabled`.
  </behavior>
  <action>
    Add the two tests to `tests/Pellmonweb/test_mobile_forms.py` (RED), then in settings.html change line 23 to `<div class="col-xs-6 col-sm-6 col-md-4">` (D-05: two tiles per row on phones and tablets, three on desktop, as today) and add `sysimg-save` to the Save image button class list so it reads `class="btn btn-primary sysimg-save"` followed by the unchanged `${'' if auth_configured else ' disabled'}` expression. No other markup changes: Phase 8 tile states (radio, selected border, Current label, check glyph, help text, alerts) stay exactly as they are; only their phone sizing comes from Plan 02 CSS.
  </action>
  <verify>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_forms.py tests/Pellmonweb/test_settings_page.py tests/Pellmonweb/test_settings_image.py -v</automated>
    <automated>PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "col-xs-6 col-sm-6 col-md-4" src/Pellmonweb/html/settings.html` = 1.
    - `grep -c "sysimg-save" src/Pellmonweb/html/settings.html` = 1.
    - All seven tests in `tests/Pellmonweb/test_mobile_forms.py` pass; `test_settings_page.py` and `test_settings_image.py` pass (or skip only for their pre-existing gi reasons on Windows).
    - Full Windows suite: only the 3 known baseline failures (test_backup_script::test_conf_d_overrides_pellmon_conf, test_plugin_loader::test_every_available_plugin_loads[consumption|silolevel]); no new failures.
  </acceptance_criteria>
  <done>Settings gallery renders two per row on phones with a full-width Save button hook; Phase 8 behavior tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /parameters set endpoints | Existing parameter write forms (unchanged behavior, restyled only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering (DOM injection) | parameters.js aria sync | mitigate | Only `.attr('aria-expanded', 'true'/'false')` on a trigger selected by the section's own id; no `.html(` (asserted by test_parameters_js_aria_sync) |
| T-10-09 | Elevation of Privilege | parameters/settings forms | accept | No change to handlers, `@require()` auth or CSRF behavior; markup-only class and wrapper changes |
</threat_model>

<verification>
- `PYTHONPATH=src ./venv-py3/Scripts/python.exe -m pytest tests/Pellmonweb/test_mobile_forms.py -v` all green.
- Full suite shows only the 3 baseline failures.
</verification>

<success_criteria>
- D-05 delivered in markup: stacked Parameters controls via grid classes, collapsible sections, two-up pills and gallery, Save button hook; desktop unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-optimize-for-mobile-devices-screen-size/10-04-SUMMARY.md` when done.
</output>
