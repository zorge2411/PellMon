---
phase: 04-protocol-module-hardening
plan: 01
wave: 1
depends_on: []
files_modified:
  - src/Pellmonsrv/plugins/calculate/__init__.py
  - src/Pellmonsrv/daemon.py
  - tests/Pellmonsrv/plugins/test_calculate_hardening.py
  - tests/Pellmonsrv/test_daemon_hardening.py
autonomous: true
requirements: [PROTO-02, PROTO-03, PROTO-05]
must_haves:
  truths:
    - "Calculate plugin does not import maketrans from string (PROTO-05)"
    - "Calculate plugin setItem uses str(value) instead of unicode(value) (PROTO-02)"
    - "daemon.py uses line-buffered text I/O (buffering=1) for stderr redirection (PROTO-03)"
  artifacts:
    - path: "tests/Pellmonsrv/plugins/test_calculate_hardening.py"
      provides: "Unit test for Calculate plugin hardening"
      min_lines: 30
    - path: "tests/Pellmonsrv/test_daemon_hardening.py"
      provides: "Unit test for daemon stderr buffering"
      min_lines: 25
---

<objective>
Harden the Calculate plugin and Daemonizer against Python 3 runtime crashes: remove the Python 2 `maketrans` import (PROTO-05), replace `unicode(value)` with `str(value)` in Calculate `setItem` (PROTO-02), and fix `daemon.py` stderr buffering to avoid `ValueError` (PROTO-03).

Purpose: Eliminate crashes when Calculate runs on Linux/WSL, when setItem is called, or when the daemon forks.
Output: Clean `calculate/__init__.py` and `daemon.py` backed by passing unit tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-module-hardening/04-CONTEXT.md
@.planning/phases/04-protocol-module-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Calculate plugin maketrans and unicode() calls</name>
  <files>src/Pellmonsrv/plugins/calculate/__init__.py</files>
  <read_first>
    - src/Pellmonsrv/plugins/calculate/__init__.py (lines 20-35, 345-365)
  </read_first>
  <action>
    1. In `src/Pellmonsrv/plugins/calculate/__init__.py`:
       Remove line 28: `from string import maketrans`.
    2. In `src/Pellmonsrv/plugins/calculate/__init__.py` line 351:
       Change `stack = [unicode(value)]` to `stack = [str(value)]`.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "import ast; tree = ast.parse(open('src/Pellmonsrv/plugins/calculate/__init__.py', 'rb').read()); assert 'maketrans' not in open('src/Pellmonsrv/plugins/calculate/__init__.py').read(); assert 'unicode(' not in open('src/Pellmonsrv/plugins/calculate/__init__.py').read()"</automated>
  </verify>
  <done>Neither maketrans nor unicode() remain in calculate/__init__.py.</done>
</task>

<task type="auto">
  <name>Task 2: Fix daemon.py stderr redirection buffering</name>
  <files>src/Pellmonsrv/daemon.py</files>
  <read_first>
    - src/Pellmonsrv/daemon.py (lines 65-80)
  </read_first>
  <action>
    In `src/Pellmonsrv/daemon.py` line 72:
    Change:
    `se = open(self.stderr, 'a+', buffering=0)`
    to:
    `se = open(self.stderr, 'a+', buffering=1)`
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "import tempfile, os; f = tempfile.NamedTemporaryFile(delete=False); f.close(); se = open(f.name, 'a+', buffering=1); se.write('test\n'); se.close(); os.unlink(f.name)"</automated>
  </verify>
  <done>daemon.py uses buffering=1 for stderr redirection, compatible with Python 3 text I/O.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for Calculate hardening and daemon stderr buffering</name>
  <files>
    tests/Pellmonsrv/plugins/test_calculate_hardening.py
    tests/Pellmonsrv/test_daemon_hardening.py
  </files>
  <read_first>
    - tests/Pellmonsrv/plugins/test_calculate_logging.py
  </read_first>
  <action>
    1. Create `tests/Pellmonsrv/plugins/test_calculate_hardening.py`:
       - Verify that `maketrans` is not imported.
       - Test that calling `Calc` logic with string values executes without `NameError`.
       - Test `setItem` execution path with mock items and confirm `str(value)` is used.
    2. Create `tests/Pellmonsrv/test_daemon_hardening.py`:
       - Test `Daemon` initialization and verify the redirection open statement with `buffering=1` succeeds on a temporary file.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_calculate_hardening.py tests/Pellmonsrv/test_daemon_hardening.py -v</automated>
  </verify>
  <done>Tests verify that Calculate setItem and daemon stderr redirection operate without Python 3 errors.</done>
</task>

</tasks>

<success_criteria>
- `calculate/__init__.py` has no `maketrans` import and no `unicode()` calls.
- `daemon.py` opens stderr with `buffering=1`.
- `pytest tests/Pellmonsrv/plugins/test_calculate_hardening.py tests/Pellmonsrv/test_daemon_hardening.py -v` passes.
</success_criteria>
