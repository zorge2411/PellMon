---
phase: 04-protocol-module-hardening
plan: 04
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/Pellmonsrv/database.py
  - tests/Pellmonsrv/test_database.py
autonomous: true
requirements: [PROTO-01, PROTO-02, PROTO-03, PROTO-04, PROTO-05]
must_haves:
  truths:
    - "Keyval_storage.writeval preserves value column when updating confval (database.py:199)"
    - "test_database.py asserts both value and confval columns after writeval update"
    - "Full pytest suite passes with 0 failures and 0 regressions across all phases"
  artifacts:
    - path: "tests/Pellmonsrv/test_database.py"
      provides: "Strengthened Keyval_storage writeval test verifying value column preservation"
      contains: "test_writeval_with_confval_sets_both_columns"
---

<objective>
Fix the `Keyval_storage.writeval()` confval-upsert bug in `src/Pellmonsrv/database.py:199`, strengthen `tests/Pellmonsrv/test_database.py` to assert value preservation, and execute the full test suite as a phase-closeout verification gate.

Purpose: Eliminate the silent data-overwriting bug in database keyval persistence and verify that all Phase 4 hardening measures pass cleanly.
Output: Fixed `database.py`, strengthened database test, and complete passing test suite.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-module-hardening/04-CONTEXT.md
@.planning/phases/04-protocol-module-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Keyval_storage.writeval confval-upsert parameter binding and strengthen tests</name>
  <files>
    src/Pellmonsrv/database.py
    tests/Pellmonsrv/test_database.py
  </files>
  <read_first>
    - src/Pellmonsrv/database.py (lines 190-205)
    - tests/Pellmonsrv/test_database.py (test_writeval_with_confval_sets_both_columns)
  </read_first>
  <action>
    1. In `src/Pellmonsrv/database.py` line 199:
       Change:
       `cursor.execute("INSERT OR REPLACE INTO keyval (id, value, confvalue) VALUES (?,?,?)", (item, confval, confval))`
       to:
       `cursor.execute("INSERT OR REPLACE INTO keyval (id, value, confvalue) VALUES (?,?,?)", (item, value, confval))`

    2. In `tests/Pellmonsrv/test_database.py`:
       Strengthen `test_writeval_with_confval_sets_both_columns` (or add a dedicated test):
       - First call `writeval("test_item", "initial_value")`.
       - Then call `writeval("test_item", confval="new_conf")`.
       - Read both columns directly via SQL query and assert that `value == "initial_value"` and `confvalue == "new_conf"`.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_database.py -v</automated>
  </verify>
  <done>Keyval_storage.writeval correctly preserves the existing value column when setting confval, verified by test.</done>
</task>

<task type="auto">
  <name>Task 2: Full Phase 4 test suite gate</name>
  <files>tests/</files>
  <read_first>
    - .planning/phases/04-protocol-module-hardening/04-VALIDATION.md
  </read_first>
  <action>
    Run the full pytest suite:
    `venv-py3/Scripts/python.exe -m pytest tests/ -v`

    Verify that:
    1. All tests pass (except platform-dependent skips).
    2. Zero failures occur.
    3. Zero known_broken tests exist.
    4. All newly added Phase 4 tests pass (`test_calculate_hardening`, `test_daemon_hardening`, `test_nbe_protocol_roundtrip`, `test_scotte_protocol_roundtrip`, `test_database`).
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <done>Full test suite passes with 0 failures, proving all Phase 4 hardening goals are met.</done>
</task>

</tasks>

<success_criteria>
- `database.py` does not overwrite `value` with `confval`.
- All Phase 4 unit and round-trip tests pass.
- Full test suite passes green.
</success_criteria>
