---
phase: 04-protocol-module-hardening
plan: 03
wave: 1
depends_on: []
files_modified:
  - src/Scotteprotocol/protocol.py
  - tests/test_scotte_protocol_roundtrip.py
autonomous: true
requirements: [PROTO-04]
must_haves:
  truths:
    - "Scotteprotocol.Protocol accepts an injectable transport parameter in __init__ (PROTO-04)"
    - "Per-frame-type encode and decode logic executes cleanly over mocked/loop serial without hardware"
    - "Checksum calculation and verification functions behave deterministically on string and byte inputs"
  artifacts:
    - path: "tests/test_scotte_protocol_roundtrip.py"
      provides: "Automated frame encode/decode round-trip tests for Scotteprotocol"
      min_lines: 45
---

<objective>
Enable constructor-injectable transport in `Scotteprotocol.Protocol` and implement per-frame-type encode/decode round-trip tests using the Phase 1 `loop_serial` fixture (`loop://`) without physical burner hardware (PROTO-04).

Purpose: Provide automated regression protection for Scotte serial communication and frame parsing.
Output: Injectable `Scotteprotocol.Protocol` and comprehensive round-trip tests in `tests/test_scotte_protocol_roundtrip.py`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-module-hardening/04-CONTEXT.md
@.planning/phases/04-protocol-module-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add constructor-injectable transport to Scotteprotocol.Protocol</name>
  <files>src/Scotteprotocol/protocol.py</files>
  <read_first>
    - src/Scotteprotocol/protocol.py (lines 30-70)
  </read_first>
  <action>
    In `src/Scotteprotocol/protocol.py`:
    Update `Protocol.__init__` signature:
    `def __init__(self, device, version_string, transport=None, start_thread=True):`
    
    If `transport is not None`:
        `self.dummyDevice = False`
        `self.ser = transport`
        `self.dataBase = self.createDataBase('6.99' if not version_string else version_string)`
        `self.q = queue.Queue(300)`
        `threading.Thread.__init__(self)`
        `self.setDaemon(True)`
        `if start_thread: self.start()`
        return

    If `device is None`:
        `self.dummyDevice = True`
        `self.dataBase = self.createDataBase('6.99')`
        return

    Keep normal `serial.Serial()` opening when `transport is None` and `device is not None`.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "import Scotteprotocol, serial; ser = serial.serial_for_url('loop://', timeout=1); p = Scotteprotocol.Protocol(None, '6.99', transport=ser, start_thread=False); assert p.ser == ser; ser.close()"</automated>
  </verify>
  <done>Scotteprotocol.Protocol accepts an injectable transport and can be instantiated without real serial ports.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Scotte protocol per-frame round-trip tests</name>
  <files>tests/test_scotte_protocol_roundtrip.py</files>
  <read_first>
    - src/Scotteprotocol/frames.py
    - src/Scotteprotocol/protocol.py (checksum and Frame.parse methods)
    - src/Scotteprotocol/datamap.py
    - tests/conftest.py (loop_serial fixture)
  </read_first>
  <action>
    Create `tests/test_scotte_protocol_roundtrip.py`:
    1. Test `addCheckSum` and `checkCheckSum`:
       - Verify checksum on sample strings (e.g. `'Z000000'`).
       - Verify that `checkCheckSum(addCheckSum(msg)) == 0` (checksum verification passes).
       - Verify that tampering with a character produces a nonzero checksum (failure).
    2. Test Frame parsing for various frame types (`FrameZ00`, `FrameZ01`, `FrameZ02`, `FrameZ03`):
       - Create synthetic response frames matching the field length definition.
       - Append valid checksum.
       - Call `frame.parse(response_string, protocol)` and assert it returns `True`.
       - Assert parsed values match expected field values.
    3. Test serial loop round-trip with `loop_serial` fixture:
       - Instantiate `Protocol(None, '6.99', transport=loop_serial, start_thread=False)`.
       - Write a command to `loop_serial` and verify `protocol.ser.read()` reads it correctly.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/test_scotte_protocol_roundtrip.py -v</automated>
  </verify>
  <done>Round-trip tests verify Scotte frame formatting, checksum validation, and serial I/O without hardware.</done>
</task>

</tasks>

<success_criteria>
- `Scotteprotocol.Protocol` accepts `transport` and `start_thread` parameters.
- `pytest tests/test_scotte_protocol_roundtrip.py -v` passes.
</success_criteria>
