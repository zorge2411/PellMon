---
phase: 04-protocol-module-hardening
plan: 02
wave: 1
depends_on: []
files_modified:
  - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py
  - tests/Pellmonsrv/plugins/test_nbe_protocol_roundtrip.py
autonomous: true
requirements: [PROTO-01, PROTO-04]
must_haves:
  truths:
    - "Proxy.get() returns split payload strings without raising TypeError (PROTO-01)"
    - "nbeprotocol.Proxy accepts an injectable transport parameter in __init__ (PROTO-04)"
    - "NBE request/response round-trip tests execute and pass without real hardware or network sockets"
  artifacts:
    - path: "tests/Pellmonsrv/plugins/test_nbe_protocol_roundtrip.py"
      provides: "Unit test for NBE protocol payload splitting and mocked transport round-trips"
      min_lines: 40
---

<objective>
Harden the NBE protocol implementation: fix the `TypeError` in `Proxy.get()` caused by inappropriate `.encode('ascii')` before string splitting (PROTO-01), add a constructor-injectable `transport` parameter to `Proxy` (PROTO-04), and create automated round-trip tests.

Purpose: Allow NBEcom to read single parameters and parameter groups correctly without crashing, and enable automated testing without network sockets.
Output: Fixed `nbeprotocol/protocol.py` and a new test suite in `test_nbe_protocol_roundtrip.py`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-module-hardening/04-CONTEXT.md
@.planning/phases/04-protocol-module-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bytes/str split in Proxy.get() and add injectable transport to Proxy.__init__</name>
  <files>src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py</files>
  <read_first>
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py (lines 40-65, lines 135-155)
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py (lines 170-194)
  </read_first>
  <action>
    1. In `src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py`:
       Update `Proxy.__init__` signature to:
       `def __init__(self, password, port=1920, addr=None, serial=None, transport=None, start_threads=True):`
       If `transport is not None`:
           `self.s = transport`
       else:
           `s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)`
           ...
           `self.s = s`
       If `start_threads`:
           start the background `find_controller` and `xtea_refresh_thread` threads.

    2. In `Proxy.get()` (lines 143-146):
       Change:
       ```python
       if not group:
           return response.payload.encode('ascii').split('=', 1)[1]
       else:
           return response.payload.encode('ascii').split(';')
       ```
       to:
       ```python
       if not group:
           return response.payload.split('=', 1)[1]
       else:
           return response.payload.split(';')
       ```
       (Since `response.payload` is already a `str` decoded from ascii/latin-1).
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "from Pellmonsrv.plugins.nbecom.nbeprotocol.protocol import Proxy; p = Proxy('pass', transport='mock', start_threads=False); assert p.s == 'mock'"</automated>
  </verify>
  <done>Proxy accepts injectable transport without opening real sockets, and Proxy.get() splits on str without TypeError.</done>
</task>

<task type="auto">
  <name>Task 2: Implement NBE protocol frame round-trip and Proxy.get() tests</name>
  <files>tests/Pellmonsrv/plugins/test_nbe_protocol_roundtrip.py</files>
  <read_first>
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py
  </read_first>
  <action>
    Create `tests/Pellmonsrv/plugins/test_nbe_protocol_roundtrip.py`:
    1. Test `Request_frame` encode/decode: verify header length, sequencenumber, and payload formatting.
    2. Test `Response_frame` encode/decode: verify response payload parsing into dictionary with `parse_payload()`.
    3. Test `Proxy.get()` with mocked `make_request()`:
       - Test single parameter: response payload `"boiler.temp=65.5"`, assert returns `"65.5"`.
       - Test parameter group (`group=True`): response payload `"temp=65.5;state=3;power=10"`, assert returns `['temp=65.5', 'state=3', 'power=10']`.
       - Assert no `TypeError` occurs during splitting.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_nbe_protocol_roundtrip.py -v</automated>
  </verify>
  <done>NBE protocol round-trip and get() tests pass with 100% verified string semantics.</done>
</task>

</tasks>

<success_criteria>
- `Proxy.get()` does not raise `TypeError`.
- `Proxy` accepts `transport` and `start_threads=False` parameters.
- `pytest tests/Pellmonsrv/plugins/test_nbe_protocol_roundtrip.py -v` passes.
</success_criteria>
