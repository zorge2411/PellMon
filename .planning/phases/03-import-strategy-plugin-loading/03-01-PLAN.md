---
phase: 03-import-strategy-plugin-loading
plan: 01
wave: 1
depends_on: []
files_modified:
  - src/Scotteprotocol/__init__.py
  - src/Scotteprotocol/frames.py
  - src/Scotteprotocol/datamap.py
  - src/Scotteprotocol/protocol.py
  - src/Pellmonsrv/plugins/scottecom/__init__.py
  - src/Pellmonsrv/plugins/scottecom/scottecom.py
  - src/Pellmonsrv/plugins/scottecom/menus.py
  - tests/Pellmonsrv/test_scotteprotocol_logging.py
autonomous: true
requirements: [IMPORT-01, OBS-02]
must_haves:
  truths:
    - "Scotteprotocol intra-package imports use explicit relative syntax (PEP 328)"
    - "Pellmonsrv.plugins.scottecom no longer mutates sys.path and uses explicit relative imports"
    - "src/Scotteprotocol/protocol.py bare/broad except clauses are converted to logger.exception or specific exceptions"
    - "Scotteprotocol imports cleanly under Python 3 and emits proper tracebacks on error"
  artifacts:
    - path: "tests/Pellmonsrv/test_scotteprotocol_logging.py"
      provides: "Unit test for Scotteprotocol imports and exception logging"
      min_lines: 30
---

<objective>
Fix all implicit relative imports in `src/Scotteprotocol/` and `src/Pellmonsrv/plugins/scottecom/`, remove the `sys.path.append` shim, and execute the inherited exception-visibility retrofit on `src/Scotteprotocol/protocol.py`.

Purpose: Enable Scotteprotocol and ScotteCom to load and activate under Python 3 (IMPORT-01) while making failure paths observable with full tracebacks (OBS-02).
Output: Working Scotteprotocol package and ScotteCom plugin with zero implicit imports, zero sys.path hacks, and proper exception logging.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-import-strategy-plugin-loading/03-CONTEXT.md
@.planning/phases/03-import-strategy-plugin-loading/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix intra-package relative imports in Scotteprotocol and ScotteCom</name>
  <files>
    src/Scotteprotocol/__init__.py
    src/Scotteprotocol/frames.py
    src/Scotteprotocol/datamap.py
    src/Scotteprotocol/protocol.py
    src/Pellmonsrv/plugins/scottecom/__init__.py
    src/Pellmonsrv/plugins/scottecom/scottecom.py
    src/Pellmonsrv/plugins/scottecom/menus.py
  </files>
  <read_first>
    - src/Scotteprotocol/__init__.py
    - src/Scotteprotocol/frames.py
    - src/Scotteprotocol/datamap.py
    - src/Scotteprotocol/protocol.py (lines 20-35 and line 216)
    - src/Pellmonsrv/plugins/scottecom/__init__.py
    - src/Pellmonsrv/plugins/scottecom/scottecom.py (lines 18-28)
    - src/Pellmonsrv/plugins/scottecom/menus.py (lines 18-25)
  </read_first>
  <action>
    1. In `src/Scotteprotocol/__init__.py`:
       Change `from protocol import Protocol` to `from .protocol import Protocol`.

    2. In `src/Scotteprotocol/frames.py`:
       Change `from protocol import Frame` to `from .protocol import Frame`.

    3. In `src/Scotteprotocol/datamap.py`:
       Change `from frames import *` to `from .frames import *`.

    4. In `src/Scotteprotocol/protocol.py`:
       Change `from enumerations import dataEnumerations` to `from .enumerations import dataEnumerations`.
       Change `from transformations import dataTransformations` to `from .transformations import dataTransformations`.
       In `createDataBase` (line 216), change `from datamap import dataBaseMap` to `from .datamap import dataBaseMap`.

    5. In `src/Pellmonsrv/plugins/scottecom/__init__.py`:
       Remove line 6: `sys.path.append(os.path.dirname(os.path.abspath(__file__)))`.
       Change `from scottecom import scottecom` to `from .scottecom import scottecom`.

    6. In `src/Pellmonsrv/plugins/scottecom/scottecom.py`:
       Change `import menus` to `from . import menus`.
       Change `from descriptions import dataDescriptions` to `from .descriptions import dataDescriptions`.

    7. In `src/Pellmonsrv/plugins/scottecom/menus.py`:
       Change `from datamenu import dataBaseTags` to `from .datamenu import dataBaseTags`.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "import Scotteprotocol; from Scotteprotocol import Protocol; import Pellmonsrv.plugins.scottecom"</automated>
  </verify>
  <done>Scotteprotocol and Pellmonsrv.plugins.scottecom import cleanly without ModuleNotFoundError or sys.path mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit exception visibility in Scotteprotocol/protocol.py</name>
  <files>src/Scotteprotocol/protocol.py</files>
  <read_first>
    - src/Scotteprotocol/protocol.py (all bare `except:` sites)
    - .planning/phases/03-import-strategy-plugin-loading/03-RESEARCH.md (Inherited Exception Visibility Audit)
  </read_first>
  <action>
    Retrofit the bare/broad `except` blocks in `src/Scotteprotocol/protocol.py` according to Phase 2 dispositions:
    - Ensure `logger = getLogger('pellMon')` is used (already present at line 27).
    - Serial communication and command dispatch error paths (lines 52, 177, 249, 279, etc.): convert unhandled exceptions to `logger.exception(...)` with full traceback attached.
    - Type conversion / dictionary lookup fallbacks (e.g. lines 73, 79, 84, 90, 97, 102, 107, 140, 143, 146, 156, 163, 166, 187, 209): narrow bare `except:` to specific types such as `except (ValueError, KeyError, TypeError, IndexError):` preserving the fallback return value or logging at `logger.debug`.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "import Scotteprotocol.protocol; p = Scotteprotocol.Protocol(None, '6.99'); assert len(p.dataBase) > 0"</automated>
  </verify>
  <done>Zero bare `except:` clauses remain in `src/Scotteprotocol/protocol.py`; real errors are logged via `logger.exception` and fallbacks use narrow exception handling.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for Scotteprotocol imports and exception logging</name>
  <files>tests/Pellmonsrv/test_scotteprotocol_logging.py</files>
  <read_first>
    - tests/Pellmonsrv/test_pellmonsrv_logging.py
    - src/Scotteprotocol/protocol.py
  </read_first>
  <action>
    Create `tests/Pellmonsrv/test_scotteprotocol_logging.py` covering:
    1. Import of `Scotteprotocol`, `Protocol`, and dummy database creation (`Protocol(None, '6.99')`).
    2. A test using `caplog` verifying that an unexpected error during protocol operations (e.g. simulated read error) logs via `logger.exception` with traceback info (`record.exc_info is not None`).
    3. Import of `Pellmonsrv.plugins.scottecom` and instantiation of the plugin class.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/test_scotteprotocol_logging.py -v</automated>
  </verify>
  <done>Unit tests verify Scotteprotocol imports and exception logging behavior.</done>
</task>

</tasks>

<success_criteria>
- Scotteprotocol and ScotteCom import cleanly under Python 3 without sys.path hacks.
- `src/Scotteprotocol/protocol.py` contains no bare `except:` clauses.
- `pytest tests/Pellmonsrv/test_scotteprotocol_logging.py -v` passes.
</success_criteria>
