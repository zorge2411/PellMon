---
phase: 03-import-strategy-plugin-loading
plan: 03
wave: 1
depends_on: []
files_modified:
  - src/Pellmonsrv/yapsy/ConfigurablePluginManager.py
  - src/Pellmonsrv/yapsy/VersionedPluginManager.py
  - tests/test_no_sys_path_shims.py
autonomous: true
requirements: [IMPORT-03]
must_haves:
  truths:
    - "yapsy managers use explicit relative imports (PEP 328)"
    - "No sys.path.append or sys.path.insert shims remain anywhere in src/"
    - "An AST enforcement test validates that no implicit sibling imports exist in src/"
  artifacts:
    - path: "tests/test_no_sys_path_shims.py"
      provides: "AST enforcement test for zero sys.path shims and PEP 328 compliance"
      min_lines: 40
---

<objective>
Standardize yapsy internal imports on explicit relative syntax and add an AST-based enforcement test (`tests/test_no_sys_path_shims.py`) ensuring no `sys.path` shims or implicit sibling imports remain in `src/` (IMPORT-03).

Purpose: Turn the "no sys.path shims and explicit relative imports" requirement into an automated, non-regressing gate.
Output: Clean yapsy package and an AST enforcement test passing in the test suite.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-import-strategy-plugin-loading/03-CONTEXT.md
@.planning/phases/03-import-strategy-plugin-loading/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix intra-package relative imports in yapsy managers</name>
  <files>
    src/Pellmonsrv/yapsy/ConfigurablePluginManager.py
    src/Pellmonsrv/yapsy/VersionedPluginManager.py
  </files>
  <read_first>
    - src/Pellmonsrv/yapsy/ConfigurablePluginManager.py (lines 10-25)
    - src/Pellmonsrv/yapsy/VersionedPluginManager.py (lines 10-20)
  </read_first>
  <action>
    1. In `src/Pellmonsrv/yapsy/ConfigurablePluginManager.py`:
       Change `from IPlugin import IPlugin` to `from .IPlugin import IPlugin`.
       Change `from PluginManager import PluginManager, PluginManagerDecorator` to `from .PluginManager import PluginManager, PluginManagerDecorator`.
       Change `from PluginManager import PLUGIN_NAME_FORBIDDEN_STRING` to `from .PluginManager import PLUGIN_NAME_FORBIDDEN_STRING`.

    2. In `src/Pellmonsrv/yapsy/VersionedPluginManager.py`:
       Change `from PluginManager import PluginManager` to `from .PluginManager import PluginManager`.
       Change `from IPlugin import IPlugin` to `from .IPlugin import IPlugin`.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "import Pellmonsrv.yapsy.ConfigurablePluginManager; import Pellmonsrv.yapsy.VersionedPluginManager"</automated>
  </verify>
  <done>yapsy managers import cleanly using PEP 328 relative imports.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AST-based sys.path shim and relative import enforcement test</name>
  <files>tests/test_no_sys_path_shims.py</files>
  <read_first>
    - tests/test_no_ad_hoc_print.py (pattern for AST-based codebase scanning)
  </read_first>
  <action>
    Create `tests/test_no_sys_path_shims.py`:
    1. Walk all `*.py` files in `src/` (excluding `.py2bak`).
    2. Check for `sys.path.append` or `sys.path.insert` call nodes. Assert that the set of offending sites is empty.
    3. Check for implicit sibling imports in packages: parse ASTs for `Import` and `ImportFrom` with `level == 0`, and detect if the target module is a sibling in the same package directory (excluding permitted top-level imports). Assert no implicit sibling imports remain in `Scotteprotocol`, `scottecom`, `nbecom`, or `yapsy`.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/test_no_sys_path_shims.py -v</automated>
  </verify>
  <done>Automated test enforces zero sys.path shims and clean explicit relative imports across src/.</done>
</task>

</tasks>

<success_criteria>
- yapsy managers import cleanly without warnings or errors.
- `pytest tests/test_no_sys_path_shims.py -v` passes.
</success_criteria>
