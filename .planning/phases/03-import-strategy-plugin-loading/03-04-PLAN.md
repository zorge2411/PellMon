---
phase: 03-import-strategy-plugin-loading
plan: 04
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - tests/test_protocol_coexistence.py
  - tests/test_plugin_imports.py
  - tests/README.md
autonomous: true
requirements: [IMPORT-01, IMPORT-02, IMPORT-04]
must_haves:
  truths:
    - "Scotteprotocol and nbeprotocol load in the same process with zero namespace collision"
    - "test_plugin_module_imports[scottecom] and test_nbecom_deferred_protocol_import pass cleanly"
    - "KNOWN_BROKEN_MODULES is empty and tests/README.md records a 0-failure green baseline"
  artifacts:
    - path: "tests/test_protocol_coexistence.py"
      provides: "Automated test verifying simultaneous loading of both protocol modules"
      min_lines: 30
---

<objective>
Verify that `Scotteprotocol` and `nbeprotocol` coexist in the same process without collision (IMPORT-04), clear the `known_broken` marks from `tests/test_plugin_imports.py`, update `tests/README.md`, and verify the entire test suite runs green.

Purpose: Validate that all Phase 3 success criteria are fulfilled and the test suite has 0 expected-red baseline failures.
Output: Coexistence test, clean green `tests/test_plugin_imports.py`, and updated test baseline documentation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-import-strategy-plugin-loading/03-CONTEXT.md
@.planning/phases/03-import-strategy-plugin-loading/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add automated protocol coexistence test</name>
  <files>tests/test_protocol_coexistence.py</files>
  <read_first>
    - src/Scotteprotocol/protocol.py
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py
  </read_first>
  <action>
    Create `tests/test_protocol_coexistence.py`:
    1. In a single test case, import both `Scotteprotocol.protocol` and `Pellmonsrv.plugins.nbecom.nbeprotocol.protocol`.
    2. Assert that `sys.modules['Scotteprotocol.protocol']` and `sys.modules['Pellmonsrv.plugins.nbecom.nbeprotocol.protocol']` are distinct module objects.
    3. Assert that `sys.modules['Scotteprotocol.frames']` and `sys.modules['Pellmonsrv.plugins.nbecom.nbeprotocol.frames']` are distinct module objects.
    4. Instantiate `Scotteprotocol.Protocol(None, '6.99')` and `nbeprotocol.Proxy('testpass')` in the same test, confirming neither interferes with the other's operation or attributes.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/test_protocol_coexistence.py -v</automated>
  </verify>
  <done>Protocol coexistence test passes, proving zero duplicate module collision.</done>
</task>

<task type="auto">
  <name>Task 2: Clear known_broken marks and update tests/README.md</name>
  <files>
    tests/test_plugin_imports.py
    tests/README.md
  </files>
  <read_first>
    - tests/test_plugin_imports.py (lines 38-43 and lines 100-113)
    - tests/README.md
  </read_first>
  <action>
    1. In `tests/test_plugin_imports.py`:
       Set `KNOWN_BROKEN_MODULES = set()`.
       Remove `@pytest.mark.known_broken` from `test_nbecom_deferred_protocol_import()`.
       Update module docstring noting that Phase 3 has resolved both previously-broken imports.

    2. In `tests/README.md`:
       Update the "Expected-red baseline" section to declare 0 expected-red failures (both ScotteCom and NBEcom now pass cleanly under Python 3).

    3. Run the full pytest suite to verify all non-skipped tests pass.
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/ -v</automated>
  </verify>
  <done>test_plugin_imports.py has zero known_broken tests, all tests pass, and tests/README.md reflects the green baseline.</done>
</task>

</tasks>

<success_criteria>
- `pytest tests/test_protocol_coexistence.py -v` passes.
- `pytest tests/test_plugin_imports.py -v` passes with 0 failures and 0 known_broken.
- `pytest tests/ -v` passes with 0 failures.
</success_criteria>
