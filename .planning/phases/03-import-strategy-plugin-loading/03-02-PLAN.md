---
phase: 03-import-strategy-plugin-loading
plan: 02
wave: 1
depends_on: []
files_modified:
  - src/Pellmonsrv/plugins/nbecom/__init__.py
  - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py
  - src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py
  - src/Pellmonsrv/plugins/nbecom/nbeprotocol/language.py
  - tests/Pellmonsrv/plugins/test_nbecom_imports.py
autonomous: true
requirements: [IMPORT-02]
must_haves:
  truths:
    - "nbeprotocol intra-package imports use explicit relative syntax (PEP 328)"
    - "Pellmonsrv.plugins.nbecom no longer mutates sys.path and uses explicit relative imports"
    - "Pellmonsrv.plugins.nbecom.nbeprotocol.protocol imports cleanly under Python 3"
  artifacts:
    - path: "tests/Pellmonsrv/plugins/test_nbecom_imports.py"
      provides: "Unit test for NBEcom and nbeprotocol import and instantiation"
      min_lines: 25
---

<objective>
Fix all implicit relative imports in `src/Pellmonsrv/plugins/nbecom/` and its `nbeprotocol/` subpackage, remove the `sys.path.append` shim, and verify that NBEcom and its protocol proxy load cleanly under Python 3 (IMPORT-02).

Purpose: Enable NBEcom and nbeprotocol to import without errors, isolating them from module shadowing.
Output: Self-contained NBEcom plugin and nbeprotocol package with clean explicit relative imports.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-import-strategy-plugin-loading/03-CONTEXT.md
@.planning/phases/03-import-strategy-plugin-loading/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix intra-package relative imports in NBEcom and nbeprotocol</name>
  <files>
    src/Pellmonsrv/plugins/nbecom/__init__.py
    src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py
    src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py
    src/Pellmonsrv/plugins/nbecom/nbeprotocol/language.py
  </files>
  <read_first>
    - src/Pellmonsrv/plugins/nbecom/__init__.py (lines 20-45)
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py (lines 20-35)
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py (lines 18-26)
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/language.py (lines 18-30)
  </read_first>
  <action>
    1. In `src/Pellmonsrv/plugins/nbecom/__init__.py`:
       Remove line 28: `sys.path.append(os.path.dirname(os.path.abspath(__file__)))`.
       Change `from nbeprotocol.protocol import Proxy` to `from .nbeprotocol.protocol import Proxy`.
       Change `from nbeprotocol.language import event_text, state_text, lang_longname, lang_description` to:
       `from .nbeprotocol.language import event_text, state_text, lang_longname, lang_description`.

    2. In `src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py`:
       Change `from frames import Request_frame, Response_frame` to `from .frames import Request_frame, Response_frame`.
       Change `from protocolexceptions import *` to `from .protocolexceptions import *`.
       Change `import language` to `from . import language`.

    3. In `src/Pellmonsrv/plugins/nbecom/nbeprotocol/frames.py`:
       Change `from protocolexceptions import *` to `from .protocolexceptions import *`.

    4. In `src/Pellmonsrv/plugins/nbecom/nbeprotocol/language.py`:
       Change `from langmap import langmap` to `from .langmap import langmap`.
       Change `from directories import DATADIR` to:
       ```python
       try:
           from Pellmonsrv.directories import DATADIR
           langfile = os.path.join(DATADIR, 'Pellmonsrv', 'plugins', 'nbecom', 'lang.uk.prop')
       except ImportError:
           langfile = os.path.join(os.path.dirname(os.path.realpath(__file__)), 'language', 'lang.uk.prop')
       ```
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -c "import Pellmonsrv.plugins.nbecom; import Pellmonsrv.plugins.nbecom.nbeprotocol.protocol; from Pellmonsrv.plugins.nbecom.nbeprotocol.protocol import Proxy"</automated>
  </verify>
  <done>NBEcom and its nbeprotocol subpackage import cleanly without ModuleNotFoundError or sys.path mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for NBEcom and nbeprotocol imports</name>
  <files>tests/Pellmonsrv/plugins/test_nbecom_imports.py</files>
  <read_first>
    - src/Pellmonsrv/plugins/nbecom/__init__.py
    - src/Pellmonsrv/plugins/nbecom/nbeprotocol/protocol.py
  </read_first>
  <action>
    Create `tests/Pellmonsrv/plugins/test_nbecom_imports.py` covering:
    1. Import of `Pellmonsrv.plugins.nbecom` and instantiation of `nbecomplugin`.
    2. Direct import of `Pellmonsrv.plugins.nbecom.nbeprotocol.protocol` and instantiation of `Proxy('testpass')`.
    3. Direct import of `Pellmonsrv.plugins.nbecom.nbeprotocol.frames` and creation of `Request_frame()`.
    4. Assert that `Proxy` has expected attributes (`controller_online == False`, etc.).
  </action>
  <verify>
    <automated>venv-py3/Scripts/python.exe -m pytest tests/Pellmonsrv/plugins/test_nbecom_imports.py -v</automated>
  </verify>
  <done>Unit test verifies NBEcom and nbeprotocol import and basic instantiation.</done>
</task>

</tasks>

<success_criteria>
- NBEcom and nbeprotocol import cleanly under Python 3 without sys.path hacks.
- `pytest tests/Pellmonsrv/plugins/test_nbecom_imports.py -v` passes.
</success_criteria>
