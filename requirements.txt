# Core dependencies - Python 3 versions
# Note: Some dependencies are platform-specific and may need manual installation

# Cross-platform dependencies
pyserial>=3.5
CherryPy>=18.8.0
Mako>=1.2.0
python-dateutil>=2.8.2
argcomplete>=2.0.0

# Platform-specific dependencies (Linux/Unix)
# rrdtool>=1.7.0  # Requires system package: apt-get install python3-rrdtool
# dbus-python>=1.2.18  # Requires system package: apt-get install python3-dbus
# PyGObject>=3.42.0  # Requires system package: apt-get install python3-gi

# Optional dependencies
ws4py>=0.5.1

# Plugin dependencies
# OWFS plugin
pyownet>=0.10.0

# NBEcom plugin (replaces python-crypto with pycryptodome)
pycryptodome>=3.15.0
xtea>=0.7.1

# Openweathermap plugin
pyowm>=3.3.0

